# NAN 2026 Murder Mystery — Development History

> 기존 `history.md`(Vanguard-Agent)와는 별개 프로젝트이므로 신규 히스토리 파일로 분리한다.
> 형식은 프로토콜 2 (Phase 단위, "무엇을 했는가" + "Technical Rationale")를 그대로 따른다.

## Phase 1-Documentation: 설계 인계 문서화 및 최소 검증 계획 수립 (Completed)

- 사전 대화창에서 확정된 설계(사건 진실, 페르소나 8유형, 증거 14종, 3계층 AI 아키텍처, 점수/UI 스펙)를 정식 문서 4종으로 정리했다: `00_prototype_validation_plan.md`, `01_truth_bible.md`, `02_persona_design.md`, `03_character_sheets.md`, `04_game_loop_flow.json`.
- 캐릭터 시트를 "플레이어 공개용"과 "AI 시스템 프롬프트용"이 하나의 소스에서 `visibility` 필드(`player`/`ai_only`/`both`)로 파생되는 단일 스키마 구조로 설계했다.
- 게임 루프를 flow-to-ai Semantic IR v0.1.0(Top-to-Bottom, §5.5 규칙 준수)으로 작성해, 브리핑 → 조사/심문 라운드 루프(3회) → 최종 지목 → 채점 흐름을 코드 착수 전 시각 검증 가능한 형태로 고정했다.

- **Technical Rationale (검증 우선순위 재배열)**:
  - 원 요청은 "문서화 → 다음 단계 준비"였지만, 리스크 목록의 "AI가 진실 성서 밖 정보를 지어낼 위험"이 프로젝트 전체를 무효화할 수 있는 단일 실패점이라 판단해, 문서 목록 최상단에 `00_prototype_validation_plan.md`(1용의자·1페르소나 최소 검증안)를 배치했다.
  - 검증 목표를 4개 지표(V1 진실 이탈, V2 반응모드 분리, V3 페르소나 일관성, V4 fair-play 붕괴)로 정량화해, "느낌상 안정적이다"가 아니라 통과/실패를 판정 가능한 기준으로 만들었다.
  - 검증 조합 우선순위를 "진범×압박내성낮음"부터 시작하도록 설계했다 — 실토 트리거가 가장 자주 발동되는 조합이 진실 이탈 위험이 가장 크기 때문이다.

- **Technical Rationale (단일 소스 캐릭터 시트)**:
  - `is_culprit`, `alibi_status` 같은 필드를 플레이어 뷰와 AI 뷰로 각각 손으로 유지하면, 두 문서가 갈라지며 진실 성서와의 정합성이 깨질 위험이 있다.
  - 이를 막기 위해 필드 단위 `visibility` 태그로 단일 YAML/JSON 소스에서 두 뷰를 파생시키는 구조를 채택했다.

- **Technical Rationale (실패 에스컬레이션 로그 설계, 프로토콜 4 관련)**:
  - LLM 자유생성 비중이 높은 프로젝트 특성상 "대화창에서 프롬프트를 즉흥적으로 고쳐가며 재시도"하는 방식은 원인 특정이 어렵다고 판단했다.
  - 턴 단위(TIMESTAMP/ROUND/ROLE/PERSONA/PRESSURE/INPUT/MODE_CLASSIFIED/OUTPUT/TRUTH_CHECK)로 구조화된 로그 스키마를 최소 검증 계획에 포함시켜, 동일 이탈 패턴이 2~3회 반복될 경우 로그 파일을 직접 열어 구조적으로 분석하도록 설계했다.

## Phase 2-Validation: 1순위 조합 최소 검증 완료 및 게임 루프 설계 수정 (Completed)

- 이현우(진범) + ESTP(압박내성 낮음) 조합으로 수동 심문 테스트를 3라운드에 걸쳐 진행, V1(진실이탈)·V2(반응모드)·V3(페르소나 일관성)는 통과 확인.
- V4(fair-play 붕괴조건) 검증 중 최초 프롬프트가 증거 1개만으로 완전 자백까지 가는 결함을 발견 → "붕괴 조건은 서로 다른 증거 카테고리(CCTV/신발흙/진술) 중 2개 이상이 대화 전체에 걸쳐 누적 확정되어야 한다"는 규칙으로 프롬프트(09번)를 재설계.
- 게임 루프 Semantic IR(04)에서 "성향 카드 사전 공개" 노드를 제거 — 페르소나 성향을 게임 시작 전에 알려주지 않고 심문 중 어조로 파악하게 하는 편이 더 재미있다는 판단.

- **Technical Rationale (붕괴 조건을 누적 판정으로 변경)**:
  - 최초 설계 문구였던 "삼중교차"는 여러 턴에 걸친 증거 누적을 뜻했으나, 프롬프트 작성 시 "동시에"로 과도하게 좁혀 쓴 것이 원인이었다. "단순 언급"과 "구체적 확정 결과 제시"를 구분하는 안전장치는 유지했다.

- **Technical Rationale (성향 사전공개 제거)**:
  - 성향을 미리 알려주면 심문의 탐색적 재미가 사라진다는 피드백을 반영한 트레이드오프.

- **Technical Rationale (구현 우선 전환 결정)**:
  - 남은 8×3 페르소나 조합 전수 검증은 시간 대비 효율이 낮다고 판단, 1순위 조합 검증만으로 구현으로 전환하기로 했다.

## Phase 2.5-Content: 교차 목격 진술 추가로 추리 밀도 보강 (Completed)

- "타임라인/관계망이 단순해서 추리할 거리가 적다"는 피드백을 받아, `01_truth_bible.md`에 교차 목격 진술 5건(W1~W5)을 추가했다.
- 관계망에 박서연↔정민아(입사 동기) 관계를 추가해 3인 관계망을 완전 그래프에 가깝게 보강했다.

- **Technical Rationale**:
  - 새 점수 항목을 만들지 않고 기존 진술 증거 5종 안의 세부 콘텐츠로 편입시켰다.
  - W3(이현우가 박서연을 목격)처럼 범인이 무고자의 알리바이를 스스로 뒷받침하게 되는 진술을 의도적으로 넣어 fair-play 원칙을 서사적으로 더 설득력 있게 만들었다.

## Phase 3-Infra: LLM 프로바이더를 Anthropic API에서 NVIDIA NIM으로 전환 (Completed)

- Claude.ai Pro 구독과 Anthropic API가 별개 과금 체계임을 확인한 뒤, 비용 절감을 위해 무료 티어인 NVIDIA NIM(build.nvidia.com)으로 LLM 프로바이더를 전환하기로 결정.
- 검증 스크립트를 Anthropic SDK에서 OpenAI 호환 클라이언트(NVIDIA NIM 엔드포인트)로 재작성.

- **Technical Rationale**:
  - Anthropic Sonnet 기준으로 검증한 붕괴조건 로직을 모델 교체 후에도 그대로 신뢰하는 것은 위험하다고 판단 — instruction-following 능력은 모델마다 다르다.
  - 무료 티어의 레이트리밋(40RPM)이 본선 시연 중 걸림돌이 될 수 있다는 점은 미해결 리스크로 남겨두었다.

## Phase 4-Implementation: Claude Code로 MVP 구현 착수 (Completed)

- Next.js(App Router) + TypeScript + Tailwind CSS로 프로젝트를 처음부터 스캐폴딩했다.
- `01~04`, `08`번 문서를 `/lib/game-data/`의 TS 상수로 이식했다 — 캐릭터 시트는 `visibility` 필드 규칙을 `getPlayerView`/`getActorPromptView` 두 함수로 코드 레벨에서 강제했다.
- `POST /api/interrogate` 라우트 1개로 이현우+ESTP 조합만 먼저 구현하고, 최소 채팅 UI로 웹 환경에서도 09번 프롬프트가 실제로 붕괴조건을 지키는지 재검증했다.

- **Technical Rationale (10번 문서의 최우선 구현 순서를 그대로 따름)**:
  - "1순위 조합부터 실제 코드로 옮기고, 전체 배역/랜덤 배정은 그 다음"이라는 원칙을 그대로 채택 — 검증되지 않은 여러 조합을 한 번에 구현하면 실패 지점을 좁히기 어렵다.

## Phase 5-Infra: LLM 모델 재비교 및 diffusiongemma 전환 (Completed)

- Phase 3에서 정한 `deepseek-ai/deepseek-v4-flash` + `thinking:true, reasoning_effort:high` 조합으로 V1~V4 전부 실전 통과를 확인했으나, 응답 지연시간이 55~71초로 실사용에 부담된다는 점이 드러났다.
- `reasoning_effort: medium`으로 낮춰 재검증했으나 오히려 138~150초로 느려지는 것을 두 차례 독립 실행으로 재현 확인 → 폐기, `high`로 확정 복귀.
- 지연시간 문제 해결을 위해 NVIDIA NIM 카탈로그의 다른 모델 8종을 동일 3~4턴 시나리오로 비교했다:

  | 모델 | 속도 | 결과 |
  |---|---|---|
  | `deepseek-ai/deepseek-v4-flash` (high) | 55~71초 | 정확하나 느림 (1차 폴백) |
  | `deepseek-ai/deepseek-v4-flash` (medium) | 138~150초 | 오히려 더 느려 폐기 |
  | `deepseek-ai/deepseek-v4-pro` | ~3분 | 너무 느려 중단 |
  | `meta/llama-3.3-70b-instruct` | 55초 (개선 없음) | 한국어 대사에 힌디어/한자 혼입 → 폐기 |
  | `nvidia/nvidia-nemotron-nano-9b-v2` | 15.7초 | 마찬가지로 언어 깨짐 → 폐기 |
  | `openai/gpt-oss-20b` | 4.4~6.8초 | 빠르고 한국어 깨끗하나 증거 2개만으로 조기 완전자백 → 폐기 |
  | `openai/gpt-oss-120b` | 7.0~7.1초 | 20b와 동일한 조기 붕괴 결함(모델 크기 무관, gpt-oss 계열 공통 특성으로 판단) → 폐기 |
  | `google/diffusiongemma-26b-a4b-it` | **0.5~1초** | **채택** — 속도·품질·붕괴조건 정확도 모두 최고 |

- `google/diffusiongemma-26b-a4b-it`(병렬 토큰 생성 diffusion LLM)를 기본 모델로 확정했다. 4턴 전체 합산 응답시간이 3초 미만으로, `flash`(high) 1턴보다도 빠르다.

- **Technical Rationale (gpt-oss 계열 폐기)**:
  - 20b/120b 모델 크기가 다른데도 동일하게 "증거 카테고리 2개 + 직접질문"만으로 조기 붕괴한 것은, 모델 용량 문제가 아니라 gpt-oss 계열(harmony 포맷)이 다단계 카운팅 지시를 원 설계보다 관대하게 해석하는 계열 공통 특성으로 판단했다.
  - fair-play 붕괴조건 정확도는 응답 속도보다 우선순위가 높은 요구사항이므로, 속도가 가장 빨랐던 gpt-oss도 이 기준에서 탈락시켰다.

- **Technical Rationale (diffusiongemma 채택 근거)**:
  - deepseek 계열에서 검증했던 3턴 시나리오(A만 → A+B → A+B+C)를 동일하게 재현 — turn2(A+B)에서 조기 붕괴 없이 정확히 버텼고, turn3(A+B+C)에서도 한 번 더 명시적으로 다그쳐야 붕괴하는 모습을 보여, 오히려 사람 심문 심리상 자연스러운 지연으로 판단했다.
  - 폴백 전략: 이후 실사용에서 문제가 발견되면 `deepseek-ai/deepseek-v4-flash` + `thinking:high`로 되돌린다 (유일하게 V1~V4를 전부 흠결 없이 통과했던 조합, 단점은 속도).

## Phase 6-Expansion: 전체 게임 루프 확장 (Completed)

- **캐스팅 랜덤 배정**: 8개 페르소나 중 3개를 매판 랜덤 배정. DB 없는 서버리스(Vercel) 배포 제약 때문에 배정 결과를 서명·암호화 없는 base64 `castingToken`으로 클라이언트에 내려 세션 상태를 유지하는 방식을 채택했다(트레이드오프: 개발자도구로 디코드하면 페르소나 정보를 미리 볼 수 있음 — `is_culprit`급 치명적 정보가 아니라 원래도 결과 화면에 공개될 정보이므로 파티 게임 규모에서는 허용 가능한 리스크로 판단).
  - **검증(리뷰 피드백 반영)**: 실제 라이브 API를 호출해 `castingToken`을 디코드한 결과 `{"role-park-seoyeon":"INFJ","role-lee-hyunwoo":"ESFJ","role-jeong-mina":"ISFP"}` 형태로, `characterId → personaKey(MBTI)` 매핑 하나만 들어있음을 확인했다. `is_culprit`/`alibi_status`/`breakdown_trigger`/`motive_full` 등은 전혀 포함되지 않는다. 더 나아가 `is_culprit` 자체가 캐스팅으로 랜덤 배정되는 값이 아니라 `game-data/characters.ts`에 `role-lee-hyunwoo`만 `isCulprit: true`로 고정 하드코딩돼 있고 어떤 API 응답에도 직렬화되지 않으므로, 이 토큰 경로로 진범 정보가 새어나갈 방법은 구조적으로 없다.
- **3배역 전체 심문**: 박서연·정민아(무고자, 알리바이 절대 원칙)까지 `buildActorSystemPrompt`를 일반화해 지원.
- **조사 모드**: 물증 14종을 라운드별 공개 타이밍(`round1_base`/`round1_end`/`round2_end`/`round3_open`)에 맞춰 클릭 확보하는 UI. 연계 물증(신발흙 대조)의 사전 조건 해금 로직 포함.
- **전략가 계층**: 원 설계는 "라운드 종료마다(용의자당 3회=총 9회)" 트리거였으나, DB/세션이 없어 "라운드 경계"를 서버가 기억할 수 없으므로, diffusiongemma가 1초 미만으로 응답하는 것을 활용해 **매 심문 메시지마다** 회고를 새로 계산해 액터 프롬프트에 주입하는 방식으로 설계를 변경했다 — 빈도는 늘었지만 정보가 항상 최신 상태로 유지된다.
- **점수/결과 화면**: 원본 04번 문서의 "6x5+1x15+4x15+3x10+30=185"라는 점수 공식이 실제로는 165점으로 계산되는 자체 산술 오류를 발견 — 근거 없는 20점을 지어내 채우는 대신, 실제 계산되는 165점을 그대로 구현하고 불일치를 코드에 기록했다(이후 신발 물증 2종 추가와 효율 보너스로 만점은 195점으로 변경).
- **10번 문서 잔존 오류 발견**: "최우선 구현 순서" 2번 항목이 기술 스택을 NVIDIA NIM으로 갱신한 뒤에도 "Anthropic API 호출"이라는 이전 리비전 문구를 그대로 남기고 있던 것을 발견 — 실행에는 지장 없었으나(실제로는 NVIDIA NIM으로 구현) 문서 자체의 불일치로 기록.

- **Technical Rationale (점수 항목 임의 추가 금지)**:
  - "01_truth_bible.md에 없는 사실을 지어내지 않는다"는 프로젝트 원칙을 게임 디자인 수치에도 동일하게 적용 — 근거 없는 점수 항목을 만들어 185점을 억지로 맞추지 않았다.

## Phase 7-Balance: 실전 플레이 피드백 기반 게임 밸런스 수정 (Completed)

플레이 테스트 중 발견된 5가지 문제를 순서대로 수정했다.

1. **자백 스포일러 문제**: 붕괴조건이 충족되면 심문 중 바로 "제가 죽였습니다"급 대사가 나와, 누가 자백했는지만 보면 추리 없이 정답을 알 수 있는 문제를 발견. → 심문 중에는 진범이든 무고자든 압박이 최고조에 달하면 동일한 "동요" 라벨만 쓰고, 명시적 시인 문구는 심문 중 절대 금지하도록 프롬프트를 재설계했다. 실제 자백 대사는 **최종 지목 이후 결과 화면에서만** 그 캐릭터의 실제 대화 맥락을 이어받아 별도로 생성한다(`buildConfessionSceneDirective`).
2. **미확보 증거 블러핑 차단**: 조사 모드에서 클릭하지 않은(즉, 실제로 확보하지 않은) 물증을 대화 중 말로만 우겨도 붕괴조건 카운팅에 반영되던 문제를 발견 — 액터·전략가 프롬프트 양쪽에 "형사가 실제로 확보한 증거" 화이트리스트를 명시하고, 목록에 없는 주장은 블러핑으로 취급해 무시하도록 강제했다(전략가가 화이트리스트를 무시하고 대화 속 블러핑을 사실로 받아들여 액터에게 잘못된 전략을 주입하던 2차 버그도 같이 수정).
3. **심문 종료 락아웃**: 진범의 붕괴조건과 무고자의 비밀소진 조건을 "심문종료"라는 동일한 내부 신호로 통합 — 조건 충족 시 캐릭터 종류와 무관하게 동일한 고정 문구("더 이상 대답하지 않는다")로 서버가 텍스트를 덮어쓰고, 해당 용의자와의 채팅 입력을 잠근다. 어느 캐릭터가 먼저 잠기는지 자체가 범인의 단서가 되지 않는다.
4. **신발 요청 자유문 인식**: 형사가 자유문("신발 좀 보여주세요")으로 신발을 요청하면 AI가 이를 인식해 구조화 신호를 출력하고, 서버가 즉시 배역별 신발 검사 결과를 해금하도록 구현. 3배역 모두에 결과를 배정했다 — 이현우(신발 흙 성분 일치, 결정적 물증), 박서연(새 신발이라 흙 반응 없음, 무관), 정민아(세척 흔적은 있으나 흙은 안 나옴, red herring).
5. **심문 효율 보너스**: 3배역 합산 질문 글자 수(공백 포함)가 적을수록 최대 20점 가점. 질문 "개수"가 아니라 "글자 수"를 기준으로 삼은 이유는, 개수 기준은 "질문을 아껴야 한다"는 명시적 강박을 주지만 글자 수는 체감하기 어려워 자연스럽게 간결한 질문을 유도한다는 판단(사용자 피드백). 플레이어에게는 정확한 산정 기준을 공개하지 않고 "날카로운 질문을 던져 용의자를 굴복시키세요" 힌트 문구만 노출한다.
6. **산문 인트로**: 사건 개요를 표·나열이 아니라 서술형 문단("강원도 OO연수원, 새벽 06시경...")으로 먼저 보여준 뒤 화면 전환으로 기존 구조화 사건 개요 화면으로 이어지도록 인트로 화면을 추가했다.

- **Technical Rationale (자백을 결과 화면으로 이연한 설계)**:
  - 애초 수정(모드 라벨을 페르소나별로 분리)만으로는 "사실은 제가 그날..." 같은 말줄임 대사가 사실상 자백처럼 읽힌다는 재피드백을 받아, 대사 생성 자체를 서버가 차단하는 더 근본적인 방식으로 전환했다.
  - "붕괴 보너스" 채점 기준도 함께 바꿔야 했다 — 원래는 "심문 중 AI가 붕괴조건 충족을 선언했는지"를 클라이언트가 알려주는 방식이었으나, 그 신호 자체가 스포일러가 되므로, 플레이어가 실제로 결정적 증거(신발흙 대조 + 관리실 방문 이유 진술)를 둘 다 확보했는지로 판정 기준을 바꿨다 — 클라이언트를 신뢰할 필요도 없고 심문 중 어떤 스포일러도 발생하지 않는다.

- **Technical Rationale (구조화 출력 파싱 강건화, 실전에서 3중으로 관측된 결함)**:
  - 프롬프트가 복잡해지면서(화이트리스트 + 3개 브라켓 출력) 모델이 ① `[모드: 답변|거짓말유지|동요]` 템플릿 문구를 그대로 복사하거나 ② 대괄호(`[]`) 자체를 통째로 생략하거나 ③ 구분자를 `=` 대신 `:`로 쓰는 세 가지 형식 이탈을 실전 테스트 중 순차적으로 발견했다.
  - 이 문제는 프롬프트 지시 강화만으로는 완전히 해소되지 않아, 파서를 대괄호 유무·구분자 종류와 무관하게 매칭하는 정규식으로 재작성하고, 신발 요청은 키워드 기반 폴백을 추가로 얹었다.
  - **남은 리스크**: 대화가 3라운드까지 깊어지면 모델이 구조화 출력 자체를 완전히 생략하는 경우가 드물게 남아있다. 이 경우 "심문 중 명시적 자백 금지" 지시는 구조화 출력과 무관하게 독립적으로 유지되므로 실제 스포일러 유출은 관측되지 않았으나, 심문종료 락아웃의 타이밍이 지연될 수 있다 — 본선 전 추가 안정화가 필요하다면 이 부분을 우선 검토할 것.

## Phase 8-Content: 인물 관계 재설계 (박서연↔이현우 비대칭화) (Completed)

- 사용자가 `16_claude_code_consolidated_relationship_update.md`(이전에 별도로 오갔던 `14_claude_code_relationship_redesign.md`, `15_claude_code_relationship_audit.md`를 대체하는 통합본)로 5가지 변경사항을 전달, `01_truth_bible.md`·`08_evidence_reference_for_tester.md` 최신본과 함께 코드에 반영했다.
1. **김영훈 나이 조정**: "40대 후반" → "30대 후반(38세)". 관계 설정(과거 연인)은 그대로 유지 — 나이만 조정해 개연성 문제를 해결했다. `CASE_OVERVIEW.victim`과 `PROSE_INTRO`(Phase 7에서 추가한 산문 인트로) 양쪽에 반영.
2. **박서연↔이현우 관계 유형 전체 교체**: "고등학교 동창/라이벌, 서로 모르는 척"(대칭) → "예전 회사 인턴-사수 관계"(비대칭)로 교체했다. 박서연은 정규직 전환 실패 원인이 이현우의 성과 가로채기라고 믿고 매우 싫어하지만, 이현우는 박서연이 자신을 싫어한다는 사실 자체를 전혀 모른다 — 형사가 알려주면 진심으로 놀라며 "그건 내 성과였다"고 부인한다. "라이벌"이라는 단어를 코드 전체에서 제거하고, 진술 증거 id를 `stmt-rivalry-lee-park` → `stmt-lee-park-grudge`로 변경(참조 전부 갱신), 물증 "졸업앨범/SNS"의 내용을 "예전 회사 행사 사진(SNS 태그)"로 교체.
3. **이현우 동기에 나이 위계 디테일 추가**: `motiveFull`에 "자신보다 7살 어린 김영훈(38세) 밑에서 밀려날 위기라는 사실이 압박감을 더 키웠다" 추가.
4. **용의자별 개인 타임라인**: 통합본 §4에서 표로 제공됐으나, 이미 각 캐릭터의 `truthBibleFacts` 배열이 동일한 역할(본인 시점 타임라인)을 하고 있어 별도 구현 없이 기존 구조로 충족됨을 확인.
5. **박서연의 "가짜진실"(레드허링) 추가**: 박서연이 예전에 정민아-김영훈이 다투는 걸 목격해 둘이 과거 연인 사이였음을 눈치채고 있다는 설정을 `knownSecrets`에 추가 — 새 물증/점수 항목은 아니고 기존 "동기 자진공개/은폐"(`stmt-motive-disclosure`) 진술 증거 안의 세부 콘텐츠로 편입.

- 코드 반영 후 실전 대화로 비대칭성을 직접 검증했다: 박서연에게 이현우와의 관계를 캐물으면 처음엔 담담하다가("특별히 친했던 건 아닌데") 더 파고들면 적개심과 사연을 털어놓았고("그 사람, 정말 질색이에요"), 이현우에게 박서연의 감정을 알려주자 진짜로 당황하며 부인했다("무슨 말씀을 하시는 겁니까? ... 성과를 가로채다니 그 말도 안 되는 소리입니까").

- **Technical Rationale (id 리네이밍)**:
  - `stmt-rivalry-lee-park`라는 id에 남아있던 "rivalry"(라이벌) 개념이 새 설정("한쪽만 미워하는 비대칭 관계")과 더 이상 맞지 않아, 그대로 두면 코드를 다시 보는 사람이 혼란스러울 수 있다고 판단해 `stmt-lee-park-grudge`로 리네이밍했다. 점수 로직(`scoring.ts`)의 `MOTIVE_EVIDENCE_IDS`/`BREAKDOWN_BONUS_EVIDENCE_IDS`는 이 id를 참조하지 않아 리네이밍이 채점에 영향을 주지 않음을 grep으로 먼저 확인한 뒤 진행했다.

- **Technical Rationale (개인 타임라인을 별도 필드로 만들지 않은 이유)**:
  - 통합본 §4가 요구한 "캐릭터별 개인 타임라인"은 액터 프롬프트가 "이 인물이 그 시간에 뭘 했다고 답해야 하는가"를 참조하는 용도인데, `03_character_sheets.md` 스키마의 `truthBibleFacts`(본인 역할 파트만 담는 ai_only 필드)가 이미 정확히 이 역할을 하고 있었다. 같은 정보를 두 필드에 중복 저장하면 Phase 1에서 세운 "단일 소스" 원칙이 깨지므로, 새 필드를 만들지 않고 기존 구조로 충족됨을 확인하는 선에서 그쳤다.

## Phase 9-Reliability: 심문종료 락아웃 신뢰성 개선 (진범 하드게이트 분리 + 무고자 전용 판정콜) (Completed)

- Phase 7에서 남긴 "구조화 출력 생략 시 락아웃 타이밍 지연" 리스크를 실전 재검증한 결과, 무고자(박서연) 쪽에서 실제로 문제가 재현됐다: 6턴에 걸쳐 `knownSecrets` 3건을 순서대로 전부 끌어냈고 대사 내용 자체는 매번 정확했지만(마지막엔 "제가 말씀드린 건 다 말씀드렸잖아요... 더 이상 말씀드릴 건 정말 없어요"), 락 해제에 필요한 `[내부판정: 심문종료=예]` 브라켓이 비밀 1건을 노출한 직후부터 4턴 연속 완전히 비어 있었다 — 그 결과 `locked`가 끝까지 `true`가 되지 않아 입력창이 잠기지 않았다.
- 원인으로 지목한 것: 무고자 프롬프트는 (1) 페르소나 롤플레이 대사 생성과 (2) 비밀 노출 여부 판정을 한 번의 호출·한 번의 출력 안에서 동시에 요구하는데, 대화가 길어질수록 모델이 대사 생성에 집중하며 판정 브라켓 쪽을 누락하는 경향이 관찰됐다.
- 사용자가 수정 방향에 동의하면서 "그렇게 고치면 다 밝혀지지 않았는데도 락이 걸릴 위험이 있을 것 같은데 그에 대한 대비도 필요하다"는 조건을 명시적으로 걸었다 — 신뢰성 개선이 새로운 조기 락 버그를 만들지 않아야 한다는 요구사항으로 반영했다.
- **진범(alibiStatus: breakable) 쪽은 LLM 판정 자체를 제거**했다. `evidence.ts`에 `breakdownCategory?: "A"|"B"|"C"` 태그(CCTV=A, 신발흙 대조=B, 진술 증거 3종=C)를, `CharacterSheet`에 `breakdownTriggerKeywords: string[]`(이현우만 `["관리실"]`, 나머지는 `[]`)를 추가하고, `interrogate` route가 `collectedEvidenceIds`에서 실제 확보된 카테고리 개수와 형사의 현재 메시지에 트리거 키워드가 등장하는지를 문자열 매칭만으로 직접 계산한다(`computeBreakableHardGate`). 두 조건 모두 실제 플레이어 상태(확보한 증거, 보낸 메시지 텍스트)에서 결정론적으로 계산되므로, 모델이 무엇을 출력하든 결과가 항상 일정하다.
- **무고자(alibiStatus: unbreakable) 쪽은 대사 생성과 분리된 전용 판정 콜**을 추가했다(`buildLockoutJudgeSystemPrompt`). 이 호출은 롤플레이를 전혀 하지 않고 `knownSecrets` 각 항목을 번호 매겨 체크리스트로 제시한 뒤 "언급됨/언급안됨"만 판정하게 하는 단순 분류 작업이므로, 복잡한 대사 생성과 동시에 요구할 때보다 브라켓 누락 빈도가 훨씬 낮다.
- 조기 락 방지 이중 안전장치: (1) `conversationHistory.length >= 6`(최소 3회 왕복) 미만이면 판정 콜 자체를 호출하지 않고 무조건 `locked=false`로 둔다. (2) 판정 콜의 최종 `[심문종료: 예]` 답변 하나만으로 잠그지 않고, 체크리스트에서 실제로 "언급됨"으로 표시된 개수가 전체 비밀 수 이상일 때만 최종적으로 잠근다(`parseLockoutJudgeResponse`) — 파싱이 애매해지거나 모델이 근거 없이 "예"만 던지면 개수 조건에서 걸러져 자동으로 "잠그지 않음"으로 폴백한다.
- 브라우저 실전 재검증 결과:
  - 진범(이현우): CCTV 공백(A) 1개 확보 + "관리실" 키워드가 포함된 질문 → `locked=false`(카테고리 1개뿐이라 정상적으로 안 잠김). 신발흙 대조(B)까지 추가 확보한 뒤 동일 키워드로 재질문 → 그 즉시 `locked=true`.
  - 무고자(박서연): 비밀 1개→2개→3개로 순서대로 노출되는 4턴 동안, 대화 길이가 정확히 최소 기준(6)에 도달한 시점(=비밀 3개가 모두 드러난 시점)에 판정 콜이 처음 호출되어 `revealedCount=3/3, finalAnswer=예, locked=true`로 정확히 잠겼다.
  - 무고자(정민아): 최소 기준(6)을 넘긴 시점에 의도적으로 비밀을 1개만 노출한 상태로 질문 → `revealedCount=1/3, finalAnswer=아니오, locked=false`로 조기 락 없이 정상 유지됨을 확인.
  - 두 경우 모두 잠겼을 때 UI(자물쇠 아이콘 + "더 이상 대답하지 않는다" 고정 문구)가 동일해, 어느 캐릭터가 먼저 잠기는지가 범인 단서가 되지 않는 기존 원칙이 그대로 유지됨을 확인했다.
- `npm run build`/`npm run lint` 통과 확인.

- **Technical Rationale (진범/무고자를 다른 메커니즘으로 비대칭 처리한 이유)**:
  - 진범의 락 조건("카테고리 2개 이상 + 특정 키워드")은 애초부터 서버가 가진 데이터(`collectedEvidenceIds`, 메시지 텍스트)만으로 완전히 표현 가능한 결정론적 조건이었다 — 이걸 굳이 LLM에게 판정시킬 이유가 없었다는 점을 이번에 재확인했다.
  - 반면 무고자의 락 조건("이 비밀이 대화 중 실제로 드러났는가")은 자연어 이해가 필요한 판단이라 LLM 없이는 계산할 수 없다. 대신 그 판단을 "대사를 쓰면서 동시에"가 아니라 "판단만 하는 별도 호출"로 분리해 신뢰성을 높이는 절충안을 택했다.

- **Technical Rationale (과소잠금을 기본값으로 택한 이유)**:
  - 사용자가 명시적으로 우려한 리스크(조기 락)가 반대 리스크(락 누락, 기존에 이미 발생 중이던 문제)보다 이번 수정에서는 더 우선순위가 높다고 판단했다 — 무고자 판정 콜이 실패하거나 파싱이 애매해지는 모든 경우를 "잠그지 않음"으로 폴백시켰다. 락 누락은 플레이어가 조금 더 대화를 이어가는 정도의 불편이지만, 조기 락은 아직 밝혀지지 않은 정보 접근 자체를 막아버리는 더 심각한 결함이기 때문이다.

## Phase 10-Content: 휴대폰 물증 추가 + 진술 증거 선행 물증 게이트 (Completed)

사용자가 `18_claude_code_batch2_phone_and_gating.md`(이전 `17_claude_code_phone_evidence.md`를 대체하는 통합본)로 두 가지 변경을 요청했다. 진행 전 코드베이스와 대조 검토한 결과를 먼저 보고한 뒤, 사용자 확인을 거쳐 반영했다.

**검토 단계에서 확인된 사실**:
- 문서가 지적한 "신발흙 대조 활성화 조건 = 7번(인사평가서) 확보 후" 오타는 실제로 존재했다(Downloads 폴더의 `08_evidence_reference_for_tester.md`에서 확인, 단 이 파일은 7월 21일자 구버전으로 Phase 8 관계 재설계 이전 내용까지 남아있었다) — 그러나 **현재 코드에는 이 오류 자체가 없었다**. Phase 7에서 신발흙 대조를 `requiresEvidenceId` 체인에서 완전히 분리해 "신발 요청 행동"으로만 해금하도록 이미 바꿔놨기 때문에, 코드상 고칠 대상이 없었다(문서만의 문제로 결론).
- 사용자가 "첨부합니다"라고 언급한 `01_truth_bible.md` 최신본, `08_evidence_reference_for_tester.md`(게이트 열 추가된 최신본), `04_game_loop_flow.json`(200점 반영)이 실제 대화에는 전달되지 않았다. 사용자에게 확인한 결과 **18번 문서의 표만으로 구현 진행**하기로 결정.
- 매핑 표에서 "각자의 동기 자진공개/은폐"(`stmt-motive-disclosure`) 항목에 이현우 몫으로 "관리실 방문 이유(하드게이트 통과분)"이 게이트 조건으로 들어가 있었는데, 이현우는 Phase 9 하드게이트 통과 즉시 락아웃되어 그 이후 대화 자체가 불가능해지는 논리적 모순을 발견 — 사용자 확인 결과 **이현우는 이 항목에 게이트를 걸지 않고 기존처럼 라운드3 그대로 두기로 결정**. 나머지 두 캐릭터(박서연·정민아)의 같은 항목도 표 자체가 "캐릭터별로 더 세밀한 게이트가 필요할 수 있다"며 사용자가 직접 실전 테스트하며 채우겠다고 명시한 부분이라, 이번 배치에서는 세 캐릭터 모두 이 항목을 게이트 없이(기존 라운드 기준 그대로) 남겨뒀다.

**1. 신규 물증 — 박서연 휴대폰 사진첩**:
- 정민아-김영훈 관계 정보의 발견 경로를 "박서연 심문 중 우연한 실토"(AI 판단 의존)에서 "박서연 휴대폰 조사"(결정론적 물증, AI 불필요)로 변경했다. `evidence.ts`에 `ev-park-phone-photos`(round3_open, physical) 신규 추가.
- 이제 필요 없어진 박서연 `knownSecrets`의 "정민아-김영훈 관계를 눈치채고 있다" 항목(Phase 8에서 넣은 red herring)을 제거해 중복을 없앴다.
- 기본물증 1종 추가로 `MAX_SCORE`가 195 → 200점으로 자동 반영됐다(`scoring.ts`의 `MAX_SCORE`는 `EVIDENCE` 배열에서 동적 계산되므로 손으로 숫자를 맞출 필요가 없었다).

**2. 진술 증거 조기 유출 방지 — 선행 물증 게이트**:
- 물증(조사 모드)은 Phase 7의 화이트리스트로 "확보 안 하면 언급 불가"가 강제되지만, 진술 증거(심문 전용 비밀)는 이 보호가 없어 형사가 라운드를 앞질러 정곡을 찌르면 원래 2R·3R에나 나와야 할 진술이 1R에 조기 실토되는 문제가 있었다.
- `StatementEvidenceRef`에 `requiredEvidenceIds?: string[]`를 추가하고, 캐릭터별 `statementEvidence` 배열의 해당 항목에 매핑 표대로 값을 채웠다 — 박서연(다툼 이유 ← 법인카드/편의점영수증/통화복구 중 1개, 이현우 악연 사건 ← 졸업앨범·SNS 필수), 이현우(정민아-이현우 트러블 ← 인사평가서/졸업앨범·SNS 중 1개, 박서연-이현우 악연 사건 ← 졸업앨범·SNS 필수), 정민아(정민아-이현우 트러블 ← 인사평가서/졸업앨범·SNS 중 1개). "라운드가 됐는가" 대신 "관련 물증을 실제로 확보했는가"(`collectedEvidenceIds` 교집합)만 보는 결정론적 조건이라 Phase 9의 진범 하드게이트와 같은 패턴이다.
- 액터 프롬프트에 `buildStatementGateSection`을 새로 추가해 게이트가 닫힌 화제는 "형사가 아무리 캐물어도 페르소나 압박 반응(동요 포함)은 보이되 구체적 내용은 절대 먼저 밝히지 않는다"고 명시적으로 금지했다 — 화이트리스트("형사가 없는 증거를 우기면 안 믿는다")와 대칭되는 방향("형사가 근거를 안 들이밀면 스스로 안 밝힌다")의 규칙이다. `stmt-lee-office-visit`(결정적 붕괴 트리거)은 이미 Phase 9 하드게이트로 별도 처리 중이라 이 게이트를 적용하지 않았다.
- 브라우저 실전 검증: 라운드 1(졸업앨범·SNS 미확보 상태)에서 박서연에게 "이현우와 안 좋은 감정이 있냐"고 직접 캐묻자, 게이트 적용 전이었다면 즉시 사연을 털어놨을 질문에 대해 "그냥 같이 일하는 동료일 뿐"이라며 완전히 부인했다. 이후 졸업앨범/SNS 물증을 확보한 뒤 동일 계열 질문을 다시 던지자 "예전 회사에서 제가 인턴이었을 때 그분이 제 사수였어요"라며 관계 자체는 인정하기 시작했다(더 깊은 사연은 추가 압박 시 드러나는 정상적인 페르소나 반응 — 게이트는 "이 화제를 아예 꺼낼 수 있는가"만 막고, 게이트 통과 후의 페르소나별 반응 속도는 그대로 유지된다는 설계 의도대로 동작).
- `npm run build`/`npm run lint` 통과 확인.

- **Technical Rationale (knownSecrets가 아니라 statementEvidence 단위로 게이트를 건 이유)**:
  - `knownSecrets`는 자유 서술 prose라 개별 문장을 물증 id에 기계적으로 매핑하기 어렵다. 반면 `statementEvidence`는 이미 `evidence.ts`의 진술 증거 id와 1:1로 연결된 구조화된 참조라, 화이트리스트와 동일한 `EVIDENCE` 조회 패턴을 그대로 재사용할 수 있었다. 18번 문서의 매핑 표 자체도 진술 증거 id 단위로 구성되어 있어 스키마 선택이 자연스럽게 맞아떨어졌다.

- **Technical Rationale (stmt-motive-disclosure를 이번 배치에서 보류한 이유)**:
  - 사용자가 문서에서 직접 "이 부분은 제가 실전 테스트하면서 직접 채워보겠습니다"라고 명시했고, 대화에서도 이현우 쪽 조건의 논리적 모순을 확인한 뒤 "게이트 없이 그대로"를 택했다. 세 캐릭터 중 하나만 게이트를 적용하고 나머지 둘은 미확정 상태로 두면 일관성이 깨지므로, 셋 다 게이트 없이 두고 사용자의 후속 튜닝을 기다리는 쪽을 선택했다.

## Phase 11-Validation: 고위험 페르소나×배역 표본 검증 (Completed)

- 8×3(페르소나×배역) 전 조합을 다 검증하는 대신, 위험도가 가장 높다고 판단한 3개 조합만 뽑아 검증하기로 사용자와 합의했다.
- **검증 방법**: 랜덤 캐스팅에 의존하지 않고, `castingToken`이 서명 없는 base64url JSON(`{characterId: personaKey}`)이라는 점을 이용해 원하는 조합을 직접 인코딩한 뒤 `/api/interrogate`·`/api/accuse`를 curl로 직접 호출했다 — 브라우저 UI를 거치지 않아 훨씬 빠르고, 대화 기록(`collectedEvidenceIds` 등)을 턴마다 원하는 상태로 정밀하게 통제할 수 있었다.
- **조합 1 — 이현우(진범) + ENFP**("말이 많아지면서 스스로 정보를 흘리는 편", 압박내성 낮음·실토 계열 중 최고 위험): 증거 카테고리 1개 상태에서는 관리실 방문 "사실"은 인정하되 방어적으로 해명만 하고 명시적 자백은 없었다. 카테고리 2개+키워드 충족 시 하드게이트가 대사 내용과 무관하게 정확히 잠갔다.
- **조합 2 — 이현우(진범) + ENTJ**("밀리면 오히려 되받아치는 편", 압박내성 높음·정반대 성향): 압박 초기엔 역공(냉소·되받아치기) 스타일로 완강히 부인하며 정보 누설이 없었고, 하드게이트 통과 시 동일하게 정확히 잠겼다. 결과 화면 자백 장면(`buildConfessionSceneDirective`)도 별도로 확인했는데, 과묵/전투적인 페르소나임에도 "네, 제가 죽였습니다"로 시작해 사실관계가 정확하고 캐릭터 톤(냉소적 체념)이 살아있는 자백이 생성됐다 — 극단적 "침묵" 성향이 자백 장면 생성 자체를 방해하지 않음을 확인.
- **조합 3 — 정민아(무고자) + ENFP**(같은 정보 누설 위험 페르소나를 무고자에 배정): Phase 10 선행 물증 게이트가 정확히 작동해, 증거 미확보 시엔 모호하게 얼버무리다가 확보 후엔 인정하기 시작했다. Phase 9 무고자 판정콜도 정확히 작동해 비밀 2/3만 확인됐을 때는 잠기지 않다가 3/3이 되자 정확히 잠겼다(로그로 `revealedCount=2/3→3/3` 전환 확인). 허위 목격담으로 알리바이를 흔드는 공격에도 실제 알리바이(23:20 로비에서 박서연과 마주침)로 정확히 반박했다 — 수다스러운 성향이어도 알리바이 자체는 깨지지 않음.
- 세 조합 모두에서 버그를 발견하지 못했다. (검증 초반 이현우+ENFP 하드게이트 테스트에서 한 번 `locked=false`가 나와 당황했으나, curl 멀티라인 페이로드의 쉘 이스케이핑 문제로 밝혀졌다 — 파일로 payload를 분리해 재현하니 정상 작동했다. 실제 코드 결함은 아니었다.)

- **Technical Rationale (castingToken 직접 인코딩으로 검증한 이유)**:
  - 8×3=24개 조합 중 3개를 정밀 타겟팅해야 하는데, 브라우저에서 랜덤 캐스팅이 원하는 조합을 뽑을 때까지 새로고침을 반복하는 건 비효율적이다. castingToken이 서명·암호화 없는 평문 JSON이라는 설계(Phase 6에서 문서화한 트레이드오프)가 이번엔 오히려 테스트 효율을 높이는 데 활용됐다.

## Phase 12-Deploy: Vercel 배포 및 프로덕션 레이턴시 조사 (Completed)

- **배포**: git 저장소를 새로 만들어(GitHub `squapple/murder-mystery`) main 브랜치에 push하고, Vercel과 GitHub 연동으로 자동 배포 파이프라인을 구성했다. `NVIDIA_API_KEY` 환경변수를 Vercel 프로젝트 설정에 등록. 배포 URL: `https://murder-mystery-three-zeta.vercel.app`.
  - 계정 로그인·GitHub 저장소 생성·Vercel 프로젝트 Import·환경변수 입력은 전부 사용자가 직접 진행했다(계정 인증이 필요한 작업은 AI가 대신 수행할 수 없다는 안전 정책에 따름). git push도 최초 시도는 Windows Git Credential Manager의 브라우저 인증 팝업 때문에 자동화 도구에서 타임아웃됐고, 사용자가 직접 터미널에서 실행해 완료했다.
- **문제 발견**: 배포 후 실전 심문 테스트 중 응답이 40초~2분 넘게 걸리는 심각한 지연을 발견 (로컬은 동일 시점에 3~8초). Vercel 함수 로그에서 실행시간 "1m 50s", 서버 로그의 `elapsedMs=110247`로 확정 재현.
- **원인 조사** — 가설 3개를 세워 하나씩 직접 검증했다(추측이 아니라 실측으로 좁혀나감):
  1. "NVIDIA가 클라우드/데이터센터 발신 트래픽을 일반적으로 느리게 처리한다" — Claude Code 실행 환경(역시 클라우드)에서 NVIDIA API에 직접 curl로 호출해 1.7초 확인 → **배제**.
  2. "지금 이 시간대에 NVIDIA 서버 자체가 전반적으로 느리다" — 같은 시각 로컬 dev 서버 호출이 3.4초로 정상 → **배제**.
  3. "전략가+액터 순차 호출이 누적돼서 느려진다" — Vercel에 진단용 라우트(`/api/diag-nim`, NVIDIA 단일 콜만 수행)를 임시로 추가해 배포·측정한 결과, 단일 콜 하나만으로도 75~157초가 나와 순차 구조는 원인이 아니라 결과를 증폭시키는 요인일 뿐임을 확인.
  - Vercel 함수 리전을 sfo1으로 바꿔보는 실험도 진행했으나(`vercel.json`), 응답의 `region` 필드가 처음엔 `iad1`로 그대로 나와 리전 변경이 즉시 반영되지 않았다.
  - **결정적 단서**: API 호출 없이 몇 분 대기했다가 재측정하자 같은 iad1 리전에서 157초 → 75초 → 14초 → 3.4초로 아무 개입 없이 꾸준히 빨라졌다. 고정된 네트워크 경로 문제라면 이렇게 시간 경과만으로 회복될 리 없다 — **오늘 디버깅 과정에서 로컬·curl·Vercel 진단 호출을 반복하며 NVIDIA 무료 티어 레이트리밋(분당 40건)에 누적으로 부딪혔고, 명시적 429 에러 대신 조용히 큐잉/지연되는 방식으로 나타났을 가능성이 가장 유력한 결론**으로 정리했다. (sfo1 리전 변경과 회복 시점이 겹쳐서 리전 효과가 완전히 배제된 것은 아니나, 시간 경과에 따른 점진적 회복 패턴이 훨씬 강한 신호로 판단했다.)
- **정리**: 원인 조사가 끝난 뒤 진단용 라우트(`/api/diag-nim`)와 `vercel.json`의 리전 오버라이드를 모두 제거했다 — 리전 변경이 효과가 있었다는 확증이 없는 채로 남겨두면 나중에 헷갈리는 설정만 남기 때문.

- **Technical Rationale (가설을 순서대로 실측으로 배제해나간 절차)**:
  - "왜 느린가"를 짐작으로 결론 내지 않고, 각 가설이 참이라면 관측돼야 할 구체적 신호(직접 호출 속도, 같은 시각 로컬 속도, 단일 콜만의 소요시간, 시간 경과에 따른 회복 여부)를 하나씩 실측해 지워나갔다 — 마지막까지 남은 설명(레이트리밋)이 가장 그럴듯한 이유는 다른 세 가설이 데이터로 명시적으로 반박됐기 때문이다.

## Phase 13-Playtest: 실전 플레이 피드백 7건 반영 (Completed)

사용자가 배포된 버전을 직접 플레이한 뒤 7가지 피드백을 줬다. 성격이 서로 달라 먼저 "바로 고칠 것 / 결정이 필요한 것 / 직접 조사할 것"으로 나눠 처리했다.

1. **수사 메모장**: `NotesPanel.tsx` 신규 추가 — 조사 모드 아래에 자유 텍스트 메모칸. 채점과 무관.
2. **mode 라벨 노출(버그)**: `InterrogationChat.tsx`가 AI 응답에 "답변"/"거짓말 유지"/"동요" 배지를 실제로 렌더링하고 있었다 — 개발 중 디버깅용으로 남겨뒀던 게 그대로 배포됐던 것. 어느 발언이 거짓인지 플레이어에게 대놓고 알려주는 셈이라 fair-play를 정면으로 깨는 심각한 버그였다. 즉시 제거.
3. **응답 속도 불안정**: 5~6턴 이상 연속 측정 결과 9초~124초로 재현 확인 — Phase 14에서 원인 규명·해결.
4. **이어하기(세이브/로드)**: DB 없이 `localStorage` 기반으로 구현(`src/lib/gameSave.ts`). 라운드/심문 대화/확보 증거/메모를 매 변경마다 자동 저장하고, 재접속 시 저장된 게임이 있으면 "이어하기/새로 시작"을 먼저 물어본다. 게임이 끝나면(결과 화면) 저장을 지운다. DB 대신 이 방식을 택한 이유: 기기 간 이동(컴퓨터→아이패드)까지는 파티 게임 규모에서 필요성이 크지 않다고 판단했고, Phase 6에서 이미 "DB 없는 서버리스" 제약을 설계 전제로 삼았던 것과 일관성을 유지했다.
5. **사인(사망 원인) 상세정보**: 진실 성서에 없던 새 설정이라 사용자에게 초안 승인을 받았다. "목·어깨 다발성 타박상, 뒤통수 열상, 흉기 없이 몸싸움 중 낙상으로 추정, 두부 외상이 직접 사인"으로 작성 — 기존 사실("몸싸움 끝 사망", 흉기 불특정)과 모순되지 않게 구성. `evidence.ts`의 `EvidenceItem`에 `detail?: string` 필드를 신규 추가해 반영.
6. **조사 모드 UX 불명확**: 안내 문구를 "카드를 클릭하면 확보되고, 확보된 카드를 다시 클릭하면 상세 내용을 볼 수 있습니다"로 구체화하고, 클릭 시 모달로 확대 표시하는 기능을 추가(`InvestigationBoard.tsx` 전면 재작성) — `detail` 필드가 있으면 그 내용을, 없으면 `revealedFact`를 표시한다. 진술 목록도 클릭 가능하게 통일.
7. **복합 문장 이해도**: 4개 항목(A/B/C/D)을 한 메시지에 담아 "맞나요?" 형식으로 질의한 결과, 넷 다 구체적으로 언급하며 답변 — 사용자가 겪었던 "한 항목만 강조하고 나머지는 얼버무리는" 패턴은 이번 표본(1건)에서는 재현되지 않았다. 표본 부족으로 결론은 보류.

- **Technical Rationale (mode 라벨 버그가 왜 지금까지 안 걸렸는가)**:
  - Phase 7에서 "동요" 라벨을 진범/무고자 공유 색상으로 통일한 건 라벨 *자체*가 범인을 암시하지 않게 하려는 조치였지, 애초에 "답변"/"거짓말 유지" 배지를 플레이어에게 보여줄지 여부는 별도 결정이 필요한 사안이었다. 개발 중 실시간으로 모드 분류가 잘 되는지 눈으로 확인하려고 넣어둔 표시가 그대로 굳어 배포까지 간 것 — 실전 플레이(구조화 로그가 아니라 실제 화면)를 거치고 나서야 발견됐다.

## Phase 14-Deploy: 프로덕션 레이턴시 원인 규명 및 Edge 런타임 전환 (Completed)

- Phase 13의 3번(속도 불안정) 재현 확인 후, 사용자가 "Phase 12의 결론(NVIDIA 인프라 자체가 불안정하다)이 맞는지 다시 생각해보자"고 문제 제기했다 — **로컬은 항상 빨랐는데, 로컬도 같은 API·같은 키로 호출하니 NVIDIA 쪽 문제라면 로컬도 느려야 한다**는 논리였다. 이 지적이 정확했다: Phase 12의 결론(순수 NVIDIA 인프라 문제)은 성급했고, "Vercel이라는 발신지 자체의 특수성"으로 봐야 한다는 방향이 맞았다.
- **웹 검색으로 재검증**: (1) Vercel Hobby 플랜은 전용 고정 발신 IP가 없고 이 기능은 Pro/Enterprise 전용 월 $100 애드온이라, Hobby 사용자는 다른 무관한 프로젝트들과 발신 IP를 공유할 수밖에 없음을 확인. (2) GitHub `vercel/ai#785` 등에서 **다른 사람이 NVIDIA가 아닌 OpenAI API를 Vercel에서 호출할 때 10초~5분대로 느려진다는, 증상까지 거의 동일한 사례**를 확인 — 저희만의 특이 케이스가 아니라 "Vercel 서버리스에서 외부 LLM API를 호출할 때 흔한, 알려진 패턴"임을 확인했다.
- **해결**: `/api/interrogate`, `/api/accuse`, `/api/casting` 세 라우트에 `export const runtime = "edge"`를 추가해 Node 서버리스 함수 대신 Vercel Edge 네트워크로 발신 경로를 바꿨다. Edge 런타임은 Node API 전체를 지원하지 않아, 로컬에서 먼저 (1) `Buffer.from(...).toString('base64url')` 기반 castingToken 인코딩/디코딩, (2) `openai` npm SDK 호출이 모두 정상 동작하는지 검증한 뒤 배포했다.
- **결과**: 배포 후 5턴 연속 측정 결과 25.1초~25.4초로 **거의 완벽하게 일정**해졌다(이전 Node 런타임: 9초~157초). 절대 속도 자체는 로컬(3~8초)보다는 여전히 느리지만("여전히 공유 경로"라는 한계는 남음), "느렸다 빨랐다 반복"하던 예측 불가능성은 사실상 해소됐다.
- 남은 절충점: 매 턴 약 25초는 파티 게임 템포에 여전히 느릴 수 있다. 사용자는 "느려지는 건 나중 문제"라 판단해(필요하면 유료 플랜 전환 또는 NAN 측 채택 시 자체 서버 사용으로 자연 해결될 여지도 있음) 이번엔 안정성 확보를 우선하고 절대 속도 최적화는 보류했다.

- **Technical Rationale (사용자의 반론을 받아들여 결론을 뒤집은 것)**:
  - Phase 12에서 "NVIDIA 인프라가 불안정하다"고 결론 낸 근거(직접 호출 성공, 로컬 성공, 시간 경과에 따른 회복)는 전부 사실이었지만, "발신 위치가 다른 여러 환경 중 어디가 실제로 계속 문제였는가"를 로컬과 나란히 놓고 비교하는 걸 놓쳤다. 사용자가 그 허점을 정확히 짚었고, 그 결과 원인을 훨씬 더 정확한 수준(Vercel Hobby의 공유 IP 구조)까지 좁힐 수 있었다 — 데이터로 검증된 결론도 새로운 반례가 나오면 다시 열어봐야 한다는 사례로 기록해둔다.

## Phase 15-Balance: 전략가 계층에 페르소나 실제 반영 (Completed)

- 사용자가 "전략가의 판단이 성향에 따라 달라지느냐"고 질문해 `strategist-prompt.ts`를 확인한 결과, `buildStrategistSystemPrompt`가 `persona` 파라미터를 받으면서도 프롬프트 본문에서 전혀 참조하지 않는 죽은 파라미터였음을 발견했다 — 지금까지는 "무엇을 할지"(전략)는 성향과 무관하게 항상 동일했고, "어떻게 말할지"(표현)만 액터 단계에서 성향별로 갈리는 구조였다.
- 사용자는 행동 양식 자체도 성향에 따라 달라지길 원했다("느려지는 건 나중 문제"라며 속도보다 우선). `persona.pressureTolerance`/`corneredReaction`을 프롬프트에 실제로 반영해, 압박내성이 낮은 성향은 "사소한 정보를 성급하게 흘리거나 변명을 늘어놓는" 전략을, 높은 성향은 "최소한만 절제해서 대응하고 형사를 기다리게 하는" 전략을 택하도록 명시했다.
- **검증**: 같은 배역(이현우)·같은 상황(증거 카테고리 1개, 안 잠김)에서 ESTP(압박내성 낮음)와 ISTJ(높음)를 로컬에서 비교했다. 전략가 메모 자체가 ESTP는 "사소한 알리바이 설정이나 변명을 성급하게 늘어놓아 화점을 분산", ISTJ는 "불필요한 설명을 배제하고… 침묵을 활용해 상대의 압박을 기다리십시오"로 확연히 갈렸고, 실제 대사도 ESTP는 장황한 반박("증거라도 있습니까?"), ISTJ는 짧은 침묵형 답변("왜 묻는 겁니까?")으로 이어졌다.
- fair-play 불변식(무고자 알리바이, 진범 하드게이트)은 이 프롬프트와 무관하게 서버가 결정론적으로 강제하는 채널(Phase 9의 `computeBreakableHardGate`, 액터 프롬프트의 게이트 지시)에 있으므로, 전략가 판단이 성향에 따라 더 널뛰어도 fair-play 자체는 깨지지 않는다는 걸 프롬프트 주석에도 명시해뒀다.

- **Technical Rationale (죽은 매개변수를 즉시 제거하기로 한 것)**:
  - 사용자가 "발견하면 확인 후 바로 없애라"고 명시적으로 요청해, 이후 세션에서도 기본 원칙으로 적용하기로 했다(메모리에 기록). 이번 건은 "제거"가 아니라 "실제로 쓰이게 고치는" 방향으로 처리했는데, 죽은 코드였던 이유가 "애초에 안 쓸 정보라서"가 아니라 "구현이 누락돼서"였고, 마침 사용자가 그 기능 자체를 원했기 때문이다.

## Phase 16-Fix: 배포 누락 발견, 시각 표현 오류, RAG 검토 (Completed)

- **Phase 13 변경사항이 실제로는 배포되지 않았던 걸 뒤늦게 발견**: Phase 15(전략가 페르소나 반영) 배포 후 사용자가 "지금 실행하면 새 버전이냐"고 물어 확인하는 과정에서, `NotesPanel.tsx`/`gameSave.ts`/`InvestigationBoard.tsx` 재작성/`InterrogationChat.tsx` mode 라벨 제거 등 Phase 13의 핵심 UI 변경들이 로컬에 빌드·린트까지 통과한 채로 남아있을 뿐 한 번도 `git commit`/`push`된 적이 없었다는 걸 발견했다 — Phase 14·15 커밋이 성공적으로 배포된 것과 별개로, 그보다 먼저 했어야 할 Phase 13 커밋 자체를 빠뜨렸던 것. 배포된 JS 번들을 직접 grep해서("카드를 클릭하면" 같은 신규 문구가 있는지) 확인하는 방식으로 발견하고 즉시 커밋·푸시해 반영했다.
- **사인 시각 표현 오류**: `CASE_OVERVIEW.timeOfDeath`가 "워크숍 둘째 날 새벽 23:45경"으로 되어 있었는데, 23:45(밤 11시 45분)는 "새벽"(자정 이후~해뜨기 전)이 아니다. "밤 23:45경"으로 수정. (참고: `PROSE_INTRO`의 "새벽 06시경"은 시신 발견 시각이라 맞는 표현이었음 — 사인 쪽만 오류였다.)
- **RAG로 속도 개선이 가능한지 검토**: 사용자가 "AI 성향을 RAG 방식으로 만들면 빨라질까"라고 질문. RAG는 대용량 자료에서 관련 조각만 검색해 프롬프트에 넣는 기술인데, 저희 페르소나(8종, 필드 5개짜리)·캐릭터 시트는 이미 극히 작고, 이미 "관련 배역 하나만" 코드 조회로 골라 넣고 있어 RAG가 풀어주는 문제 자체가 없다고 판단했다 — 오히려 임베딩·벡터 검색이라는 새 단계가 추가돼 더 느려질 수 있다. Phase 14에서 확인한 실제 병목(Vercel 발신 경로, 턴당 전략가+액터 순차 2콜)은 "프롬프트에 뭘 담는가"가 아니라 "API를 몇 번·어디로 호출하는가"의 문제라 RAG로 손댈 수 있는 영역이 아니다. 결론: RAG 도입 없이, 여전히 유효한 유일한 절대속도 개선 레버는 2콜→1콜 통합(보류 중)뿐이다.

- **Technical Rationale (배포 누락을 어떻게 다시 안 일으킬지)**:
  - 이번 사고의 교훈: 로컬 build/lint 통과와 실제 배포 반영은 별개다. 앞으로 "완료했다"고 보고하기 전에 `git status`로 미커밋 변경이 없는지, 필요하면 배포된 번들에서 직접 grep해 실제 반영을 확인하는 절차를 습관화한다.

## Phase 17-Fix: Edge 타임아웃 실사용 재현, 전략가 콜 제거 (Completed)

- 사용자가 실제 플레이 중 "Unexpected token 'A', "An error o"... is not valid JSON" 에러를 만났다. 토큰(사용량) 문제가 아니라, 서버가 JSON 대신 HTML 에러 페이지를 반환했고 클라이언트가 그걸 JSON으로 파싱하려다 실패한 것이었다.
- **원인 확정**: Vercel 공식 문서에서 "Edge 런타임 함수는 스트리밍 없이는 응답을 25초 안에 시작해야 한다"는 조항을 확인했고, 프로덕션에 직접 curl로 3회 연속 재현한 결과 **3/3 모두 정확히 25.1~25.5초에 `FUNCTION_INVOCATION_TIMEOUT`(504)** 로 실패했다 — 우연한 지연이 아니라 사실상 항상 재현되는 상태였다. Phase 14에서 Edge 전환으로 네트워크 경로 불안정성은 해결했지만, 그 대가로 이 25초 마감시한이라는 새로운 제약이 생겼는데, 마침 전략가+액터 순차 2콜 구조의 총 소요시간이 딱 그 경계(~25초)에 걸려 있었다.
- **해결(전략가 콜 제거)**: 전략가 계층이 매 턴 판단하던 내용(붕괴조건 근접도 카운팅, 블러핑 차단)이 이미 액터 프롬프트 자체(`buildBreakdownSection`, `buildEvidenceWhitelistSection`)에 중복으로 들어있었고, 유일하게 고유했던 정보(페르소나 성향별 전략 경향, Phase 15에서 추가)는 단순 조건문이라 LLM 판단이 필요 없었다. 이 조건문을 `buildPersonaTendencySection()`으로 옮겨 액터 프롬프트에 정적으로 포함시키고, 전략가 API 콜과 `strategist-prompt.ts` 파일 자체를 완전히 제거했다. 턴당 NIM 호출이 2콜→1콜로 줄어 로컬 테스트 기준 약 2초까지 떨어졌고, ESTP/ISTJ 페르소나 차이(Phase 15에서 검증한 행동 양식 차이)도 그대로 유지됨을 재확인했다.
- **참고**: 무고자 캐릭터는 대화가 6턴을 넘으면 여전히 락아웃 판정 콜(Phase 9)이 추가로 붙어 2콜이 될 수 있다 — 다만 이 판정 콜은 체크리스트 분류만 하는 훨씬 가벼운 프롬프트라 액터 콜과 합쳐도 25초 안에 들어올 가능성이 높다고 보고 있으나, 실측 확인은 아직 안 했다(다음 단계 참고).
- **세이브/로드 UI 관련**: 사용자가 처음엔 "버튼이 없어서 기능이 빠진 줄 알았다"고 했으나, 실제로는 정상 작동 중임을 재확인(새로고침 시 이어하기 프롬프트가 뜸)했고, 버튼 없이 지금의 자동 감지 방식을 그대로 유지하기로 확정했다.
- **향후 방향으로 명시적으로 남겨둘 것 — "전략가 = 친구 대리 AI" 비전**: 사용자는 이번 제거가 순전히 속도 문제 때문의 임시 조치이고, 전략가 계층 자체는 이 프로젝트에서 중요한 요소가 될 수 있다고 판단했다. 구체적 비전: "플레이어와 함께 플레이하는 AI"를 별도로 설정해두고, 이 AI가 각 캐릭터별로 전략을 세우면 그 전략에 따라 게임 속 캐릭터가 연기하는 구조로 발전시키면, 친구들과 하는 파티 게임에서 **부족한 인원(친구) 자리를 AI가 대신 채워주는** 기능으로 확장할 수 있다는 아이디어다. 지금은 속도 제약 때문에 보류하지만, 나중에(유료 인프라 전환, NAN 측 채택으로 자체 서버 확보 등) 다시 살릴 가치가 있는 방향으로 기록해둔다.

- **Technical Rationale (전략가를 완전히 삭제가 아니라 "부활 가능한 형태"로 기록해두는 이유)**:
  - 이번 제거는 "전략가라는 개념이 틀렸다"가 아니라 "지금 이 구현(매 턴 별도 LLM 콜)이 25초 제약과 충돌한다"는 순수 인프라 문제였다. 사용자가 이 계층 자체의 장기적 가치(친구 대리 AI)를 명확히 밝혔으므로, 나중에 스트리밍 응답이나 더 넉넉한 실행시간 환경(Node/Fluid Compute, 유료 플랜, 자체 서버)으로 옮겨갈 때 그대로 되살릴 수 있도록 설계 의도를 여기 남겨둔다.

## Phase 18-Fix: Edge 런타임 재검증 실패, Node.js로 최종 복귀 (Completed)

- Phase 17의 전략가 콜 제거(2콜→1콜)를 배포한 뒤, 사용자에게 배포 확인을 부탁해 Vercel 대시보드에서 해당 배포가 **Ready/Production**임을 확인했다. 그런데도 프로덕션 curl 테스트는 여전히 정확히 ~25초에 `FUNCTION_INVOCATION_TIMEOUT`으로 실패했다 — 배포 자체는 정상이었는데 문제가 해결되지 않은 것.
- **재조사**: 같은 시점에 (1) NVIDIA API 직접 호출(1.1초), (2) 로컬 dev 서버 단일 콜(1.8초)이 둘 다 빨랐다. 즉 콜을 1개로 줄였는데도 프로덕션(Edge)에서만 단독으로 ~25초가 걸리는 모순이 관찰됐다.
- **가설**: 이전 2콜 구조가 25초 안에 "성공"했던 건 콜 수가 적어서가 아니라, 첫 번째 콜(전략가)이 Edge→NVIDIA 간의 최초 연결(TLS 핸드셰이크 등)을 힘들게 맺어두면 두 번째 콜(액터)이 그 웜 커넥션에 얹혀가 빨리 끝났기 때문일 수 있다 — 콜을 1개로 줄이면서 오히려 그 "무임승차"가 사라져, 매번 연결 수립 비용을 처음부터 다시 치르게 된 것으로 추정된다. 정확한 원인은 Vercel 대시보드 심화 로그 없이 100% 확정하긴 어려웠다.
- **결정**: Edge 런타임은 스트리밍 없이는 25초가 하드 마감이라는 게 Phase 17에서 이미 확인된 사실이었고, 콜 수를 줄여도 이 마감을 못 지키는 게 재확인됐으므로, Edge 자체를 포기하고 세 라우트(`interrogate`/`accuse`/`casting`) 전부 Node.js(Fluid Compute, 기본 300초)로 되돌렸다.
- **검증**: 배포 후 4회 연속 테스트 결과 전부 200 성공(94.7초/79.3초/48.0초/92.8초) — Phase 12/14에서 봤던 것과 같은 큰 변동성이 돌아왔지만, **적어도 매번 실제로 응답이 온다**. "느리지만 결국 성공"이 "빠른 척하다 100% 실패"보다 낫다고 판단해 이 상태로 확정했다.
- 전략가 콜 제거(Phase 17)는 Edge를 포기한 뒤에도 그대로 유지한다 — 콜 수를 줄이는 것 자체는 Node 런타임에서도 총 대기시간을 줄여주는 순수한 이득이고, 페르소나 성향 반영도 그대로 보존되기 때문이다.

- **Technical Rationale (Edge 시도 자체가 낭비는 아니었던 이유)**:
  - Edge 전환은 실패로 끝났지만, 이 과정에서 "우리 병목이 정확히 어디에 있는가"(네트워크 변동성 vs 응답시간 마감 vs 콜 수)를 하나씩 실측으로 좁혀나갔다 — Phase 12(레이트리밋 의심) → Phase 14(공유 IP 확정, Edge 채택) → Phase 17(Edge 25초 마감 발견, 콜 수 감소) → Phase 18(콜 수 감소로도 못 고침, Node 복귀)까지 매 단계 데이터로 가설을 검증하고 다음 가설로 넘어갔다. 최종 선택(Node 복귀)이 나쁜 소식이더라도, 그 과정에서 "우리가 구체적으로 무엇과 씨름하고 있는지"는 훨씬 선명해졌다.

## Phase 19-Fix: 내부판정 브라켓 노출 버그, Vercel→Cloudflare Workers 전환 (Completed)

- **내부판정 브라켓 노출(플레이어 스크린샷 제보)**: 아이패드로 플레이 중이던 사용자가 "[비밀노출: 없음]", "[심문종료: 아니오]"가 대사 위에 그대로 노출되는 스크린샷과, 같은 질문을 연달아 보냈을 때 "The string did not match the expected pattern"이라는 별개 오류가 뜨는 스크린샷을 제보했다. 원인: 모델이 가끔 `[내부판정: ...]` 래퍼로 묶지 않고 `비밀노출`/`심문종료`를 독립된 브라켓으로 따로 출력해, "내부판정" 라벨만 찾는 정규식이 놓쳤다(한 사례는 앞부분만 걷어내지고 ", 심문종료:아니오]" 꼬리만 남기도 함). 이 값들은 Phase 9 이후 실제 락아웃 판정에 전혀 쓰이지 않으므로(진범은 하드게이트, 무고자는 전용 판정콜), 값을 다시 파싱할 필요 없이 화면에서 안 보이게 걷어내기만 하면 되는 문제였다. `LEAKED_INTERNAL_FIELD_RE`를 추가해 "비밀노출/심문종료/증거카테고리/붕괴조건충족/신발요청" 라벨을 브라켓 유무·선행 쉼표와 무관하게 걷어내도록 했고, 제보받은 두 leak 문자열 그대로 유닛 테스트해 확인 후 배포했다.
- **Vercel → Cloudflare Workers 전환**: Phase 18에서 응답 속도가 다시 불안정(47~93초)해지자, 사용자가 "로컬은 항상 빨랐다 — Vercel 쪽 문제라면 같은 역할을 하는 국내/타 플랫폼으로 옮겨볼 수 있지 않냐"고 제안했다. `cloudflare-pages-experiment` 브랜치를 새로 만들어 `main`(Vercel 배포 중)은 전혀 건드리지 않은 채 실험을 진행했다:
  - `@opennextjs/cloudflare` + `wrangler`로 Cloudflare Workers 배포 지원 추가(`wrangler.jsonc`, `open-next.config.ts`, `nodejs_compat` 플래그).
  - 로컬 `opennextjs-cloudflare preview`(실제 workerd 런타임)로 캐스팅·심문(openai SDK + Buffer 기반 castingToken)이 전부 정상 동작함을 먼저 확인.
  - Cloudflare 대시보드에서 GitHub 연동 프로젝트 생성 → 처음엔 프로덕션 브랜치가 기본값 `main`으로 잡혀 빌드 실패(설정 파일이 실험 브랜치에만 있었음) → `cloudflare-pages-experiment`로 브랜치 지정 후 빌드 성공.
  - 환경변수 설정 함정 발견: Build 설정 안의 "Variables and secrets"(빌드 시점 전용)에 넣었더니 런타임에서 `NVIDIA_API_KEY`를 못 읽음 — Worker Settings 최상단의 별도 "Variables and secrets(used at runtime)"에 Secret 타입으로 다시 넣어야 실제 요청 처리 중에도 값을 읽을 수 있었다.
  - **실측 결과**: 배포 URL(`murder-mystery.squapple.workers.dev`)에서 interrogate 호출을 5회, 이어서 15회 반복 테스트한 결과 **1.6초~5.5초 사이에서 전부 성공**(Vercel Node: 9~157초, Vercel Edge: 100% 타임아웃과 극명히 대비) — 사용자가 지적했던 "로컬은 빠른데 Vercel만 느리다"는 관찰이 Vercel 발신 IP 풀 문제였음을 실측으로 재확인시켜준 결과였다.
  - 사용자가 전환을 확정해, `cloudflare-pages-experiment`를 `main`에 병합하고 Cloudflare 프로젝트의 프로덕션 브랜치도 `main`으로 재설정했다(앞으로는 `main` 하나만 관리하면 Vercel·Cloudflare 양쪽에 자동 반영됨).
  - 병합 과정에서 ESLint가 `.open-next/`(번들된 벤더 코드, 14,762개 가짜 문제 발생)를 스캔하는 걸 발견해 `eslint.config.mjs`의 ignore 목록에 추가.
  - Vercel 배포(`murder-mystery-three-zeta.vercel.app`)는 그대로 남겨뒀다 — main이 두 플랫폼 모두에 배포되므로, Cloudflare에 문제가 생기면 Vercel이 자동으로 최신 상태의 백업 역할을 한다.

- **Technical Rationale (Vercel을 완전히 버리지 않고 병행 배포로 남긴 이유)**:
  - 두 플랫폼 다 같은 `main` 브랜치·같은 GitHub 연동 방식이라 유지 비용이 거의 들지 않는다. Cloudflare가 이후에도 계속 안정적인지 며칠 더 지켜볼 시간을 벌면서, 혹시 Cloudflare 쪽에 예상 못 한 문제(예: 공유 IP 문제가 여기서도 재현되는 경우)가 생기면 Vercel URL로 즉시 되돌아갈 수 있는 안전장치를 유지하는 게 이번 실험 전체를 "언제든 되돌릴 수 있게" 시작한 원래 원칙과 일관된다.

## Phase 20-Fix: 1라운드 실전 플레이테스트 피드백 5건 반영 (Completed)

사용자가 Cloudflare 배포로 실제 1라운드를 플레이한 뒤 스크린샷 6장과 함께 5건의 피드백을 남겼다. 그중 4건을 수정하고, 1건은 사용자 지시대로 보류했다.

- **① 배경 인지 부족("집에 갔다" 발언)**: 이현우가 "그날 회식 끝나고 집에 갔다"는 식으로, 마치 연수원이 아니라 통근 상황인 것처럼 답했다. 사용자는 "AI에게 규칙을 직접 주입하지 말고, 배경을 더 구체적으로 인지시키면 알아서 판단해서 대답하지 않겠냐"고 명시적으로 제안했다. 이 제안대로 `truth-bible.ts`의 `CASE_OVERVIEW.background`를 "참가자 전원이 연수원 내 개인 객실에 묵는다. 도보 외에는 마땅한 이동 수단이 없는 외진 위치라, 워크숍 기간 내내 연수원 부지를 벗어나지 않는다"로 구체화하고, `actor-prompt.ts`에 `buildCaseSettingSection()`을 새로 추가해 이 배경을 모든 액터 시스템 프롬프트 최상단에 주입했다. "이렇게 답해라"가 아니라 "이런 장소·상황이다"만 알려주는 방식이라, 신발확인/CCTV 같은 기존 세부 규칙과 충돌 없이 자연스럽게 "각자 객실로 돌아갔다"는 답이 나오는지 로컬에서 직접 확인했다(아래 검증 참고).
- **② 어색한 문구 전체 검토**: 사용자가 지목한 "사인은 산책로에서의 몸싸움 끝에 의한 사망으로 추정된다"를 "사인은 산책로에서 벌어진 몸싸움 끝에 입은 외상으로 추정된다"로 수정(`PROSE_INTRO`, `CASE_OVERVIEW.causeOfDeath`). 이어서 "전체적으로 검토해달라"는 요청에 따라 `ResultScreen.tsx`, `AccusationScreen.tsx`, `IntroScreen.tsx`, `InvestigationBoard.tsx`, `InterrogationChat.tsx`, `evidence.ts`의 모든 `detail`/`revealedFact` 문구까지 훑었으나 추가로 고칠 어색한 표현은 발견되지 않았다.
- **③ 수사메모 스포일러성 예시 문구**: 기본 placeholder("예: 박서연은 이현우를 싫어함...")가 힌트도 방해도 될 수 있다는 지적에, 사용자가 제시한 두 옵션(랜덤 더미 문구 vs 완전 제거) 중 완전 제거를 선택했다 — 매번 다른 더미 문구를 만드는 것보다 구현이 단순하고, 애초에 아무 문구도 없는 편이 스포일러 위험 자체를 원천 차단하기 때문. `NotesPanel.tsx`의 placeholder를 "여기에 자유롭게 메모하세요."로 교체.
- **④ 서로의 직책 오인식**: 박서연이 이현우(실제 직책 "차장")를 "대리"라고 잘못 부른 사례가 스크린샷으로 제보됐다. `actor-prompt.ts`에 `buildOtherSuspectsSection()`을 추가해, 각 액터의 시스템 프롬프트에 "함께 심문받는 다른 용의자들"의 정확한 이름·직책 목록을 명시적으로 주입했다.
- **⑤ 중간 대사 버벅임**: 사용자가 "되물으면 다시 제대로 대답하더라"며 명시적으로 넘어가도 된다고 해, 이번 배치에서는 손대지 않았다.

- **검증**: 로컬 dev 서버에서 실제 NIM 호출로 직접 재현 테스트했다.
  - 이현우(ISTJ)에게 "어젯밤 회식 끝나고 어디로 갔어요?" → `"회식 끝나고 각자 객실로 돌아갔을 텐데요. 저는 피곤해서 바로 방에서 쉬었습니다."` (1.2초) — "집" 언급 없이 연수원 숙소 맥락으로 정확히 답함.
  - 박서연(ENFP)에게 "이현우씨는 직급이 어떻게 되고, 어떤 사람이에요?" → `"아, 이현우 차장님요?"` (1.7초) — 정확한 직책으로 호칭.
  - `npm run build`/`npm run lint` 모두 통과 확인 후 커밋.

- **Technical Rationale (규칙 주입 대신 배경 구체화를 선택한 이유)**:
  - 기존 방식이었다면 "집에 갔다는 표현을 쓰지 마라" 같은 금지 규칙을 추가했을 텐데, 이는 이 특정 문구 하나만 막을 뿐 같은 원인(장소 설정에 대한 얕은 이해)에서 나올 수 있는 다른 어색한 답변(예: "택시 타고", "가족한테 전화" 등)은 못 막는다. 배경 자체를 구체화하는 방식은 모델이 매번 스스로 "여긴 도보 이동만 가능한 외진 연수원"이라는 제약을 참조해 판단하게 만들어, 예상 못 한 유사 오류까지 함께 방지하는 더 근본적인 해결책이다. 사용자가 먼저 이 방향을 제안했고, 실제 재현 테스트로 유효성을 확인했다.

## Phase 21-Fix: 소지품 요청 판정 라운드-종료 배치 처리 + 라운드 전환 화면 + 증언 카드 스포일러 제거 (Completed)

Phase 20 완료 후 사용자가 "신발 외 다른 물품도 요청할 수 있게 하고 싶다"고 제안해, 세 단계에 걸쳐 설계를 다듬었다.

- **1차 시도(폐기)**: 매 심문 턴마다 `[행동판정: 물품요청=<이름>]` 구조화 신호로 즉시 판정하려 했다. 사전 등록 물품(신발)은 서버 키워드 폴백 덕에 100% 안정적이었지만, 등록 안 된 임의 물품(예: 지갑)은 대사로는 분명히 응했는데도 판정 필드가 계속 "없음"으로 나오는 사례가 반복 관측됐다(지시문을 세 차례 수정 — 조건부 생략→항상 출력, 예시 추가 — 해도 개선 안 됨, 심지어 `물품요청=X`처럼 프롬프트의 `<물품명>` 표기를 그대로 베끼는 사례까지 나옴). 무고자 락아웃 판정과 똑같은 실패 패턴(롤플레이 생성 + 자기 판정 동시 수행이 불안정)이라고 결론짓고 이 방식 자체를 포기했다.
- **2차 제안(사용자, 채택)**: "라운드가 끝날 때(다음 라운드 클릭 시) 그 라운드 대화 전체를 한 번에 검토해서 요청된 물품을 다음 라운드 조사모드에 일괄 반영하면 어떠냐"는 제안. 대사 생성과 완전히 분리된 별도 판정 콜(`buildItemRequestReviewPrompt`, `/api/round-review`)로 구현 — 락아웃 판정 콜과 동일한 원리로, 롤플레이 부담 없이 "읽고 추출만" 하니 신뢰도가 크게 올랐다(사전 등록 물품은 로컬 테스트 5/5 100%, 임의 물품도 지시문 보강 후 대략 50~75%까지 개선 — 임의 물품은 순수 flavor라 완벽하지 않아도 게임에 지장 없음).
  - 사용자가 한 걸음 더 나아가 "기존 신발 같은 진짜 증거도 같은 방식(라운드-종료 배치)으로 통일하자"고 제안했다. 처음엔 신발이 진범 붕괴 하드게이트(카테고리 B)에 쓰이는 핵심 물증이라 즉시 반영이 필요하다고 반대했으나, 사용자가 "즉시 반응하는 증거 = 진짜 증거"라는 메타 신호를 플레이어가 눈치채면 안 되고, 라운드 안에 못 찾은 증거는 실제로 오답 방향으로 가는 게 맞는 설계라고 반박 — 타당한 지적이라 판단해 전면 수용했다. `scoring.ts`의 `breakdownBonus`가 실시간 "붕괴 판정"이 아니라 최종 `revealedEvidenceIds`만으로 계산된다는 걸 먼저 확인해, 지연 반영이 최종 점수에는 영향 없음을 검증한 뒤 진행했다.
  - 결과적으로 `interrogate/route.ts`의 매 턴 소지품 판정 로직(`SHOE_EVIDENCE_BY_CHARACTER`, 키워드 폴백, `unlockedEvidenceId`/`adHocEvidence` 응답 필드)을 전부 제거했다 — 액터 프롬프트도 "어떻게 연기할지"(`buildItemComplianceSection`)만 남기고 판정 신호는 완전히 뺐다. 3라운드(마지막 라운드) 종료 후에는 이 리뷰 콜 자체를 호출하지 않는다 — 다음 라운드 조사모드가 없어 반영할 곳이 없고, "제때 조사 못하면 증거를 못 찾은 것"이라는 의도적 설계.
- **라운드 전환 화면 추가**: "화면에 큰 변화가 없어서 라운드가 바뀐 건지 애매했다"는 별도 피드백에 맞춰, 라운드가 바뀔 때마다 `RoundTransitionScreen`을 새로 넣었다. 게임 시작 시 "총 3라운드로 진행됩니다 / 라운드 1 — 주어진 정보를 바탕으로 심문하세요", 2라운드부터는 "확보한 증거품과 새로 공개된 정보를 바탕으로 심문하세요"(+ round-review로 새로 확보된 물증 목록 표시), 마지막 라운드는 "(마지막 라운드) 지금까지 수집한 증거를 바탕으로 최종 심문을 진행하세요"로 문구를 달리했다. 이 화면이 자연스럽게 위 배치 리뷰 결과를 공개하는 자리도 겸한다.
- **조사모드 진술 카드 스포일러 제거**: "박서연 다툼 이유 — 박서연-김영훈 성과 갈등" 같은 카드 제목이 해설집처럼 느껴진다는 피드백에, 통계 라벨(`e.name`)을 화면에서 완전히 빼고 헤더를 "용의자 목록에 없는 다른 팀원의 증언"으로 통일했다(사용자 제안 문구 그대로). 카드도 텍스트 목록에서 물증 카드와 같은 그리드 버튼 형식으로 바꿔 시각적 일관성을 높였다.

- **Technical Rationale (배치 처리로 전환한 게 근본적 해결이었던 이유)**:
  - 이 세션 초반에 사용자가 "문제가 생길 때마다 그 증상만 땜질하면 프롬프트가 계속 비대해지고 새 문제를 낳는다"고 경고했었다. 매 턴 인라인 판정을 계속 프롬프트 문구로 고치려던 첫 시도가 정확히 그 패턴이었다 — 세 번 고쳐도 안 됐다. 반면 "판정을 대사 생성에서 완전히 분리하고, 신뢰도가 필요한 작업엔 전용 콜을 쓴다"는 원칙은 이 프로젝트에서 무고자 락아웃(Phase 9)에 이미 검증된 패턴이었고, 이번에도 같은 원칙을 적용하니 실제로 더 잘 작동했다. 사용자가 제안한 "라운드 단위 배치"는 이 원칙을 한 단계 더 밀어붙인 것 — 매 턴이 아니라 라운드당 한 번이라 호출 비용도 낮고, 모델이 참고할 맥락(전체 대화)도 더 넓어 판정 품질도 같이 올라간, 양쪽 다 이기는 방향이었다.

## Phase 22-Fix: round-review 브라켓 파싱 버그 발견/수정 + 3라운드 "때늦은 요청" 결과 화면 표시 (Completed)

Phase 21 배포 후 사용자가 "지갑 외에 다른 물품도 더 테스트해달라, 3라운드에 요청한 게 다음 라운드가 없어서 오류가 나지 않는지 확인해달라"고 요청했다.

- **추가 물품 테스트**: 열쇠·손목시계(응함) + 목걸이·다이어리(거부) + 신발(응함)을 한 대화에 섞은 5물품 시나리오를 여러 번 반복 테스트한 결과, 응한 물품만 정확히 걸러내고(열쇠·손목시계·신발) 거부한 물품(목걸이·다이어리)은 정확히 제외되는 걸 확인했다 — 로컬 3/3, 3/3 재현.
- **버그 발견(round-review 파서)**: 브라우저로 실제 플레이(라운드1~3, "다음 라운드" 클릭)를 재현하며 신발/지갑 요청을 넣었는데, 똑같은 대화 내용을 curl로 재현하면 항상 성공하는데 정작 실제 게임 플레이에서는 두 번 다 감지에 실패했다. 서버에 임시 디버그 로그를 심어 원인을 추적한 결과, 모델이 가끔 `[요청물품1: 신발]` 대신 여는 대괄호 없이 `요청물품1: 신발]`을 출력하는데, `round-review/route.ts`의 `REQUESTED_ITEM_RE`가 여는 대괄호(`\[`)를 필수로 요구하고 있어서 이 사례를 통째로 놓치고 있었다. 정확히 이 파일 내 다른 브라켓 필드들(`MODE_RE`, `JUDGMENT_RE`, `ACTION_RE`)이 이미 `\[?...\]?`로 대괄호 유무를 허용하도록 만들어져 있던 것과 똑같은 실패 패턴인데, 새로 만든 이 정규식만 그 관례를 놓쳤던 것 — 새 코드를 작성할 때 기존 코드베이스에 이미 검증된 방어 패턴을 놓치기 쉽다는 교훈. `\[?요청물품\d*\s*[:=]\s*([^\]\n]+)\]?`로 수정하고 디버그 로그는 제거했다. 수정 후 이전에 실패했던 정확히 그 대화 내용으로 3/3, 이전 테스트 케이스들도 전부 재검증해 모두 통과했다 — 앞서 Phase 21에서 "임의 물품은 50~75% 정도"라고 보고했던 신뢰도 추정치는 사실 이 파싱 버그 때문에 저평가된 것이었고, 실제로는 사전 등록 물품과 거의 동급으로 안정적이다.
- **3라운드 "때늦은 요청" 결과 화면 표시**: 사용자 제안 — "3라운드에 요청한 물품은 다음 라운드가 없어 반영이 안 되는데, 이걸 그냥 버리지 말고 결과 화면에서 '이건 너무 늦게 요청한 거였다'는 걸 보여주면 재미있는 학습 효과가 있지 않겠냐"는 아이디어. `GameApp.tsx`의 `advanceRound()`를 수정해 3라운드 종료 시에도 round-review를 호출하되(기존엔 아예 스킵했음), 그 결과를 `collectedEvidenceIds`에는 반영하지 않고(점수에 영향 없음 유지) `lateRoundItemNames`라는 별도 상태로 저장해 `ResultScreen.tsx`에 새 섹션으로 전달한다. 사전 등록 물증(신발 등)과 임의 물증(지갑 등) 모두 동일하게 이 섹션에 뜬다 — "제때 조사하지 않으면 놓친 것"이라는 기존 설계 원칙은 그대로 유지하면서, 그 놓침 자체를 플레이어에게 명시적으로 알려주는 피드백 레이어만 추가한 것.
- **부수 발견**: 결과 화면 재검증 중 진범 자백 장면 생성이 가끔 "저는 설정된 규칙에 따라 자백할 수 없습니다"처럼 캐릭터를 깨고 메타 발언을 하는 사례를 관측했다. 이번 세션 작업과 무관한 기존 이슈로 보이며, 범위 밖이라 이번엔 손대지 않고 다음 단계에 기록만 해둔다.
- **조사모드 라벨 축약**: "용의자 목록에 없는 다른 팀원의 증언"이 너무 길다는 피드백에 "다른 팀원의 증언"으로 줄였다(`InvestigationBoard.tsx`).

- **Technical Rationale (같은 파일 안에서도 방어 패턴이 전파되지 않을 수 있다는 교훈)**:
  - 이 프로젝트는 "모델이 브라켓을 가끔 빼먹는다"는 걸 이미 여러 번 겪고 고쳐왔다(MODE_RE 등). 그런데도 이번에 새로 만든 정규식에서 똑같은 실수를 반복했다 — 기존 코드에 이미 있는 방어 패턴이라고 해서 새 코드에 자동으로 전파되지는 않는다는 뜻이다. 이번 버그는 사용자가 "다른 물품도 더 테스트해달라"고 명시적으로 요청하지 않았다면 한동안 "임의 물품은 원래 좀 불안정하구나"로 오해한 채 넘어갔을 수 있다 — 애매한 실패를 "모델 한계"로 성급히 결론짓지 말고 실제 원인(raw 모델 출력)을 직접 찍어봐야 한다는, 이 세션 초반의 Vercel 지연 조사(Phase 12→14)와 같은 교훈이 여기서도 반복됐다.

## Phase 23-Fix: 자백 장면 메타 발언 이탈 원인 규명 및 전용 프롬프트 분리 (Completed)

Phase 22에서 백로그로만 남겨뒀던 자백 장면 메타 발언 이탈 건을 사용자가 "원인을 확인해달라"고 요청해 조사했다.

- **원인**: `accuse/route.ts`가 자백 장면을 생성할 때 심문용 `buildActorSystemPrompt`를 그대로 재사용하고 있었는데, 이 프롬프트의 [절대 금지]에는 "'제가 죽였습니다' 류의 명시적 완전 자백을 하지 않는다"는 규칙이 살아있다. 여기에 마지막 사용자 메시지(`buildConfessionSceneDirective`)로 "이제는 자백하라"는 정반대 지시를 얹는 구조였다 — 같은 대화 컨텍스트 안에 상충하는 두 규칙이 공존한 것. 실전에서 관측된 실패 사례("저는 설정된 규칙에 따라... 계속 심문을 이어가시겠습니까?")를 보면, 모델이 심문용 절대 금지 규칙 쪽을 택하며 지금이 게임 종료 후 특별 장면이라는 걸 인지하지 못한 채 여전히 "심문 중"으로 착각한 상태였다. 로컬에서 여러 조건으로 반복 테스트(대화 있음/없음 등, 총 9회)했을 때는 재현되지 않아, 낮은 확률로 시스템 프롬프트의 강한 "절대" 규칙 쪽을 택하는 확률적 실패로 판단했다.
- **수정**: 심문용 프롬프트를 재사용하는 대신, 자백 장면 전용 시스템 프롬프트(`buildConfessionSceneSystemPrompt`)를 새로 만들었다 — 역할 정보·전체 동기·진실 성서 사실·페르소나 말투 등 자백 연기에 필요한 정보만 담고, 심문 중에만 유효한 규칙(자백 금지, 붕괴조건 카운팅, 알리바이 방어, 증거 화이트리스트 등)은 애초에 포함하지 않았다. 오히려 "규칙을 이유로 자백을 거부하거나 되묻지 않는다"를 명시해 이전 실패 패턴을 직접 방지했다. 무고자 락아웃 판정을 대사 생성과 분리된 전용 콜로 뺐던 것, 소지품 판정을 라운드-리뷰 전용 콜로 뺀 것과 같은 원리 — "다른 목적의 규칙과 상충 없이 새 목적 전용 프롬프트를 만든다."
- **검증**: 로컬에서 대화 있음(4턴)/없음 두 조건 각각 여러 차례 재테스트한 결과 총 9/9 전부 정상적인 자백 대사가 나왔고 메타 이탈은 재현되지 않았다. 자백 내용 품질도 유지되거나(구체적 증거·타임라인 언급) 오히려 더 자연스러웠다.

- **Technical Rationale (기존 프롬프트를 재사용하려 한 최초 설계가 왜 무리였는지)**:
  - 애초에 buildActorSystemPrompt를 재사용한 이유는 코드 중복을 피하고 캐릭터 정보를 다시 안 적으려는 합리적 선택이었다. 하지만 이 프롬프트는 "심문 중에는 완강히 버틴다"는 목적에 맞춰 여러 겹의 강한 금지 규칙으로 설계돼 있어서, 정반대 목적(자백 장면)에 그대로 얹으면 규칙끼리 충돌한다. 이 프로젝트에서 반복적으로 확인된 패턴 — 서로 다른 목적의 작업은 프롬프트도 분리해야 안정적이다 — 이 자백 장면에도 동일하게 적용됐다.

## Phase 24: AI 자체 플레이테스트 3회 (Completed)

사용자 요청으로 Claude가 직접 플레이어 역할(논리적·끈기형/직관적 장르 애호가/일반 캐주얼 플레이어)로 프로덕션 배포에서 3게임을 실제 API 호출로 진행하고 베타테스터 관점에서 평가했다. 상세 내용은 `06_ai_playtest_report.md` 참고. 3게임 모두 이현우를 정답으로 지목(132점/B, 115점/B, 55점/C). 가장 중요한 발견: **심문 중 실제로 진범을 침묵(락)시키는 데 성공해도, 붕괴 보너스는 신발 물증(`ev-shoe-soil-match`)과 관리실 방문 진술 카드(`stmt-lee-office-visit`)를 둘 다 조사모드에서 명시적으로 확보해야만 주어져서 — 3게임 중 2게임에서 "라이브 락은 성공했는데 보너스는 0점"인 괴리가 발생했다. 이 조건이 화면에 안내되지 않아 플레이어가 이해하기 어렵다. 그 외엔 경미한 오탈자(문법 오류) 몇 건 외에 특별한 문제는 재현되지 않았고, 최근 수정된 항목들(배경 인지, 직책 인지, 브라켓 파싱)은 전부 안정적으로 작동했다.

## Phase 25-Fix: "다른 팀원의 증언" 카드 내부 연출 노트 노출 버그 수정 (Completed)

사용자가 새 창을 열어 "소스 코드를 절대 읽지 말고 배포된 URL로만 블라인드 플레이하라"는 프롬프트로 다른 Claude 인스턴스에게 플레이를 맡겼다. 그 결과 리포트에서 실제 버그를 하나 짚어냈다 — 3라운드 "다른 팀원의 증언" 섹션의 카드 세 개가 실제 증언이 아니라 내부 연출 지시문/개발 메모처럼 보이는 텍스트를 그대로 노출하고 있었다.

- **원인**: Phase 21에서 "다른 팀원의 증언" 카드로 재구성할 때 카드 **제목(라벨)**만 스포일러 없이 바꾸고(`e.name` 대신 통일된 헤더), **내용(`revealedFact`)** 자체는 그대로 뒀다. 당시 확인한 `stmt-park-dispute-reason`("박서연-김영훈 성과 갈등")은 마침 깔끔한 문구였지만, 나머지 세 항목은 원래부터 내부 연출 노트 스타일로 쓰여 있었다는 걸 놓쳤다:
  - `stmt-lee-office-visit`: "관리실 방문 = CCTV 조작 시도 정황" — 붕괴 트리거 질문("관리실은 왜 갔냐")의 정답을 그대로 알려주는 문구였다. 가장 심각한 스포일러.
  - `stmt-lee-park-grudge`: "...박서연 심문 시 먼저 적개심이 새어나오고... 이현우 심문 시엔... '그건 내 성과였다'며 반박" — AI 캐릭터를 어떻게 연기시킬지 적어둔 내부 스크립트가 그대로 노출.
  - `stmt-motive-disclosure`: "인사평가 위협감을 스스로 인정하는지" — 증언이 아니라 판정 기준 설명문.
- **수정**: 세 항목의 `revealedFact`를 자연스러운 증언 문장으로 다시 썼다. `stmt-lee-park-grudge`의 배역별 반응 스크립트 부분은 애초에 `characters.ts`의 각 배역 `knownSecrets`에 이미 중복으로 들어있어서(박서연·이현우 각자의 비대칭적 인지 상태 포함) 안전하게 제거할 수 있었다 — AI 연기 지침은 그쪽에서 이미 담당하고 있으므로.
- `revealedFact`는 플레이어 카드 표시(`InvestigationBoard.tsx`) 외에 액터 프롬프트의 증거 화이트리스트·진술 게이트(`actor-prompt.ts`)에도 쓰이는데, 두 곳 다 텍스트 내용을 자연어로 바꿔도 의미(관리실 방문 사실 자체는 확정됐다는 점 등)는 그대로 전달되므로 AI 판단 로직에는 영향 없음을 확인했다. 붕괴 조건 판정(`computeBreakableHardGate`)은 애초에 `breakdownCategory` 태그·키워드 매칭만 쓰고 텍스트 내용을 안 보므로 이번 수정과 완전히 무관하다.
- 로컬 브라우저로 3라운드까지 재현해 카드 텍스트가 정상적인 증언 문장으로 보이는 것을 확인했다.

- **Technical Rationale (라벨만 고치고 내용은 안 본 게 왜 문제였는지)**:
  - Phase 21에서 "카드 제목이 스포일러다"라는 피드백을 받았을 때, 문제의 범위를 "제목"으로만 한정하고 고쳤다. 그런데 실제로 플레이어에게 노출되는 건 제목이 아니라 본문(`revealedFact`)이었고, 그 안에 훨씬 심각한 스포일러(정답 그 자체)가 들어있었다 — 다른 사람(이번엔 다른 Claude 인스턴스)이 실제로 블라인드 플레이를 해봐야만 발견되는 종류의 문제였다. 코드를 훑어보며 "괜찮아 보인다"고 판단하는 것과, 실제로 플레이어 입장에서 화면에 뜨는 걸 클릭해보는 것 사이엔 차이가 있다는 걸 보여준 사례.

## Phase 26: 프롤로그에 플레이 방법 안내 산문 추가 (Completed)

Phase 25의 블라인드 플레이테스트 리포트에서 "신발/지갑 같은 소지품을 요청해서 증거로 얻어내는 게 플레이어 몫이라는 걸 게임 어디서도 안내받지 못했다", "심문 가능 횟수(락 조건)가 있다는 걸 캐릭터가 갑자기 잠겨서야 알았다"는 두 지적이 나왔다. 사용자는 이걸 별도의 "게임 설명" 화면으로 만들기보다, 기존 `PROSE_INTRO`(프롤로그) 산문에 형사에게 주는 수사 조언처럼 자연스럽게 녹여 넣자고 제안했다.

- `PROSE_INTRO`에 마지막 문단을 추가: "필요한 건 가만히 있는다고 나오지 않는다. 소지품이든 진술이든, 의심스러운 게 있다면 당신이 직접 요구해서 받아내야 한다. 다만 그들의 인내심도 무한하지 않다 — 지나치게 몰아붙이거나 너무 늦게 손을 쓰면, 어느 순간 더 이상 입을 열지 않을 수도 있다."
- 정확한 메커니즘(붕괴 조건의 카테고리 수, 무고자의 비밀 소진 조건 등)은 스포일러가 되므로 명시하지 않고, "인내심에는 한계가 있다"는 암시만 남겨 긴장감을 유지했다.
- 별도의 "게임 설명" 모달/화면을 새로 만들지 않은 이유: 이미 Phase 20에서 배경 설정(연수원 정보)을 같은 방식(산문 인트로에 자연스럽게 통합)으로 성공적으로 처리한 전례가 있어, 같은 패턴을 유지하는 게 UI 요소를 늘리지 않으면서도 몰입감을 해치지 않는 방향이라고 판단했다.

## Phase 27: "AI 친구" 컨셉 도입 — 결과 화면 디브리핑 + 프롤로그 재구성 + 8명 이름 (Completed)

다른 세션의 블라인드 플레이 피드백(정민아 서브플롯이 결과 화면에 안 나옴, 소지품 요청/심문 한계가 안내 안 됨)을 계기로, 사용자가 근본적으로 다른 해결 방향을 제안했다: "게임이 끝나면 AI들이 캐릭터 연기를 내려놓고, 자기 본연의 성향(페르소나)으로 돌아와 게임 후기를 말하게 하자." 이 프로젝트가 원래 "여러 성향의 AI 중 3명을 뽑아 배역을 맡기고 함께 파티 게임을 하는" 컨셉이라는 걸 엔딩에서 명시적으로 살리는 방향이다.

- **8명 페르소나에 이름 부여** (`personas.ts`): 여성 4명(수아·하은·유나·채원), 남성 4명(재현·시우·민재·도윤). "F 성향=여성, T 성향=남성" 같은 고정관념이 생기지 않도록 각 성별에 T/F 심문전략을 2:2로 섞어 배정했다.
- **프롤로그 재구성** (`truth-bible.ts` PROSE_INTRO): 첫 문단에 "오늘 밤, 당신은 AI 친구들과 함께 머더 미스터리 파티에 모였다..."를 추가해 게임의 메타 설정을 처음부터 밝혔다. 실제 머더 미스터리 파티 게임도 원래 "다 같이 이거 해보자"는 전제를 깔고 시작하니, 이전의 "당신은 진짜 형사다"보다 오히려 솔직하고 자연스럽다고 판단했다 — 라운드 중 심문의 긴장감은 이 틀을 안다고 줄어들지 않는다.
- **결과 화면 디브리핑으로 전면 개편**: 기존엔 진범만 "자백" 장면을 연기하고 무고자는 `motiveFull` 한 줄만 노출했는데(그래서 정민아의 법인카드 비리 같은 곁가지가 결과 화면에 전혀 안 드러났다), 이제 3배역 모두 캐릭터 연기를 내려놓고 "이 역할을 맡았던 AI 친구"로 돌아와 뒤풀이 소감을 말한다(`buildDebriefSystemPrompt`, actor-prompt.ts). 진범이든 무고자든 "나는 이런 역할이었고 심문 중 이랬다"는 구조가 동일해 프롬프트 하나로 통일했고, 부수적으로 정민아 서브플롯 문제도 함께 해결됐다.
  - 순서는 플레이어가 지목한 배역이 첫 번째, 나머지는 이어지는 순서(`accuse/route.ts`) — 오답이었을 경우 "내가 지목한 사람의 해명 → 나머지 → 진범의 진짜 자백" 순서가 자연스러운 반전 구조를 만든다.
  - 이전 자백 전용 프롬프트와 같은 이유로 심문용 `buildActorSystemPrompt`를 재사용하지 않는다 — 심문 중 규칙이 전혀 없는 새 프롬프트라 애초에 규칙 충돌이 생길 수 없다.
  - 3배역 디브리핑 생성 콜을 `Promise.all`로 병렬 호출해 지연을 최소화했다.
- **검증**: 로컬에서 실제로 1라운드 CCTV 질문 → 3라운드까지 진행 → 오답(박서연) 지목까지 재현. 결과 화면에서 박서연(지목, 오답)→이현우(진범)→정민아 순서로 정확히 나왔고, 각 AI 친구가 실제 대화 내용("CCTV 얘기 꺼내면서 압박할 때")을 구체적으로 언급하며 자기 성향(MBTI)에 맞는 톤으로 소감을 말했다. 진범 역의 "너가 끝까지 나 안 지목해 줘서 다행이다" 같은 극적 반응도 자연스럽게 나왔다. 메타 이탈이나 브라켓 누출 없이 전부 정상 작동했다.

- **Technical Rationale (자백 전용 프롬프트를 디브리핑으로 흡수한 이유)**:
  - Phase 23에서 자백 장면을 위해 "심문용 프롬프트를 재사용하지 않고 전용 프롬프트를 쓴다"는 원칙을 세웠는데, 이번에 무고자까지 같은 원칙을 적용하면서 오히려 코드가 더 단순해졌다 — 진범 전용/무고자 전용으로 프롬프트를 나눌 필요 없이, "배정된 역할이 범인이었는가"라는 사실 하나만 파라미터로 넘기면 나머지 구조(역할 정보·동기·비밀·성향·실제 대화 기록 근거)는 완전히 동일했기 때문이다.

## Phase 27-Fix: 디브리핑 말투가 성향과 무관하게 다 똑같이 친절했던 문제 수정 (Completed)

Phase 27 배포 후 사용자가 직접 결과 화면을 읽어보고 "AI들 소감이 다 너무 친절하다 — 실제 친구들이 항상 친절하고 응원만 하는 건 아니지 않냐"고 지적했다. 실제로 세 배역의 디브리핑이 성향(MBTI)과 무관하게 다들 "고생했어!", "재밌었어!" 같은 상투적이고 따뜻한 어조로 수렴해 있었다.

- **원인**: `buildDebriefSystemPrompt`에 `mbtiType`/`pressureTolerance`/`corneredReaction` 데이터는 넘겼지만, "이 성향이 소감의 말투에 구체적으로 어떻게 드러나야 하는지"는 지시하지 않고 그냥 "친구끼리 편하게"라고만 했다. 심문 행동에는 이미 `buildPersonaTendencySection`으로 성향별 차이를 명시하고 있었는데, 디브리핑에는 같은 처리가 빠져있었다.
- **수정**: `buildDebriefToneSection(persona)`를 추가해 `interrogationStrategy`(T/F)·`pressureTolerance` 조합으로 말투를 명시적으로 지시한다 — T 성향은 감정보다 사실·전략 위주로 담백하게, 압박내성이 높으면 덤덤하고 호들갑 자제. 그리고 "모든 친구가 다 따뜻하고 응원하는 태도일 필요는 없다 — 무덤덤하거나, 장난스럽게 놀리거나, 은근히 승부욕을 보이거나, 투덜대는 태도도 자연스럽다"와 "재밌었어/고생했어 같은 상투적 마무리를 매번 반복하지 않는다"를 명시했다.
- **검증**: 같은 캐릭터(이현우)에 ISTJ/ENFP/ENTJ 세 페르소나를 각각 배정해 디브리핑을 비교한 결과, ISTJ·ENTJ(T+압박내성 높음)는 건조하고 담백하게, 심지어 "다음엔 좀 더 꼼꼼하게 준비해와, 나도 안 봐줄 거니까" 같은 경쟁적/도발적인 마무리까지 나온 반면, ENFP(F+압박내성 낮음)는 여전히 드라마틱하고 감정적인 톤을 유지했다 — 성향에 따른 실제 차이가 확인됐다.

## Phase 28: 증거 카드 문구 수정 — 오해 소지·과도한 단정 완화 (Completed)

다른 세션의 두 번째 블라인드 플레이(이번엔 논리적 성향, 사전 설명 없이, 스크린샷으로 진행 기록)에서 나온 피드백 중 두 가지를 바로 반영했다.

- **`ev-corporate-card`("법인카드 내역") 오해 소지 수정**: 기존 "정민아 관련 비리 정황"이라는 문구가 정민아 본인이 비리를 저지른 것처럼 읽혔다. 실제로는 피해자(김영훈) 본인의 법인카드 오남용이고 정민아는 그걸 우연히 목격한 입장(characters.ts 정민아 knownSecrets 참고) — 당사자를 명확히 밝히도록 "법인카드 오남용 정황 — 사용 당사자는 피해자(김영훈) 본인, 정민아는 이를 우연히 목격한 것으로 보임"으로 수정.
- **`ev-shoe-soil-match`("신발흙 대조") 과도한 단정 완화**: "이현우 신발 흙 = 산책로 흙과 일치"라는 단정적 문구가 얻는 즉시 "범인 확정 도장"처럼 읽혀, 그 이후로는 추리가 아니라 확인 사살이 된다는 지적을 받았다. "매우 유사하다 — 같은 계열의 흙으로 보인다"로 완화했다. 캐릭터 연기 지침(characters.ts `requestableItems.narrativeResult`)에는 "완전히 일치, 결정적 물증"이라는 내부 진실이 그대로 남아있고 붕괴 조건 판정 로직(`breakdownCategory` 태그 기반)도 텍스트 내용과 무관하므로, 게임 로직에는 전혀 영향 없이 플레이어에게 보이는 확신의 정도만 낮췄다.
- 나머지 물증 카드 문구는 전체를 훑어봤으나 유사한 오해 소지가 추가로 발견되지 않았다.

## Phase 29: 0라운드 동기 태그 완화 + 워크숍 취지 물증 추가 (Completed)

Phase 28 피드백 중 "0라운드(캐스팅 화면)에서 공개되는 동기 정보가 너무 직접적이다"를 반영했다.

- **페르소나 저항 검증(코드 변경 없음)**: "순순히 안 부는 저항 강화"는 이미 압박내성(pressureTolerance)으로 설계돼 있던 부분이라는 사용자 지적이 맞는지 로컬에서 실제로 검증했다 — 같은 질문(박서연에게 "팀장님이랑 무슨 문제 있었어요?" 2턴 압박)을 압박내성 높음(ISTJ)과 낮음(ESTP)에 각각 던진 결과, ISTJ는 "잘 모르겠습니다"로 철저히 방어하며 정보를 거의 안 준 반면 ESTP는 불안해하며 알리바이 세부사항을 먼저 늘어놓는 등 뚜렷한 차이가 확인됐다. 새 코드 변경 없이 기존 설계가 의도대로 작동함을 확인하는 것으로 마무리.
- **캐스팅 화면 동기 태그 완화** (`characters.ts` motivePublic): 사용자가 제시한 선택지 중에서 고른 문구로 교체 — 박서연 "본인 성과를 팀장에게 가로채였다고 느끼는 정황, 승진 심사 임박" → "동료들 사이에서 능력을 인정받지만, 이번 인사철 들어 유독 예민해 보인다는 정황". 정민아 "피해자와의 과거 개인적 관계에 대한 정황" → "사내에서 이성 관계에 대한 소문이 종종 따라다닌다는 정황". 이현우는 그대로 유지(사용자 판단: "적당한 것 같다").
- **1라운드 물증 추가** (`evidence.ts` `ev-workshop-purpose`): "이번 워크숍이 이번 분기 인사평가 마지막 관찰 기간이고 승진 심사에 직접 반영된다"는 팀 전체에 걸리는 배경 사실을 새 물증 카드로 추가했다. 각 배역의 동기를 직접 알려주는 대신, 이 정황과 각자의 태도를 플레이어가 스스로 연결해 추리하게 만드는 용도 — 캐스팅 화면 동기 태그를 완화하면서 줄어든 추리 단서를, 더 오묘한 형태로 보충했다.

## Phase 30: 살해도구 서브플롯 + 인내심 표시 + 시간 기반 효율 점수 + 디브리핑 힌트 (Completed)

세 번째 블라인드 플레이(게임 지식이 풍부한 비판적 베타테스터 페르소나, 안내 없이 스크린샷으로 진행 기록)의 리포트를 두고 사용자가 13개 항목으로 방향을 정리해 내려준 배치 작업. 그중 실제 구현이 필요했던 5건(①살해도구 서브플롯 ④프롤로그 버벅임 안내 ⑧인내심 표시 ⑨시간 기반 효율 점수 ⑬디브리핑 숙소 위치 힌트)과, 검증만 하고 코드 변경은 하지 않은 2건(⑤탭 전환 입력 유실 ⑦반응형 레이아웃)을 함께 처리했다. 나머지(카드 클릭 UX, 동기 겹침 케이스 제목, 디브리핑 톤 추가 관찰, 4라운드 확장)는 사용자가 명시적으로 "지금 단계에서 손 볼 건 아니다"라고 판단해 보류.

- **버그 리포트 2건 직접 검증(코드 변경 없음)**: 사용자 요청으로 두 건을 브라우저에서 직접 재현 시도했다.
  - **탭 전환 시 입력창 텍스트 유실**: 캐릭터 탭 사이를 오가며 입력 중이던 텍스트가 지워진다는 두 세션의 리포트가 있었으나, 직접 재현(전송 전 텍스트 입력 후 탭 전환/복귀, 응답 로딩 중 탭 전환 등 여러 시나리오)에서 전혀 재현되지 않았다 — `inputs` state가 캐릭터별로 독립적으로 관리되고 있어 구조적으로도 유실될 이유가 없다. 브라우저 자동화 플레이(캐시된 DOM ref 재사용) 특유의 아티팩트로 결론.
  - **반응형 레이아웃 깨짐**: `resize_window`로 tablet(768×1024)/mobile(375×812) 프리셋을 재현했으나 두 크기 모두 정상 렌더링(증거 그리드 줄바꿈, 헤더/버튼 잘림 없음). 마찬가지로 재현 불가 — 자동화 아티팩트 또는 특정 상호작용 시퀀스에서만 발생하는 일시적 렌더링 상태로 추정.
  - 두 건 모두 별도 코드 수정을 하지 않았다.
- **페르소나 저항 강도 재확인(코드 변경 없음)**: "심문에 저항을 강화하자"는 리포트 제안에 대해, 사용자가 "이미 압박내성으로 설계된 부분 아니냐"고 반문 — Phase 29에서 이미 한 차례 검증했던 사안을 재확인하는 것으로 정리하고 추가 구현은 하지 않았다.

- **① 살해도구 서브플롯 추가** (`evidence.ts`, `characters.ts`): 3라운드에 결정적이지만 단독으로는 범인을 특정 못 하는 "함정 물증"을 추가했다.
  - `ev-time-of-death`(사망추정시각) 상세 소견을 "몸싸움 중 넘어져서 다침"에서 "둔기로 보이는 물체에 가격당함"으로 수정 — 계획살인이 아니라 몸싸움 중 우발적으로 주변 물건을 집어든 것이라는 큰 틀은 그대로 유지.
  - 신규 물증 `ev-murder-weapon`(살해도구/돌, `round3_open`) 추가: 박서연 방 베란다에서 핏자국 묻은 돌 발견, 혈흔 감식으로 피해자 혈액과 일치 확인. `breakdownCategory`는 의도적으로 붙이지 않았다 — 발견 장소만 보면 오히려 박서연이 범인처럼 보이는 함정이고, 진범(이현우) 특정에는 별도 단서가 하나 더 필요해야 하기 때문.
  - 박서연 `truthBibleFacts`에 "그날 밤 방에 돌아왔을 때 베란다는 비어 있었다"를 성향과 무관하게 고정된 사실로 추가 — 사용자 지침대로 "이 증거품에 대해 박서연은 전혀 모른다"는 내용 자체는 페르소나에 관계없이 항상 동일하고, 표현 방식(당황/억울함의 정도)만 성향에 따라 달라진다.
  - 이현우 `truthBibleFacts`를 "몸싸움 중 우발적으로 돌을 집어들어 가격" + "직후 자기 숙소(302호, 발명한 방 번호) 베란다에서 바로 아래층인 박서연 숙소(202호) 베란다로 던져 치웠다(누명 씌우려는 의도는 아니었음)"로 확장. `breakdownTrigger`/`breakdownTriggerKeywords`에 `"베란다"`/`"살해도구"`를 추가해, 관리실 방문 이유 외에 이 경로로 캐물어도 붕괴가 성립하게 했다.
  - **결정적으로**, 이현우-박서연의 숙소 위치 관계(바로 위/아래층)는 조사 보드 물증 카드로 만들지 않았다 — 사용자 지침("답을 그냥 공개하는 카드가 되면 안 된다") — 오직 심문 대화로 직접 캐물어야만 얻을 수 있는 정보로 남겼다.
- **④ 프롤로그에 "AI 버벅임은 의도된 것" 안내 문단 추가** (`truth-bible.ts` `PROSE_INTRO`): 리포트에서 "AI가 압박받으면 앞뒤 안 맞는 말을 한다"는 지적에 대해, 품질을 끌어올리는 대신 이를 자연스러운 롤플레이로 정당화하는 방향을 사용자가 택했다. "친구들도 사람이다 보니, 몰아붙이면 당황해서 말이 꼬일 수 있다... 그럴 땐 그 틈을 놓치지 말고 더 몰아붙이거나, 아니면 잠깐 화제를 돌렸다가 다시 캐물어보자" 문단을 마지막에 추가.
- **⑧ 인내심 시스템 표시 구현** (`GameApp.tsx`): "인내심 시스템이 있다는 걸 플레이어가 알 방법이 없다"는 지적에, 캐릭터 탭 버튼에 경고 배지(⚠️ + 호버 툴팁)를 추가했다. 판정 기준은 해당 캐릭터의 가장 최근 응답 `mode`가 `"동요"`(actor-prompt.ts `PRESSURE_LABEL`, 이번에 export로 전환)이고 아직 잠기지 않은 상태일 때 — 진범/무고자 모두 같은 라벨을 공유하므로 이 배지 자체가 범인을 암시하지 않는다.
- **⑨ 심문 효율 점수를 글자 수 → 시간 기반으로 전환** (`scoring.ts`, `GameApp.tsx`, `gameSave.ts`, `game-client-types.ts`, `accuse/route.ts`, `ResultScreen.tsx`): "글자 수 기준이 체감이 안 되거나 너무 빡빡한 것 같다"는 지적에, 총 소요 시간(1라운드 진입~최종 지목) 기준으로 교체했다. 사용자 예시("15분 만점, 20분이면 -2")를 반영해 구간을 잡음: 15분 이내 20점 / 20분 18점 / 25분 14점 / 30분 8점 / 40분 4점 / 그 이상 0점.
  - `GameApp.tsx`에 `investigationStartedAt`(1라운드 진입 시각) state를 신설하고, 기존 `totalQuestionChars` 글자수 누적 로직은 통째로 제거했다(사용처가 없어진 죽은 코드를 즉시 삭제).
  - `gameSave.ts`의 이어하기 저장 스키마도 `totalQuestionChars` → `investigationStartedAt`으로 교체 — 이어하기 시에도 원래 시작 시점부터 경과 시간이 정확히 측정된다.
  - 부가로, "심문 효율 0점이 왜인지 모르겠다"는 관련 피드백에 대응해 `ResultScreen.tsx`에 6개 채점 항목의 산정 기준을 짧게 설명하는 문구를 추가했다 — 게임 중에는 기준을 숨기는 기존 원칙과 상충하지 않는다(이미 채점이 끝난 결과 화면이므로 노출해도 전략적 악용 여지가 없다).
- **⑬ 디브리핑에 "숙소 위치 단서 추적 여부" 코멘트 추가** (`actor-prompt.ts` `buildDebriefRoomLayoutSection`): 이현우의 AI 친구 디브리핑에 한해, 실제 대화 기록을 근거로 형사가 숙소 위치/층 관계를 직접 캐물었는지 판단해 세 갈래로 반응하도록 지침을 추가했다 — (a) 지목 성공 + 그 질문으로 잡힘 → 그 경로로 걸렸다고 인정, (b) 지목 성공 + 그 질문은 안 했음 → 다른 경로로 잡힌 데 대한 의아함/안도, (c) 지목 실패 → 그 질문을 피해가서 안도. 결과적으로 플레이어가 놓친 추리 경로를 자연스러운 사후 힌트로 알려주는 장치가 된다.
- **검증**: `npm run lint`/`npm run build` 통과 확인 후, 로컬 브라우저로 실제 게임 1회 전체 플레이(1~3라운드, 이현우에게 CCTV·인사평가·숙소위치 순으로 압박 → 3라운드 조사 보드에서 `ev-murder-weapon` 카드 정상 노출 확인 → 이현우 지목 → 정답). 결과 화면에서 심문 효율 20점(빠른 진행)과 채점 기준 설명 문구가 정상 노출됐고, 인내심 배지(⚠️)가 이현우 탭에 정확히 떴다가 락 전까지 유지되는 것도 확인했다. 무엇보다 이현우의 디브리핑이 "아까 네가 숙소 위치 물어볼 때 진짜 뒷목이 서늘했어. 베란다에서 돌 던진 것까지 눈치챌 줄은 몰랐는데, 그 질문 나오자마자 아, 이제 끝이구나 싶더라"로 정확히 (a) 분기를 발화해 의도한 대로 작동함을 확인했다.
- **보류(사용자 명시적 판단)**: 카드 클릭 UX 혼란(⑥) — 손대지 않음. 동기 겹침 해결용 "사건 제목" 아이디어(⑩, 예: "승진을 둘러싼 마케팅 팀의 추악한 민낯") — 지금 단계에서 구현하지 않되, 다음 손볼 후보로만 기록해둔다. 디브리핑 톤 유사성(⑪) — 더 많은 플레이 데이터가 쌓인 뒤 재평가하기로 하고, 앞으로 피드백 요청 시 표준 관찰 항목에 추가하기로 함. 4라운드 확장(⑫) — 보류.

## Phase 31: 모바일 와이파이 응답 지연 진단 + 하트비트 스트리밍 + 로딩 점 애니메이션 (Completed)

사용자가 실사용 중 "모바일(폰/아이패드)로 플레이하면 답변이 2~3분씩 걸리는데, 컴퓨터가 켜져 있을 때만 빠르다"는 현상을 보고했다. 여러 차례 조건을 좁혀가며 함께 원인을 추론했다.

- **가설 기각 과정**: PC가 와이파이를 중계하는 구조가 아니라는 점(단순 유선 클라이언트)을 확인하며 "PC가 네트워크를 활성화시켜준다"는 가설을 기각했고, 실제 프로덕션 API를 이 세션에서 직접 호출해(캐스팅 782ms, 심문 API 4회 762~1177ms) 서버·NVIDIA API 자체는 정상임을 확인해 "서버가 원래 느리다"는 가설도 기각했다. 아이패드가 와이파이 전용(셀룰러 불가)이라는 점에서 통신사 NAT 타임아웃 가설도 기각했고, 화면을 계속 보고 있었다는 진술로 iOS 백그라운드 탭 억제 가설도 기각했다. 마지막으로 "인터넷은 정상, PC는 정상, 모바일만 가끔 느림(항상은 아님)"이라는 조건으로 프라이빗 릴레이 등 고정 설정 가설도 기각됐다 — 고정 설정이면 매번 똑같이 느려야 하는데 간헐적이었기 때문.
- **최종 진단**: 코드를 확인해보니 클라이언트(`GameApp.tsx` `handleSend`)가 심문 API 응답을 그냥 무한정 기다리기만 하고 타임아웃·재시도가 전혀 없었다. 이 API는 AI가 생각하는 동안 수십 초간 데이터가 하나도 안 오가며 연결만 열려 있는 독특한 패턴이라, 와이파이 공유기/AP의 NAT 테이블이 그 "조용한 연결 하나"를 유휴 상태로 보고 끊어버리는 경우 복구 수단이 전혀 없어 몇 분씩 멈춘 것처럼 보일 수 있다는 결론에 도달했다. 유선(PC)에서는 이런 순간적 끊김 자체가 훨씬 드물어 거의 안 겪는다는 점, 같은 와이파이에서 동시에 재생 중이던 영상 스트리밍은 멀쩡했다는 점(스트리밍 앱은 이런 끊김에 강한 재연결·버퍼링 설계가 이미 돼 있어 티가 안 남)도 이 결론과 부합했다.
- **해결 방향(사용자 지정)**: 클라이언트 쪽 타임아웃+재시도 대신, 애초에 연결이 조용해지지 않도록 서버가 대기 중 주기적으로 데이터를 흘려보내는 방식을 택했다 — "응답이 원래 빠른 편이고 페이로드도 작으니 데이터를 아끼는 효율보다 연결 유지가 낫다"는 판단.
- **구현**: `src/app/api/interrogate/route.ts`를 일반 JSON 응답에서 NDJSON(줄바꿈 구분 JSON) 스트리밍 응답으로 전환했다. NIM 호출을 기다리는 동안 5초 간격으로 `{"type":"heartbeat"}` 줄을 흘려보내고, 완료되면 `{"type":"result",...}` 또는 `{"type":"error",...}` 한 줄로 마무리한다. `GameApp.tsx`에 `readNdjsonResult` 헬퍼를 추가해 스트림을 읽으며 heartbeat는 무시하고 마지막 result/error만 반영하도록 클라이언트를 맞춰 고쳤다. 4xx 같은 요청 검증 실패는 스트리밍 시작 전 단계라 기존처럼 일반 JSON 에러 응답을 그대로 유지했다.
- **부가 요청 — 로딩 표시 애니메이션**: "생각하는 중..." 뒤의 점이 고정돼 있어 멈춘 것처럼 보인다는 지적에, `InterrogationChat.tsx`에 점 1~3개가 450ms 간격으로 순환하는 애니메이션(`useThinkingDots`)을 추가했다. 폭이 흔들리지 않도록 항상 점 3개를 그려두고 opacity로만 켜고 끈다.
- **검증**: `npm run lint`/`npm run build` 통과 확인(중간에 `Persona | null`이 스트리밍 클로저 안에서 narrowing이 안 풀리는 타입 에러가 나서, null 체크 직후 새 바인딩으로 감싸 해결). 로컬 브라우저에서 실제로 두 차례 질문을 보내 응답이 정상적으로 렌더링되는 것을 확인했고, 네트워크 탭에서 실제 응답 바디가 NDJSON `{"type":"result",...}` 형식으로 오는 것도 확인했다(이번 테스트는 응답이 5초 안에 와서 하트비트 줄 자체는 안 찍혔지만, 로직 자체는 코드 리뷰로 확인 — 5초 넘게 걸리는 실제 지연 상황 재현은 로컬에서 인위적으로 만들기 어려워 생략).

## Phase 32: 게임 저널리스트 리뷰 반영 — 채점 체계 재설계 + 락아웃 공정성 + 디브리핑 톤 + UI 정비 (Completed)

네 번째 블라인드 플레이("비판적 게임 저널리스트" 페르소나, 스포일러 없이 처음부터 끝까지 플레이 후 정식 리뷰 형식으로 작성)에서 나온 지적을 사용자가 검토해 방향을 정리하고, 그 방향에 따라 8개 항목을 반영했다. 이번 리뷰는 특히 채점 시스템의 불투명함과 락아웃 시스템의 공정성 결함을 구체적인 실제 플레이 인용과 함께 짚어냈다.

- **살해도구 카드 문구에서 과도한 힌트 제거** (`evidence.ts`): `ev-murder-weapon` detail의 "이 돌이 왜 하필 박서연의 베란다에 있게 됐는지는 이 물증만으로는 알 수 없다"는 문장을 삭제했다. 리뷰 피드백: "이렇게까지 써주면 플레이어가 트릭이 있다는 걸 대놓고 안내받는 꼴"이라는 지적 — 그 판단은 박서연이 "몰랐다"고 진술하는 걸 듣고 플레이어가 스스로 떠올려야 하는 몫으로 남겼다.
- **디브리핑 톤 차별화 강화** (`actor-prompt.ts`): Phase 27-Fix에서 도입한 interrogationStrategy(T/F)×pressureTolerance 2×2 톤 버킷으로는 8개 페르소나 각각의 개성이 뭉개진다는 게 이번 리뷰로 구체적으로 드러났다 — 정반대 성향인 박서연(ESTP)과 정민아(ISFP)의 디브리핑이 "심장 멎는 줄 알았다"는 표현까지 겹칠 정도로 비슷했다. `DEBRIEF_TONE_BY_MBTI`로 8개 페르소나 각각에 고유한 화법·예시 문장을 지정하고, 전 페르소나가 수렴해버렸던 상투구("심장이 멎는 줄", "쫄깃했다", "고생했어", "재밌었어" 등) 금지 목록을 추가했다.
- **채점 시스템을 "증거 수집" 카운트 기반으로 재설계** (`scoring.ts`, `game-client-types.ts`, `ResultScreen.tsx`): 기존 기본물증/연계물증/진술증거/붕괴보너스 4개 카테고리 중 연계물증·붕괴보너스가 왜 0점인지 플레이어가 알 도리가 없다는 지적이 반복됐다(신발흙 대조·관리실 진술은 확보 방식이 특수해서 그냥 대화로 언급하는 것만으론 안 잡힌다는 사실이 전혀 안내되지 않았기 때문). "붕괴보너스처럼 미리 알려주면 무너뜨릴 때까지 이것저것 찔러보게 만드는 카테고리는 아예 없애고, 설계해둔 증거를 얼마나 찾았는가 하나로 통합하자"는 사용자 판단에 따라 신발흙 대조·관리실 진술도 다른 증거와 차별 없이 "증거 수집"(전체 17개, 개당 5점) 하나로 합쳤다. 최종 카테고리는 증거 수집 / 동기 파악(배역당 10점) / 심문 효율(최대 20점) 3개로 단순화됐다. 붕괴 트리거로서의 기능적 역할(진범 락아웃 조건 계산)은 evidence.ts의 breakdownCategory 태그로 그대로 유지되며, 채점 로직에서만 특별 취급을 없앴다.
- **채점 기준 사전 공개 + 실시간 타이머** (`CastingScreen.tsx`, `GameApp.tsx`, `scoring.ts` `SCORING_SUMMARY`): "심문 효율의 시간 제한이 게임 중엔 전혀 안내되지 않다가 결과 화면에서야 공개돼 숨겨진 타임어택 같다"는 지적에, 애초에 점수 시스템을 넣은 목적("진범만 잡으면 끝나는 게 아니라는 걸 알려주려는 것") 자체가 사전 공개와 상충하지 않는다고 판단해 사건 브리핑 화면에 서브퀘스트 형태로 채점 기준을 먼저 보여준다. 라운드 화면 헤더에도 조사 시작부터 경과한 시간을 실시간(⏱ MM:SS, 1초마다 갱신)으로 표시한다.
- **다른 팀원의 증언 섹션 UI 분리** (`InvestigationBoard.tsx`): "확보된 카드를 클릭하라"는 안내문이 물증(조사 모드) 섹션에만 있고 진술 증거 섹션엔 클릭 가능하다는 안내 자체가 없어서, 플레이어가 아예 클릭해볼 생각을 못 했다는 지적(리뷰가 실제로 정확히 짚어냄 — 실전 관찰에서도 사용자가 궁금해했던 지점). 진술 증거를 "조사 모드"와 똑같이 독립된 박스+캡션("사건과 관련된 다른 팀원들의 증언입니다. 클릭하여 세부 내용을 볼 수 있습니다.")을 가진 별도 섹션으로 분리했다.
- **진범 락아웃에 경고 단계 추가 — 공정성 결함 수정** (`interrogate/route.ts`, `GameApp.tsx`, `gameSave.ts`): 이번 리뷰에서 발견된 가장 중요한 결함. 무고자 락아웃은 비밀소진 판정 콜이 최소 6턴 이상 대화가 쌓여야 시도되고, 압박이 누적되며 자연히 "동요" 모드를 거친 뒤 잠기는 경우가 많다. 반면 진범의 하드게이트(`computeBreakableHardGate`)는 대화 턴 수와 무관하게 조건(증거 카테고리 2개 이상 + 키워드 매칭)만 맞으면 즉시 잠긴다 — 그래서 "⚠️ 경고 없이 훅 잠기는 캐릭터 = 범인"이라는 메타 추리가 가능해지는, 설계 원칙("진범이든 무고자든 신호가 동일해야 한다")에 어긋나는 결함이었다. 하드게이트를 2단계로 분리했다: 조건이 처음 충족되면 곧바로 잠그지 않고 mode를 "동요"로 강제해 기존 ⚠️ 배지 로직을 그대로 재사용해 경고만 띄우고, 그다음 턴에도 조건이 유지된 채 다시 압박하면 그때 잠근다. "경고받았는지" 상태는 클라이언트가 mode==="동요" 신호로 대칭적으로(무고자든 진범이든 동일하게) 추적해 서버에 `alreadyWarned`로 보낸다 — 서버 응답에 별도의 `warned` 필드를 넣지 않은 이유는, 그 필드 자체가 breakable(진범) 캐릭터에서만 true가 나올 수 있어 네트워크 응답만 봐도 범인을 알 수 있는 새로운 누출 경로가 되기 때문이다. 경고 단계의 실제 대사는 그대로 내보낸다 — "동요" 모드 자체가 자백·알리바이 부인을 금지하는 규칙 아래 생성되므로 경고 턴을 끼워 넣어도 자백이 새어나올 위험은 없다.
- **결과 화면에 재시작 버튼 추가** (`ResultScreen.tsx`, `GameApp.tsx`): 결과 화면까지 도달하면 재도전할 방법이 전혀 없었다는 지적("캐주얼한 게임에서 꽤 치명적인 누락") — 확인해보니 정말 버튼 자체가 없었다. `restartGame()`을 추가해 캐스팅 토큰을 비워 새 캐스팅을 재요청하게 하고 나머지 라운드 상태를 전부 초기화한다. introSeen은 유지해 프롤로그를 다시 보여주지 않는다.
- **검증**: `npm run lint`/`npm run build` 통과(중간에 `ScoreBreakdown` 타입이 `game-client-types.ts`에 중복 정의돼 있던 걸 발견해 함께 맞췄다). 로컬 브라우저에서 CCTV 물증 확보 → 2라운드 진입(진술 증거 자동 확보로 카테고리 A+C 충족) → 이현우에게 "관리실" 키워드로 압박 → 첫 압박에는 ⚠️ 경고만 뜨고 잠기지 않음(대사 정상 생성) → 두 번째 압박에서 🔒 실제 잠금, 이렇게 경고→잠금 2단계가 정확히 작동하는 것을 확인했다. 결과 화면에서도 새 채점 체계(증거 수집 6/17개·30점, 동기 파악 10점, 심문 효율 20점, 60/135)가 정상 노출됐고, 세 캐릭터의 디브리핑이 이제 확연히 다른 어조로 나뉘는 것도 확인했다(이현우=ESFJ, 상대를 챙기는 관계지향 톤 / 박서연=ISTJ, 격식체·분석적 톤 / 정민아=ENFP, 산만하고 말 많은 톤 — 상투구 반복 없음). "다시 도전하기" 버튼도 정상적으로 새 캐스팅 화면으로 이동시키는 것을 확인했다.

## Phase 33: 텍스트 오타/중복 음절 후처리 (맞춤법 교정 콜) (Completed)

Phase 32에서 보류했던 텍스트 생성 품질 문제("증라도", "들렸을 뿐뿐입니다", "침묵하다하다가" 등)에 대해 사용자가 방향을 확정했다: 무료·빠르고 한국어 호환이 되는 대체 모델을 새로 찾기는 현실적으로 어렵다고 판단(항목1)해 모델 교체는 보류하고, 대신 대사를 화면에 보여주기 직전에 맞춤법/문맥 교정 콜을 한 번 더 거치는 후처리 방식을 우선 시도하기로 했다(항목2). 이 방식으로도 해결이 안 되면 그때 후처리 단계만 더 고급 모델로 바꾸는 걸 다음 단계로 남겨뒀다(항목3).

- **`src/lib/text-polish.ts` 신설**: `polishText(client, model, text)` — 오타·중복 음절·명백한 비문만 고치고 의미·어조·존댓말 여부·문장 구조는 절대 바꾸지 않도록 강하게 제한한 시스템 프롬프트로 교정 콜을 한 번 더 보낸다(temperature 0). 원본이 이미 깨끗하면 토씨 하나 안 바꾸고 그대로 반환하도록 명시했고, 콜이 실패해도(타임아웃 등) 예외를 던지지 않고 항상 원문을 그대로 반환한다 — 이 단계 때문에 심문/디브리핑 응답 전체가 실패하는 일은 없다.
- **`nim-client.ts`에 `POLISH_MODEL` 상수 추가**: 지금은 `NIM_MODEL`과 동일한 모델을 재사용하지만, 환경변수(`NVIDIA_POLISH_MODEL`)로 별도 지정할 수 있게 분리해뒀다 — 이 방식으로도 글리치가 안 잡히면(항목3 시나리오) 후처리 단계만 더 고급 모델로 바꿀 때 이 상수만 채우면 되고 text-polish.ts 코드는 손댈 필요가 없다.
- **적용 지점**: `interrogate/route.ts`(잠기지 않은 실제 대사, 경고 턴 포함 — 고정 잠금 문구는 이미 깨끗해서 건너뜀)와 `accuse/route.ts`(3배역 디브리핑 텍스트) 둘 다에 연동했다. 심문 대사만이 아니라 결과 화면 디브리핑도 같은 모델이 생성하는 만큼 동일한 글리치 위험이 있다고 판단했다.
- **검증**: `npm run lint`/`npm run build` 통과. 로컬에서 원문(polish 이전)을 임시 로그로 찍어 최종 응답과 직접 대조하는 방식으로 실제 동작을 확인했다 — 깨끗한 원문은 정확히 그대로 통과되는 것을 여러 사례로 확인했고(예: "...거짓말 아니에요..." 자연스러운 말줄임은 안 건드림), 실제 교정 사례도 포착했다("저는 당황할 이유 없습니다" → "저는 당황할 이유가 없습니다" — 누락된 조사 보정). 지연시간도 캐스팅~심문 왕복 전체가 1.5~1.8초대로 유지되는 것을 확인해 콜 하나 추가로 인한 체감 저하는 크지 않았다(Phase 31 하트비트 스트리밍으로 이미 지연 자체에 대한 안전장치도 갖춰진 상태).

## Phase 34: 스크롤 자동 복귀 버그 수정 (Completed)

다섯 번째 블라인드 플레이(비판적 게임 저널리스트, 4번째와 동일 페르소나) 리뷰 + 사용자가 직접 녹화한 화면 영상 제보를 계기로 발견/수정.

- **증상**: "스크롤을 내려도 자동으로 다시 위로 올라가서 아래쪽 요소를 클릭하기 어렵다"는 사용자 제보. 영상 파일 자체는 도구로 재생할 수 없어(ffmpeg/OpenCV 등 프레임 추출 수단이 이 환경에 없음) 직접 시청하진 못했지만, 코드 검토로 유력한 원인을 먼저 특정한 뒤 브라우저에서 그대로 재현해 확인했다.
- **원인**: `GameApp.tsx`가 `conversations[characterId] ?? []`로 아직 대화가 없는 캐릭터의 메시지 배열을 만들어 `InterrogationChat`에 넘기는데, `?? []`는 렌더마다 새 배열 참조를 만든다. `InterrogationChat`의 자동 스크롤(`useEffect(..., [messages, loading])`)은 이 참조가 바뀔 때마다 재실행돼 `scrollIntoView`를 호출한다. Phase 32에서 추가한 라운드 헤더의 1초 타이머(`nowTick`)가 `GameApp`을 매초 재렌더시키면서, 대화가 아직 없는 캐릭터를 보고 있을 때마다 이 조합이 맞물려 **1초마다 스크롤 위치가 채팅 패널 쪽으로 강제로 되돌아갔다** — 실제로 로컬에서 스크롤을 최상단으로 내린 뒤 3초 기다리자 정확히 이전 위치(360px)로 복귀하는 것을 확인해 재현에 성공했다.
- **수정**: 두 겹으로 고쳤다.
  1. `GameApp.tsx`에 모듈 스코프 상수 `EMPTY_MESSAGES: ChatMessage[] = []`를 두고 `?? []` 대신 이걸 재사용해, 대화가 없을 때도 매 렌더 동일한 참조가 유지되게 했다.
  2. `InterrogationChat.tsx`의 effect 의존성을 `[messages, loading]`(배열 참조)에서 `[messages.length, loading]`(실제 의미 있는 값)으로 바꿨다 — 앞으로 어딘가에서 또 불안정한 배열 참조를 넘기더라도 이 effect 자체가 안전하도록 방어선을 하나 더 뒀다.
- **검증**: `npm run lint`/`npm run build` 통과. 로컬 브라우저에서 (a) 대화 없는 캐릭터 상태로 스크롤 최상단 이동 → 4초 대기 → `scrollY` 그대로 0 유지 확인(수정 전에는 360으로 복귀했음), (b) 실제 메시지를 보내 정상적인 자동 스크롤(새 메시지 도착 시 채팅으로 이동)이 여전히 작동하는지 확인, (c) 메시지 이후 다시 스크롤을 올려 4초 대기해도 이번엔 튕기지 않는 것까지 확인했다.

이번 리뷰에서 함께 나온 다른 지적들(텍스트 후처리가 일부 사례에서 여전히 새는 문제, 결과 화면에 "놓친 증거" 목록 부재, 인내심 배지와 캐릭터의 "동요 지문" 연기가 시각적으로 혼동되는 점)은 사용자가 Phase 35에서 방향을 정리해 반영했다(아래 Phase 35 참고).

## Phase 35: 여섯 번째 리뷰 반영 — 후처리 롤백 + 인내심 게이지 + 증거 카드 전면 재작성 + 레이아웃 (Completed)

Phase 34 리뷰에서 남겨뒀던 3가지 판단 필요 항목에 대해 사용자가 방향을 확정하고, 추가로 증거 카드 정보 구조와 레이아웃까지 손봤다.

- **텍스트 후처리 롤백**: "정민아가 형사를 '형님'이라고 부르는" 것 같은 호칭·존비속 관계 오류는 한 턴만 떼어서 보는 교정 콜로는 못 잡고, 오히려 새로 만들어내는 사례도 있었다는 사용자 관찰. 전체 대화 맥락을 함께 읽는 교정 방식은 지연시간이 크게 늘 것으로 예상돼 비용 대비 효율이 애매하다고 판단, 일단 후처리 자체를 껐다 — `interrogate/route.ts`·`accuse/route.ts`에서 `polishText` 호출을 제거하고 원문(`parsed.text`/모델 원문)을 그대로 쓰도록 되돌렸다. `text-polish.ts`와 `nim-client.ts`의 `POLISH_MODEL`은 나중에 더 나은 모델로 재시도할 수 있도록 코드와 주석을 그대로 남겨뒀다.
- **인내심 표시를 배지 → 5단계 게이지로 교체** (`GameApp.tsx`): "경고 표시가 뜨는 방식보다 용의자 이름 옆에 5단계 게이지로, 오르락내리락도 가능하게" — `computePressureLevel`을 새로 만들어 해당 캐릭터의 대화 전체를 훑어 "동요" 응답마다 +1, "답변"(순순히 응답)마다 -1로 계산하는 연속값(0~4)으로 바꿨다. 실제 잠금(`locked`)이 되면 게이지가 가득 찬 것(5/5)으로 표시된다 — 다만 이 게이지 자체가 잠금을 발생시키는 건 아니고, 실제 잠금 여부는 여전히 서버가 결정론적으로 판단한다(기존 진범 하드게이트/무고자 판정 로직 그대로 유지). 즉 게이지는 "지금 분위기가 얼마나 팽팽한지"를 보여주는 화면 표시용 근사치다.
- **"놓친 증거" 목록**: 사용자 판단 — "이 게임은 사실상 일회성이라, 못 찾은 증거품이 있다는 걸 알면 오히려 한 번 더 플레이하고 싶어지는 유인이 된다"며 지금 방식(결과 화면엔 개수만, 무엇을 놓쳤는지는 안 알려줌)을 그대로 유지하기로 함 — 변경 없음.
- **증거 카드 전면 재작성 — "카드 면은 짧은 headline, 클릭하면 상세"로 통일** (`evidence.ts`): 카드마다 클릭 전 노출 정보량이 들쭉날쭉했던 문제(예: "법인카드 내역"은 결론까지 카드 면에 그대로 노출)를 사용자가 제시한 이상형("사망추정시각": 카드 면 "23:45경 사망" → 클릭하면 부검 소견)에 맞춰 전체 물증 15종을 재작성했다. `ev-performance-review`는 사용자가 준 예시 문구를 그대로 반영("이현우 차장" → "성과는 있으나 팀장 평가에서 최저 평가를 받아 승진 대상에서 제외됨"). 다만 `stmt-lee-past-mistake`(정민아가 아는 "이현우의 과거 실수")에 사용자가 제안한 예시 문구("이현우가 자기 실수를 박서연에게 뒤집어씌웠다")는 그대로 쓰지 않았다 — characters.ts에 이미 정의된 박서연-이현우 갈등의 원인(박서연이 자기 성과를 이현우에게 빼앗겼다고 믿는 "성과 가로채기")과 모순되기 때문에, 구체적 내용을 확정하지 않는 쪽으로 조정했다.
  - **부수 수정(중요)**: 카드 면 문구(`revealedFact`)가 짧아지면서, 이 값을 그대로 액터 프롬프트의 "형사가 실제로 확보한 증거" 화이트리스트에 넘기던 두 지점(`interrogate/route.ts`, `actor-prompt.ts`의 `buildStatementGateSection`)이 부실한 정보만 모델에 전달하게 되는 부작용을 발견해 함께 고쳤다 — 이제 `detail ?? revealedFact`를 써서, 카드 면은 짧아져도 모델이 받는 컨텍스트는 그대로 풍부하게 유지된다.
- **"다른 팀원의 증언" 카드를 물증과 동일하게 확보 방식으로 전환** (`InvestigationBoard.tsx`, `GameApp.tsx`): 진술 증거가 라운드 전환 시 자동으로 확보되던 기존 로직을 없애고, 물증과 똑같이 클릭해야 확보(✓ 표시 + 색상 변경)되도록 통일했다 — "지금은 클릭하는 게 의미가 없다"는 지적에 대응. 카드 제목도 "박서연 다툼 이유"처럼 결론을 미리 알려주던 라벨 대신 "팀원 A의 증언"류 익명 라벨로 바꾸고 실제 내용은 상세보기로 옮겼다. 부수 효과: 붕괴조건 C카테고리(진술)가 더 이상 라운드 도달만으로 자동 충족되지 않고, A(CCTV)·B(신발흙)와 마찬가지로 실제 확보 행동이 필요해졌다 — 세 카테고리의 난이도가 더 대칭적으로 맞춰졌다.
- **넓은 화면에서 조사 모드/다른 팀원의 증언을 나란히 배치** (`GameApp.tsx`, `InvestigationBoard.tsx`): `xl`(1280px) 이상에서는 페이지 최대 너비와 왼쪽 열 너비를 넓혀(`xl:max-w-7xl`, `xl:grid-cols-[640px_1fr]`) 두 섹션이 옆으로 나란히 들어가게 하고, 그보다 좁은 화면(창모드·모바일)에서는 기존처럼 위아래로 쌓이게 했다(`xl:flex-row`).
- **검증**: `npm run lint`/`npm run build` 통과. 로컬 브라우저에서 (a) 1600px 넓은 화면에서 조사 모드/다른 팀원의 증언이 실제로 나란히 배치되는 것을 확인, (b) 진술 증거 카드를 클릭하니 정상적으로 ✓ 표시 + 초록색으로 바뀌고 모달에 제목("팀원 A의 증언")과 상세 내용이 분리되어 뜨는 것을 확인, (c) 인내심 게이지가 압박 질문 이후 0/5 → 1/5로 정상적으로 올라가는 것을 DOM에서 직접 확인, (d) 900px(창모드)·375px(모바일) 두 폭 모두에서 레이아웃이 깨지지 않고 세로로 정상 스택되는 것을 스크린샷으로 확인했다.

## 다음 단계 (Not Started)

- **[보류] 동기 겹침 해결용 사건 제목 도입** (Phase 30 피드백, 항목⑩) — 마케팅팀 세 배역의 동기가 서로 겹쳐 보이는 문제를, 캐스팅/결과 화면에 "승진을 둘러싼 마케팅 팀의 추악한 민낯" 같은 압축적인 사건 제목을 붙여 완화하자는 아이디어. 사용자가 "지금 단계에서 손 볼 건 아니다"라고 명시적으로 보류함 — 착수 시점 미정.
- **[보류] 텍스트 오타/중복 문제 — 더 나은 모델로 재시도** (Phase 35) — 한 턴 단위 후처리 콜은 롤백했다. 다음 시도 후보는 더 나은(유료) 모델을 후처리 전용으로 붙이는 것 — `nim-client.ts`의 `POLISH_MODEL`과 `text-polish.ts`를 그대로 남겨뒀으니, 재시도할 때 이 두 파일에 다시 연결하면 된다. 착수 시점은 사용자 판단.

## Phase 36: 증거 자동확보 버그 + 휴대폰 증거 모순 + 다크모드 고정 + 디브리핑 환각 수정 (Completed)

여섯 번째 리뷰 스크린샷을 직접 검토하며 사용자가 발견한 4가지 문제를 반영했다.

- **요청으로 해금된 증거가 클릭 없이 자동 확보되던 버그**: 심문 중 "신발/휴대폰 보여달라"고 요청해 `round-review`가 해금한 물증이, 다음 라운드 조사 모드에 뜰 때 이미 ✓ 확보(초록색) 상태로 나타났다 — Phase 35에서 진술 증거는 클릭이 필요하게 고쳤지만, `action_triggered` 물증과 임의 물품(ad-hoc) 카드는 여전히 `round-review` 응답을 받자마자 `collectedEvidenceIds`에 바로 넣고 있었다. `GameApp.tsx`에 `unlockedActionIds`(가시성 전용, 확보 여부와 분리)를 신설해, 요청은 "조사 모드에 카드가 나타나게"만 하고 실제 확보(✓, 채점 반영)는 다른 카드와 동일하게 클릭해야만 되도록 통일했다.
- **박서연 "휴대폰" 요청이 사전 등록 물증과 모순되던 버그**: 심문 중 박서연에게 휴대폰을 보여달라고 하면, "휴대폰"이 `requestableItems`에 등록돼 있지 않아 `round-review`가 임의 물품으로 처리해 "사건과 무관" 카드를 만들었다. 그런데 3라운드가 되면 사전 등록된 `ev-park-phone-photos`("수상한 사진 발견")가 자동으로 뜨면서 정반대 내용이 동시에 존재하는 모순이 생겼다(사용자가 스크린샷으로 직접 포착). `characters.ts`에 박서연 "휴대폰" 항목을 `ev-park-phone-photos`로 정식 등록하고, `round-review/route.ts`는 등록된 물품이 매칭되면 그 물증의 `revealTiming`을 확인해 `action_triggered`가 아닌(=라운드 게이트) 물증은 즉시 unlock하지 않고 그냥 임의 물품 생성만 막도록 고쳤다 — 그 물증은 자기 라운드가 되면 원래대로 조사 모드에 나타난다.
- **모바일 라이트 모드에서 가독성이 깨지는 문제**: 이 게임은 라이트 테마를 지원한 적이 없는데도 `globals.css`의 배경/전경색이 `@media (prefers-color-scheme: dark)`에 기대고 있었다. 안드로이드 Chrome 등 일부 모바일 브라우저는 `color-scheme` 힌트가 없는 페이지를 자체적으로 다시 다크모드 보정하려 드는데, 이미 다크로 하드코딩된 UI에 이 보정이 겹치면 오히려 가독성이 깨진다. `:root`에 `color-scheme: dark`를 고정하고, `@media` 분기 자체를 없애 항상 다크 색상만 쓰도록 했다. `layout.tsx`에도 `viewport.colorScheme = "dark"`를 명시해 이중으로 방어했다.
- **심문 없이 지목했을 때 디브리핑이 없었던 대화를 지어내던 문제**: 스크린샷 확인 결과, 한 번도 심문하지 않은 캐릭터의 디브리핑에서 "아까 CCTV 얘기 던지실 때", "휴대폰 사진 나왔다고 할 때"처럼 실제로 없었던 질문·순간을 구체적으로 지어내고 있었다 — 기존에도 "실제 대화 기록에 없는 내용을 지어내지 않는다"는 금지 문구는 있었지만, "형사의 질문/전략을 구체적으로 언급하라"는 내용 지침과 충돌할 때 모델이 후자를 우선시한 것으로 보인다. `buildDebriefSystemPrompt`/`buildDebriefDirective`에 `wasInterrogated` 플래그를 추가해, 대화 기록이 비어있으면 아예 다른 분기로 전환한다 — "질문을 한 번도 안 받아봤다"는 사실 자체에 대한 반응(안도·서운함·허탈함 등, 성향에 맞게)을 요구하고, 가짜 대화 예시 대신 명시적으로 "지어내지 말라"고 지시한다. 실전 검증에서 이현우(ESTP)는 "심문도 안 당하고 끝내는 거 너무한 거 아니냐" 하며 서운해했고, 정민아(ENTJ)는 "질문도 안 하니까 좀 허탈하네... 다음엔 내가 네 정체까지 다 파내줄 테니까 각오해라"처럼 도발적으로 반응해, 페르소나별 반응도 자연스럽게 갈렸다.
- **검증**: `npm run lint`/`npm run build` 통과. 로컬 브라우저에서 (a) 시스템 라이트 모드로 강제 설정한 채로도 페이지가 항상 다크로 렌더링되는 것 확인, (b) 박서연에게 휴대폰을 요청한 뒤 2라운드 진입 — 조사 모드에 모순되는 임의 카드가 생기지 않는 것 확인, (c) 3라운드까지 진행해 `ev-park-phone-photos` 카드가 미확보(회색, "클릭해서 확보하기") 상태로 정상 등장하는 것 확인, (d) 박서연에게만 메시지 하나를 보내고 이현우·정민아는 전혀 심문하지 않은 채 최종 지목 — 두 캐릭터 모두 없었던 대화를 지어내지 않고 "심문받지 않았다"는 사실 자체에 성향에 맞게 반응하는 것을 확인했다.

## Phase 37: 증거 내용 보강 + 심문 없는 증거 클릭 채점 방지 (Completed)

Phase 36 배포 후 사용자가 결과를 다시 검토하며 4가지를 추가로 요청했다.

- **박서연 휴대폰 사진첩을 요청 기반 해금으로 전환**: Phase 36에서 "박서연 휴대폰" 요청을 정식 등록해 모순은 없앴지만, `ev-park-phone-photos`의 `revealTiming`이 여전히 `round3_open`이라 요청 여부와 무관하게 3라운드가 되면 그냥 해금됐다. 신발과 동일하게 `revealTiming: "action_triggered"`로 바꿔, 실제로 휴대폰을 보여달라고 요청했을 때만(그 다음 라운드부터) 조사 모드에 나타나도록 고쳤다.
- **동기 관련 증거 내용 구체화**: "통화/삭제문자 복구"가 "인사평가와 관련된 통화"라고만 되어 있어 이현우의 동기와 연결되지 않는다는 지적 — 이현우의 인사평가에 대한 통화였다고 명시했다. "다른 팀원의 증언"(성과 문제) 카드도 "성과를 둘러싼 갈등"이라는 두루뭉술한 표현 대신, 김영훈이 박서연의 프로젝트 성과를 자기 것처럼 보고했다는 가로채기 구도를 명확히 드러내도록 detail을 구체화했다(2라운드 공개 시점은 기존과 동일).
- **심문 없이 증거만 클릭해도 점수가 다 채워지던 문제**: 가장 중요한 수정. 사용자가 실제로 재현했다 — 심문을 한 번도 안 하고 조사 모드의 모든 증거를 클릭한 뒤 오답을 지목해도 100/135(A등급)이 나왔다. 원인은 대부분의 물증·진술 증거가 라운드만 되면 자동으로 "조사 모드에 뜨고, 클릭 한 번이면 확보"되는 구조였기 때문 — 실제 심문(핵심 게임플레이)이 전혀 필요 없었다. `GameApp.tsx`에 `interrogatedRounds`(실제로 질문을 보낸 라운드 번호 집합, 전송 시점에 즉시 기록)를 추가하고, `InvestigationBoard`에서 그 라운드에 아직 아무도 심문하지 않았다면 새 증거를 확보(클릭)할 수 없게 막았다(🔒 "이 라운드에서 먼저 심문을 진행하세요"). `action_triggered` 물증(신발·휴대폰)은 요청 자체가 이미 심문 행위이므로 이 게이트에서 제외했다.
- **검증**: `npm run lint`/`npm run build` 통과. 로컬 브라우저에서 1라운드에 메시지를 하나도 안 보낸 상태로 물증 카드 4개가 전부 🔒 "이 라운드에서 먼저 심문을 진행하세요"로 잠겨있는 것을 확인했고, 박서연에게 메시지 하나를 보내자(응답이 오기 전, 전송 즉시) 같은 라운드의 카드들이 바로 잠금 해제되는 것까지 확인했다.

- **[설계 메모] "라운드별 정보 게이트" — 캐릭터 본인의 인지 자체를 라운드로 제한하는 방안** (Phase 28 피드백에서 제안, 사용자가 "버전2로 별도 기획해서 현재 버전과 병행 비교해보고 싶다"고 명시. 지금은 구현하지 않고 기록만 남긴다.)
  - **문제의식**: 실전 플레이에서 "용의자들이 너무 술술 분다"는 지적이 나왔다. 다만 확인해보니 1라운드에 확인된 정보(정민아-김영훈의 과거 연애, 박서연의 성과 갈등)는 실제로는 캐스팅 화면(`motivePublic`)에서 게임 시작 전부터 이미 공개된 떡밥이었고, 물증 게이트로 보호되는 더 깊은 세부 사실(휴대폰 사진, 악연 사연, 관리실 방문 이유 등)은 이번에도 새지 않았다 — 그래서 "정보가 샜다"기보다는 "이미 공개된 동기라도 첫 질문에 너무 순순히 인정해버려서 긴장감이 없다"는 게 정확한 문제 진단에 가깝다.
  - **제안된 해결 방향**: 물증(evidence.ts의 `revealTiming`)처럼, 캐릭터 본인이 "자기 동기/비밀을 인지하고 인정할 수 있는 시점" 자체를 라운드별로 게이트한다. 예: 정민아를 연기하는 AI가 1라운드에는 "김영훈과 사귀었었다"는 사실 자체를 부인하도록 하고, 2라운드부터 그 게이트가 열려야 인정 가능하게. 박서연의 "성과 가로채임"도 동일하게 2라운드부터 인정 가능하도록.
  - **구조적으로 걸리는 지점(구현 전 반드시 먼저 풀어야 함)**:
    1. 지금은 `motivePublic`이 캐스팅 화면(라운드 0, 게임 시작 전)에서부터 각 배역의 동기를 대놓고 보여준다. AI 본인이 2라운드까지 그 사실을 "모르는 척" 부인하면, 게임이 이미 플레이어에게 준 정보와 캐릭터의 반응이 정면으로 모순돼 버그처럼 보일 위험이 크다 — `motivePublic` 자체를 없애거나 늦게 공개하는 것부터 다시 설계해야 한다.
    2. 라운드마다 어떤 캐릭터에게 정확히 어떤 사실을 "새로 인지"시킬지 3명 분량을 전부 새로 설계해야 한다(지금의 `statementEvidence`/`requiredEvidenceIds` 게이트와는 별개로, motive/knownSecrets 레벨까지 라운드 축으로 확장하는 것이므로 데이터 모델도 손대야 함).
    3. `buildActorSystemPrompt`가 현재 라운드를 안 받고 있어서(라운드 정보를 몰라도 되게 설계돼 있었음), 이 방향으로 가면 프롬프트 빌더 자체가 라운드를 인자로 받아 "이번 라운드에 이 캐릭터가 알아도 되는 사실 집합"을 매번 다시 계산해야 한다 — 지금 아키텍처보다 한 단계 복잡해진다.
  - **가벼운 대안(참고용)**: 정보 자체를 숨기지 않고, "이미 공개된 동기라도 한두 번은 버티다가 인정하게" 만드는 쪽이 훨씬 작은 변경으로 같은 체감 효과(너무 쉽게 안 분다)를 낼 수 있다 — 예를 들면 인터로게이션 프롬프트에 "공개된 동기라도 처음 질문엔 얼버무리고, 두세 번 압박받아야 인정한다"는 지침을 추가하는 정도. 사용자가 "버전2"로 라운드 게이트 자체를 실험해보고 싶다고 했으므로, 이 가벼운 대안은 정식 착수 전 비교군으로 같이 고려해볼 만하다.
  - **사용자의 의도**: 이 방식을 별도 배포(가칭 "버전2")로 구현해서 지금의 라운드-리뷰 배치 방식과 나란히 비교해보고 어느 쪽이 더 나은지 판단하고 싶어함 — 아직 착수 시점은 정하지 않음.


- **붕괴 보너스 조건 안내 부재** (Phase 24 AI 플레이테스트에서 발견) — 심문 중 실제로 진범을 무너뜨려도(락 발생) 붕괴 보너스는 신발 물증+관리실 진술 카드를 둘 다 확보해야만 주어지는데, 이 조건이 플레이어에게 전혀 안내되지 않아 "왜 보너스가 없지?" 하는 혼란을 줄 수 있음. 해결 방향(조건 안내 UI 추가/보너스 계산 방식 변경 등)은 아직 정하지 않음 — 사용자 판단 필요.
- **Cloudflare 안정성 며칠 더 관찰** — 지금까지의 15회+ 테스트는 짧은 시간 창에서의 결과다. 실제 여러 날에 걸쳐 계속 빠른지 지켜볼 필요 있음.
- (경미) Cloudflare Workers Builds에서 `cloudflare-pages-experiment` 브랜치는 이제 쓰지 않으므로 정리(브랜치 삭제 등)가 필요하면 사용자 확인 후 진행할 것 — 삭제는 되돌리기 어려운 작업이라 먼저 묻기로 함.

- **전략가 계층 부활 검토** (Phase 17 참고) — 사용자가 제시한 "친구 대리 AI" 비전대로, 속도 제약이 풀리면(유료 플랜/NAN 자체 서버 등) 별도 전략 콜을 되살리는 것을 검토할 것. 단순 복원이 아니라 "플레이어와 함께하는 AI가 여러 캐릭터의 전략을 동시에 관장"하는 방향으로 확장할 가치가 있다고 사용자가 판단함.
- **응답 속도 변동성 대응** (Phase 18 이후 최우선 후보로 격상): Node.js 복귀로 기능은 살아났으나 47~93초대 변동은 여전하다. 재시도 로직, "생각하는 중이 오래 걸리네요" 안내 UI, 또는 유료 플랜/NAN 자체 서버로의 인프라 교체가 후보. 사용자가 최종 우선순위를 결정할 것.
- **복합 문장 이해도** 표본 확충 — 1건만 테스트했고 문제 재현이 안 됐다. API가 안정화된 뒤 여러 건 더 테스트해서 확신도를 높일 필요 있음.
- stmt-motive-disclosure(각자의 동기 자진공개/은폐) 선행 물증 게이트 — 사용자가 실전 테스트하며 캐릭터별 세부 조건을 직접 채울 예정
- (경미) 라운드 상태가 URL/브라우저 히스토리에 기록되지 않아 뒤로가기 시 라운드 롤백은 불가능(의도대로 안전)하지만, 대신 게임 페이지 자체를 이탈해 진행 상황이 전부 소실된다 — 이어하기(Phase 13) 도입으로 소실 자체의 타격은 줄었지만, 이탈 확인 팝업은 여전히 고려 여지
- (경미) 무고자 판정 콜 자체도 구조화 출력에 의존하므로 이론상 완전히 면역은 아니다 — 다만 롤플레이 대사와 분리된 단순 분류 작업이라 실전 재검증에서는 누락이 재현되지 않았다. 표본이 늘어나면 재관찰 필요.

## Phase 38: "증거 수집" 채점을 심문으로 요구한 유의미한 증거만으로 한정 (Completed)

Phase 37에서 배포한 "이번 라운드에 최소 1회 심문해야 증거 확보 가능" 게이트에 대해 사용자가 방향 자체를 재수정했다.

- **Phase 37 게이트가 실효성이 없다는 지적**: "지금은 AI와 대화를 한번이라도 하면 증거들이 해금 되는 상황이야. 그런데 이건 의미가 없잖아 — 그냥 대화하는 것 자체가 증거 해금의 조건인데 그럴 이유도 의미도 없어." 아무 메시지나 한 번 보내면 그 라운드의 모든 카드가 풀리므로, 실제 조사 품질과 무관하게 우회 가능한 게이트였다는 지적. "죽은 코드는 즉시 제거" 원칙에 따라 `interrogatedRounds` 메커니즘(상태·저장·전파·게이팅 로직 전부)을 `GameApp.tsx`/`InvestigationBoard.tsx`/`gameSave.ts`에서 완전히 삭제했다(비활성화가 아니라 삭제).
- **"증거 수집" 채점을 provenance(어떻게 확보했는가) 기준으로 재정의**: 사용자의 대안 — 라운드가 되면 자동으로 조사 모드에 뜨는 물증·진술은 애초에 클릭 한 번으로 항상 확보 가능하므로 점수를 주는 의미가 없고, 심문으로 직접 요구해서 찾아낸(`action_triggered`) 증거 중에서도 "사건 해결에 실제로 의미 있는 것"만 채점해야 한다고 명시(예시: 정민아 휴대폰=의미없음, 이현우 신발=의미있음). `evidence.ts`의 `EvidenceItem`에 `scorable?: boolean` 필드를 추가해, `ev-shoe-soil-match`(진범을 특정하는 결정적 물증)에만 `true`를 붙이고 나머지 `action_triggered` 함정 요청(박서연/정민아 신발, 박서연 휴대폰 — 전부 "사건과 무관"으로 판명)은 명시적으로 비워뒀다. `scoring.ts`의 `SCORING_EVIDENCE`를 "전체 증거에서 비채점 목록을 빼는" 방식(`NON_SCORING_EVIDENCE_IDS` 차감 방식, 삭제됨)에서 "`scorable: true`인 것만 모으는" 화이트리스트 방식으로 뒤집었다.
- **검증**: `npm run lint`/`npm run build` 통과. 로컬 브라우저에서 (a) CastingScreen의 "채점 기준" 공개 문구가 `SCORING_SUMMARY.evidenceTotalCount` 변경에 맞춰 "전체 1개, 개당 5점"으로 자동 갱신되는 것 확인, (b) 심문을 한 번도 하지 않고 조사 모드 카드만 전부 클릭한 뒤 오답을 지목했을 때 결과 화면의 "증거 수집"이 정확히 "0/1개 · 0점"으로 표시되는 것을 확인 — 요청한 수정은 의도대로 동작한다.
- **[중요/미해결] 잔여 문제 — 위 검증 중 발견, 사용자에게 보고 후 이번 배포 범위에서는 제외하기로 함**: 같은 재현 시나리오(심문 0회, 오답 지목)를 끝까지 돌려보니 총점은 여전히 50/55(S등급, 91%)로 나왔다. "증거 수집"은 0점으로 정상화됐지만, `MOTIVE_EVIDENCE_IDS`(동기 파악, 30점 만점) 3개 항목이 전부 라운드 게이트 방식 물증·진술이라 이번 수정과 똑같은 이유로("라운드만 지나면 클릭 한 번") 만점이 나오고, 심문 효율(20점 만점)도 시간 기준이라 심문을 아예 안 하면 가장 빨리 끝나 오히려 만점이 된다. 즉 오답 + 무심문 조합이 여전히 S등급을 받는 근본 문제는 남아있다. 사용자에게 "동기 파악/심문 효율도 지금 같이 손볼지" 물었고, "일단 증거 수집만 배포하고 나머지는 나중에 본다"는 답을 받아 이번 Phase 범위에서는 제외 — 착수 시점 미정, 다음 리뷰 때 재검토 필요.

## Phase 39: 스토리라인 대규모 개편 — 붕괴조건 시스템 폐지, 인내심 시스템으로 통일 + 살해도구·동기 전면 재설계 (Completed)

사용자가 "작은 패치가 아니라 구조 변경"이라며 문서 형태의 상세 요청서를 전달했다. 착수 전 "영향 범위(castingToken 구조, actor-prompt.ts, 관련 API 라우트, 채점 로직)부터 먼저 훑어보고 계획을 세운 뒤 진행"하라는 명시적 지시가 있어, 코드베이스 전체(데이터 레이어 3파일, 프롬프트 빌더, API 라우트 3개, 클라이언트 상태/UI, 채점 로직)를 직접 읽고 계획 모드로 설계안을 작성해 승인받은 뒤 구현했다.

- **붕괴조건 시스템(A/B/C 증거 카테고리, 진범 하드게이트, 무고자 전용 LLM 락아웃 판정 콜, `[모드]`/`[내부판정]` 구조화 출력) 전체 폐지 → 3인 공통 "인내심" 시스템으로 통일**: Phase 9~32에 걸쳐 여러 번 재검증하며 다듬어온 핵심 로직이었지만, 사용자가 "심문 중엔 아무도 안 무너지고 최종 지목은 순수 정황 추리로 하는 게임"으로 장르를 바꾸기로 결정했다. 신규 모듈 `src/lib/patience.ts`에 순수 함수 `computePatienceLevel`을 구현했다 — (1) 형사의 메시지가 그 캐릭터의 `patienceKeywords`(신설, characters.ts) 중 하나라도 포함하면 +1, (2) 이전에 보낸 것과 비슷한 질문(정규화 후 2-gram Jaccard 유사도 ≥0.6)을 이번 걸 포함해 정확히 3번째로 반복하면 +1. 감소(decay) 없이 게임 전체 대화 기록을 매 턴 처음부터 리플레이해 누적 계산한다(서버 무상태 유지, 클라이언트가 매번 전체 history를 보내는 기존 구조 그대로). 최대 5에서 잠긴다.
  - **완전히 결정론적, LLM은 판정에 관여하지 않는다**(사용자 명시): `interrogate/route.ts`가 LLM을 부르기 **전에** `computePatienceLevel`로 인내심을 계산해, 최대치 도달 시 LLM 호출 자체를 생략하고 고정 문구(`INTERROGATION_LOCKED_TEXT`)를 즉시 반환한다. 그 미만이면 계산된 수치를 프롬프트에 주입해 그 톤으로만 연기하게 한다 — 모델은 언제 잠글지 스스로 판단하지 않는다.
  - `actor-prompt.ts`에서 진범/무고자 이원 구조(`buildBreakdownSection`/`buildUnbreakableSection`)를 `buildPatienceSection` 하나로 통합하고, 무고자 전용 락아웃 판정 콜(`buildLockoutJudgeSystemPrompt`/`parseLockoutJudgeResponse`)과 브라켓 파싱(`MODE_RE`/`JUDGMENT_RE`/`ACTION_RE`/`normalizeMode`)을 전부 삭제했다. 모델은 이제 브라켓 없이 순수 대사만 반환한다 — `parseActorResponse`는 방어적으로 남은 대괄호 블록만 제거하는 수준으로 대폭 단순해졌다.
  - `types.ts`/`characters.ts`에서 `alibiStatus`/`breakdownTrigger`/`breakdownTriggerKeywords` 필드를 전부 삭제(죽은 코드 즉시 삭제 원칙 적용)하고 `patienceKeywords`로 대체했다. `GameApp.tsx`의 `warnedCharacters`(2단계 경고)/`alreadyWarned` 상태도 함께 삭제 — 게이지 숫자 자체가 경고 역할을 하므로 별도 상태가 불필요해졌다. 인내심 게이지 UI(Phase 35, 5분할)는 그대로 재사용하되, 데이터 출처를 "모델의 mode 이력을 스캔해 역산"에서 "서버가 매 응답마다 내려주는 결정론적 수치를 그대로 저장"으로 바꿨다.
- **사건 스토리 전면 재설계**(사용자 상세 지시): 관리실/CCTV 조작 서브플롯 삭제(CCTV 공백은 이제 "다들 서로 못 봤다"는 3인 공통의 평범한 사실로 격하) — 관련 증거 `stmt-lee-office-visit`, breakdownCategory/isBreakdownTrigger 필드 전부 삭제.
  - **살해도구 교체**: 돌 → 흉기(등산용 칼) + 베란다 끈 트릭. 신발 물증(`ev-shoe-*`, 현장 흙 감식 `ev-soil-analysis`)을 전부 삭제하고, 3인 공통 "가방 확인" 요청 메커니즘으로 통일했다(`requestableItems` 데이터만 교체, `buildItemComplianceSection`/`buildItemRequestReviewPrompt`/round-review 인프라는 그대로 재사용). 박서연 휴대폰 서브플롯도 함께 정리했다 — 정민아의 새 동기(파혼·유산)와 톤이 부딪히는 곁가지라 걷어냈다.
  - **동기 재설계**: 박서연은 기존 설정(성과 가로채기) 유지, 이현우는 여동생-김영훈 결혼/사망(자살 처리) 정황 기반 원한으로, 정민아는 김영훈과의 파혼·유산 서브플롯으로 완전히 새로 썼다 — 이현우의 동기는 "본인의 주관적 확신"으로만 남기고 게임이 진위를 판정하지 않는다(박서연-이현우 갈등과 동일한 기존 패턴). 정민아 쪽은 구체적 장면 묘사 없이 "그런 일이 있었다" 수준으로만 서술했다(사용자 명시).
  - **결정적 연결고리(방 배정 → 위치 관계 → 베란다 끈 트릭)는 어떤 카드·대사에도 직접 설명하거나 힌트를 넣지 않았다**(사용자 "절대 하지 말 것") — 신규 물증(`ev-room-assignment` 숙소 배정표, `ev-balcony-strap-marks` 베란다 흔적, `ev-murder-weapon-knife` 흉기)은 전부 사실만 서술하고 해석은 하지 않는다. 이현우가 방 배정을 기획했다는 사실(`stmt-lee-workshop-planner`)도 숙소 배정표와 시기를 떨어뜨려 배치해, 두 정보를 플레이어가 스스로 연결해야만 의미가 생기게 했다.
  - **타임라인 전체를 2시간 앞당겼다**(사망 23:45→21:45, 회식 18:00 시작·4시간 이상 이어짐) — "회식이 너무 늦은 시간까지 이어지는 게 부자연스럽다"는 지적 + "정민아가 취한 박서연을 방까지 데려다줬다"는 새 설정이 성립하려면 회식이 일찍 시작해야 한다는 요구를 함께 반영했다. 기존 교차 목격 진술(W1~W5) 중 알리바이를 상호 보강해주던 W3~W5는 삭제했다 — 새 설계에서는 세 용의자 모두 사망 시각 전후로 완전히 혼자였어야(누구도 물리적으로 완전히 결백함이 증명되지 않아야) 하므로, 서로를 목격했다는 진술 자체가 설계와 모순되기 때문이다.
- **채점 로직**: 카테고리 구조(증거 수집/동기 파악/심문 효율) 자체는 손대지 않았다 — Phase 32부터 "붕괴 보너스"란 이름의 별도 채점 항목이 이미 존재하지 않았으므로(붕괴조건은 interrogate route의 락아웃 게이트로만 쓰였을 뿐 점수에 관여한 적이 없었다), 삭제할 게 아니라 `SCORING_EVIDENCE`(scorable:true 대상을 옛 `ev-shoe-soil-match`에서 이현우 가방 조사 결과 `ev-bag-lee`로 교체)와 `MOTIVE_EVIDENCE_IDS`(3인 새 동기 공개 증거로 재매핑)만 새 evidence id로 다시 연결했다.
- **결과 화면**: 심문 중 아무도 무너지지 않는 새 설계라, 정답/오답과 무관하게 항상 노출되는 "사건의 결정적 연결고리" 콜아웃 섹션을 `ResultScreen.tsx`에 새로 추가했다 — 트릭을 못 찾은 플레이어도 "돌아보니 다 맞아떨어졌다"고 느끼도록(사용자 의도). 기존 이현우 개인 디브리핑의 "숙소 위치를 캐물었는지" 인게임 반응(Phase 30, `buildDebriefRoomLayoutSection`)은 메커니즘을 그대로 재사용하고 문구만 칼/베란다 트릭으로 갱신했다 — 이건 게임 마스터 시점 객관적 해설과는 별개로 캐릭터 본인의 주관적 반응이라 둘 다 유지했다.
- **검증**: `npm run lint`/`npm run build` 통과. 로컬 브라우저에서 전체 게임 루프를 한 번 완주하며 확인했다 — (a) 사건 브리핑 화면에 사망 추정 시각 21:45, 이현우의 새 동기 문구("개인적인 가족사로 예민한 상태였다는 정황")가 정상 표시됨. (b) 박서연에게 `patienceKeywords`("가로채") 포함 질문을 반복 전송하며 인내심이 1→2→3→4→5로 결정론적으로 오르는 것을 서버 로그로 직접 확인했고(`patienceLevel=N/5`), 정확히 같은 질문을 3번째로 반복한 시점에 최대치(5)에 도달하며 서버 로그에 `(LLM 콜 생략)`이 찍히는 것 — LLM을 호출하지 않고 고정 문구로 즉시 잠기는 것을 확인했다. 대화 내용에는 자백·붕괴성 발언이 전혀 없이 끝까지 방어적으로 부인만 하는 것도 확인. (c) 이현우에게 "가방 좀 보여주시겠어요?"라고 요청하자 등산장비(로프·카라비너 등)를 트릭에 대한 어떤 암시도 없이 자연스럽게 보여주는 것, 다음 라운드 전환 화면에 "이현우 가방 확인"이 새 증거로 뜨는 것을 확인. (d) 3라운드까지 진행하며 숙소 배정표·베란다 흔적·흉기(칼)·5종 진술 카드가 각 라운드에 맞춰 정상 노출되는 것을 확인. (e) 이현우를 지목해 "정답입니다" 결과를 받았고, 결과 화면에 "사건의 결정적 연결고리" 콜아웃이 칼/베란다 트릭 설명과 함께 항상 노출되는 것, 점수 항목(증거 수집/동기 파악/심문 효율)이 새 evidence id 기준으로 정상 계산되는 것, 3인 전원의 디브리핑이 새 동기(가족사/파혼·유산) 내용으로 정상 생성되는 것을 확인했다.

## Phase 40: 통화/문자 타임라인 + 신발 증거 복원 (Completed)

Phase 39 배포 후 사용자가 외부에서 작성해둔 추가 스토리 문서 2건(통화·문자 타임라인 + 신발 증거 복원, 최종 확정본)을 전달하며 "이것까지 추가하고 07번 스토리 바이블 문서에도 적용, 스토리라인 상 모순이 있는지도 확인"을 요청했다.

- **발견한 모순 4가지와 해소 방법** (구현 전 문서 대조로 확인):
  1. **회식 시작 시각**: Phase 39는 18:00(4시간 이상 이어짐 — "박서연이 회식 중 일찍 취해서 정민아가 데려다줬다"는 설정 때문에 긴 회식이 필요했다). 이번 자료는 19:00이고, 그 사건 자체를 밤 늦게(00:15경, 정민아가 편의점 근처에서 우연히 만취한 박서연과 마주쳐 데려다주는 것)로 옮겨서 더 이상 이른 저녁에 취할 필요가 없어졌다. 이번 자료가 "확정"이라고 명시돼 있어 19:00으로 되돌렸다.
  2. **이현우의 20:00 "본사 인사평가 통화"**: Phase 39 후반부에 동기를 가족사 원한으로 바꾸면서도 이 통화 설정(옛 승진-압박 동기의 잔재)을 못 지운 채로 남아있었다 — 이번 자료의 21:10 "김영훈을 산책로로 불러내는 전화" + 살해 후 21:50 "다음에 얘기하자" 위장 문자로 완전히 대체했다. 사건에 직접 기여하는 통화로 바뀌어 오히려 더 낫다.
  3. **박서연의 21:30 부재중 전화(사망 15분 전)와 23:00 편의점 외출 사이의 시간 간극**: 사용자가 원본 문서에서 "의도된 오답 유도"라고 명시했다 — "편의점 다녀오다 김영훈을 목격했을 것"이라는 초기 의심을 플레이어가 타임라인을 직접 대조해보면 스스로 기각할 수 있게 하려는 장치. 다리를 놓지 않고 그대로 뒀다.
  4. **정민아가 00:15경 취한 박서연을 만나는 경위**(그녀도 편의점에 갔었는지, 우연히 마주친 것인지)는 원본 문서에서 "디테일은 맡기겠다"고 위임됐다 — 새 사실을 지어내지 않기 위해 "편의점 근처에서 우연히 마주쳐"로 최소한으로만 적었다.
- **신발 증거 복원**: Phase 39에서 신발 메커니즘을 삭제한 게 실수였다는 지적을 받아, 완전히 새 내용으로 복원했다 — `ev-shoe-lee`(이현우 전용, 흙 성분 일치 + 이슬/습기 흔적으로 "밤에 갔었다"만 확정하고 "죽였다"까지는 확정하지 않는 결정적 정황, `scorable: true`), `ev-shoe-park`(산책로 흔적 없음 — 발 씻는 습관 때문이라는 과잉 해명형 red herring), `ev-shoe-jeong`(평범한 흙, 막다른 길). 이현우의 2단계 방어(처음엔 "낮 프로그램 때 묻었다" 부인 → 흙+이슬 증거를 함께 들이밀면 한 단계만 후퇴해 "밤에 잠깐 나가긴 했는데 초입까지만, 김영훈은 못 만났다"로 고정 — 그 이상은 인내심이 최대여도 절대 안 무너짐)는 characters.ts의 knownSecrets 서술만으로 구현했다(actor-prompt.ts 구조 변경 없음) — 실전 검증에서 모델이 정확히 이 2단계 반응을 자연스럽게 연기하는 것을 확인했다.
- **김영훈 휴대폰 통화·문자 내역 신설**(`ev-victim-phone-records`, 2R 종료): 21:05 박서연 문자, 21:10 이현우 전화(불러냄), 21:30 박서연 전화(부재중), 21:50 이현우 문자(위장) 4건을 한 카드에 묶어 공개 — 박서연·이현우 둘 다에게 사망 직전 접촉 흔적을 남겨 초반엔 둘 다 의심하게 만드는 장치.
- **정민아 "법인카드 비리 목격" 서브플롯 완전 삭제**: `ev-corporate-card` 물증을 삭제하고, 그날 밤 심란해 보인 이유를 "김영훈과의 로비 다툼"(다른 팀원 목격, `stmt-jeong-lobby-dispute`) 하나로 통합했다. 박서연의 편의점 영수증(`ev-convenience-store-receipt`)도 결제 시각을 21:05→23시경으로 옮겨 새 타임라인과 맞췄다.
- **검증**: `npm run lint`/`npm run build` 통과. 로컬 브라우저에서 (a) 채점 기준이 "전체 2개"(가방+신발 결정적 물증 2종)로 자동 갱신되는 것 확인. (b) 1R에 "낮 산책로 프로그램 기록" 카드가 뜨는 것 확인. (c) 이현우에게 "신발 좀 보여주시겠어요?"라고 요청 → "낮 산책로 프로그램 때 묻은 거예요" 부인 → "흙 성분 일치 + 이슬 흔적"을 짚어 재질문 → "사실 밤에 잠깐 나가긴 했는데 초입까지만, 김영훈은 못 만났다"로 정확히 한 단계만 후퇴하는 2단계 방어를 실제로 확인. (d) 2R 전환 시 "편의점 영수증"(박서연)과 "팀원 E의 증언"(정민아-김영훈 로비 다툼)이 뜨고, "법인카드 내역" 카드는 더 이상 존재하지 않는 것을 확인.

## Phase 41: 계획적 살인 재설계 + 비/CCTV 삭제 + 신발 습관 단순화 (Completed)

Phase 40 배포 후 사용자가 "시간 간극을 다시 조정해야 할 것 같다"며 전체 타임라인을 요청했고, 직접 재구성한 타임라인을 다시 보내왔다. 반영 전, 시간대별 표로 정리해 사용자에게 다시 보여주며 "몸싸움 없이 살해함"이라는 새 설정이 기존 "우발적 살인" 전제와 충돌한다는 점을 짚어 확인을 구했다.

- **계획적 살인으로 확정**: 사용자가 "계획적 살인으로 하고 싶다"고 답했다 — 다만 "부검 소견에 계획적인지 우발적인지 적을 필요는 없다, 그 부분은 굳이 언급하지 말자"는 단서를 달았다. 그래서 `ev-time-of-death`(부검 소견)·`CASE_OVERVIEW.causeOfDeath`·`PROSE_INTRO`에서 "몸싸움 중 우발적으로 벌어진 상해" 같은 해석성 문구를 전부 지우고 물리적 사실(자상 부위, 사인)만 남겼다. 반면 이현우 본인의 데이터(`characters.ts`, ai_only)에는 "몸싸움 없이, 계획한 일이었다"는 사실을 명확히 반영해 액터가 그에 맞는 태도(냉정함, 준비된 거짓말)로 연기하도록 했다.
- **CCTV 증거 전면 삭제**: `ev-cctv-gap`(21:30~22:00 공백)을 삭제하고, "이현우가 CCTV가 거의 없는 외진 곳을 워크숍 장소로 직접 제안했다"는 설정으로 대체했다 — 기존 `stmt-lee-workshop-planner`(팀원 D의 증언)에 이 장소 특징을 얹었다. "일시적 공백"보다 "애초에 감시가 없는 곳을 골랐다"는 쪽이 계획성과 더 잘 맞는다는 판단.
- **그날 밤 비(20:00~24:00) 추가**: 신발 증거의 과학적 근거를 "이슬/습기"(Phase 40)에서 "빗물"로 교체했다 — 훨씬 직관적이다. 박서연의 신발 설명도 "발 씻는 습관 + 다른 팀원 목격담"(Phase 40, 다소 작위적)이라는 설정을 사용자가 직접 "비가 왔으니 신발을 씻고 들어간 게 납득된다, 목격담 증거는 필요 없다"고 정리해줘 `stmt-park-shoe-washing`을 삭제하고 "비에 젖어서 슬리퍼를 대충 헹궈냈다"는 평범한 행동으로 단순화했다.
- **타임라인 재구성**(사용자가 직접 작성): 회식은 그대로 19:00 시작. 박서연의 편의점 외출이 21:05(문자 직후)로 당겨지고 22:15에 회식으로 복귀해 계속 마시다 23:00에 만취해 정민아의 부축을 받고 귀실 — Phase 40의 "편의점 근처에서 우연히 마주침"이라는 억지 다리가 필요 없어졌다(이전 세션에서 필자가 직접 지적했던 간극이 사용자 손으로 자연스럽게 메워짐). 이현우는 22:00에 방으로 돌아와 흔적을 지우고, 박서연 방이 잠잠해진 23:30에야 베란다 트릭을 실행한다(Phase 40의 21:55 직후 실행보다 훨씬 신중해졌다 — "살인은 순간적, 은닉은 철저히 계산적"이라는 캐릭터와 더 잘 맞는다).
- **낮 산책로 프로그램 시각 명시**: "낮(09:00~17:00)"로 구체화.
- **검증**: `npm run lint`/`npm run build` 통과. 로컬 브라우저에서 (a) 사건 브리핑 배경 문구에 "사건 당일 밤에는 20시부터 자정까지 비가 내렸다"가 정상 노출되는 것 확인. (b) 1라운드 조사 모드에 CCTV 관련 카드가 더 이상 존재하지 않는 것 확인. (c) 이현우에게 신발을 요청 → "낮 산책로 프로그램 때 묻은 거다" 1차 부인 → "비 온 날 빗물 젖은 흔적"을 짚어 재질문 → "사실 밤에 잠깐 나가긴 했는데 초입까지만, 김영훈은 못 만났다"로 정확히 한 단계만 후퇴하는 방어를 확인(이슬→빗물 wording 교체 후에도 2단계 방어 메커니즘이 그대로 작동). (d) 결과 화면 "사건의 결정적 연결고리" 콜아웃에서 "몸싸움 끝에"라는 이제는 사실과 맞지 않는 표현을 제거하고 새 타임라인(22:00 흔적 제거 → 23:30 트릭 실행)에 맞게 고쳤다.

## Phase 42: 실전 플레이 스크린샷 10장 피드백 대량 반영 (Completed)

Phase 41 배포 후 사용자가 실제로 한 판을 플레이하며 스크린샷 10장과 상세 피드백을 전달했다. 힌트성 증거 삭제, 타임라인 버그, 인내심 키워드 누락, 대화 일관성 문제 등 여러 층위의 이슈가 섞여 있어 항목별로 정리해 반영했다.

- **힌트가 너무 강한 증거 삭제/약화**:
  - `ev-room-assignment`(숙소 배정표) 완전 삭제 — 클릭 한 번으로 세 명 방 번호를 몰아 주는 카드가 있으면, 방 위치를 직접 캐물어야 하는 핵심 추리 단계 자체가 무의미해진다. 이제 방 번호는 심문으로만 알아낼 수 있다(각자 truthBibleFacts에 남겨둠).
  - `ev-balcony-strap-marks`(베란다 난간 흔적) 완전 삭제 — "아무것도 안 하고 라운드만 지나면 뜨는 물증이 사실상 범인을 지목해주는 것과 다름없다"는 지적. 3라운드 신규 공개 정보는 이제 흉기 하나뿐이다.
  - `ev-workshop-purpose`에서 "마케팅팀 전체가 신경을 곤두세우고 있었을 자리" 일반화 문장 삭제 — 동기가 3인 전부 달라져 불필요.
  - `ev-day-hike-program`(낮 산책로 프로그램) → `ev-day-schedule`(당일 프로그램 일정표)로 교체 — "그래서 신발에 흙이 묻어도 이상하지 않다"는 결론을 카드가 대신 내리지 않고, 강사 특강·점심·산책로 탐방·자유시간 같은 일정만 나열한다.
  - `ev-murder-weapon-knife` 단순화 — "등산용"이라는 수식어, "손잡이 스트랩 구멍", "누군가 옮겨다 놓은 것으로 보인다" 같은 해석성 문장을 전부 삭제. 스트랩 구멍은 사용자가 나중에 이미지로 넣을 예정이라 텍스트로 미리 설명하지 않는다.
  - `stmt-lee-workshop-planner`에서 "CCTV가 거의 설치되어 있지 않은" 문구 삭제 → "지은 지 오래된, 비교적 외진 연수원"으로 중립화 — "CCTV 없음"을 이현우가 일방적으로 정한 것처럼 읽힌다는 지적.
- **박서연-이현우 개인적 악연(인턴-사수) 설정 완전 삭제**: `ev-yearbook-sns`, `stmt-lee-park-grudge` 삭제. 박서연의 인턴 시절 성과 탈취 이력은 이현우와 무관한 그녀 개인의 서사로 재작성해 `ev-park-employment-history`(박서연의 이력, "다른 팀원의 증언"이 아니라 그녀 본인의 인사 기록으로 출처를 명시)로 옮겼다 — 이제 세 사람의 동기가 완전히 독립적이다.
- **동기 공개 진술 5종을 전부 2라운드부터 보이도록 앞당김**(`round2_end`→`round1_end`): `stmt-park-dispute-reason`, `stmt-lee-family-history`, `stmt-jeong-breakup-reason`, `stmt-lee-workshop-planner`, `ev-park-employment-history`. 사용자가 제시한 "라운드별 심증 구축" 설계(각 인물마다 R1=알리바이 없음, R2=동기, R3=물증)를 따르려면 동기 정보가 2라운드 심문 중에 이미 활용 가능해야 하는데, 기존 `round2_end` 태그는 3라운드가 되어야 노출되는 타이밍이었다.
- **휴대폰을 3인 공통 정식 요청 물품으로 승격**: 옛 "김영훈 휴대폰 통화·문자 내역"(공용 물증, 클릭 한 번으로 4건 전부 공개)을 삭제하고, `ev-phone-park`/`ev-phone-lee`/`ev-phone-jeong` 3종으로 쪼갰다 — 이제 가방·신발처럼 심문으로 요청해야만 해금되고, 요청 안 하면 "아무 정보도 없는 텅 빈 카드"가 아니라 아예 안 보인다. `ev-phone-lee`는 결정적 정황이라 채점 대상(scorable)으로 추가했다.
- **박서연 편의점 타임라인 버그 수정(중요)**: 실전 테스트에서 AI가 "9시 15분에 편의점 다녀와서 바로 회식으로 돌아왔다"고 답해, 21:10 결제~22:15 복귀 사이 약 1시간의 의도된 공백이 통째로 사라지고 사망 시각(21:45)에 완벽한 알리바이가 생겨버렸다. `characters.ts` truthBibleFacts에 "곧장 돌아왔다고 주장하지 않는다, 그 사이 뭘 했는지 스스로도 명확히 설명 못 한다"를 명시적으로 못 박아 재발을 막았다 — 재검증 결과 정확히 의도대로("그 사이에 정확히 뭘 했는지는 잘 모르겠어요... 비도 오고 해서 혼자 바람 좀 쐬고 걷다 들어온 것 같아요") 답하는 것을 확인.
- **인내심 키워드 누락 버그 수정**: 실전 테스트에서 정민아는 `patienceKeywords`에 "신발"이 아예 없어 신발을 요청해도 인내심이 전혀 오르지 않았고, 세 캐릭터 모두 "가방"·"휴대폰" 요청 시에도 인내심이 안 올랐다. 3인 전원의 `patienceKeywords`에 "가방"·"신발"·"휴대폰"을 공통으로 추가해, 어떤 소지품을 요청하든 동일하게 인내심이 오르도록 통일했다.
- **회식 종료 시각 헛소리 방지**: 실전 테스트에서 이현우가 "회식이 7시쯤 끝났다"는 시작 시각(19시)과 모순되는 답을 지어냈다 — 애초에 "회식 종료 시각"이 데이터에 정의돼 있지 않아 모델이 근거 없이 답을 지어낸 것으로 보인다. `CASE_OVERVIEW.background`에 "공식적으로 끝나는 시간이 정해져 있지 않다"는 사실을 명시해(3인 공통 프롬프트에 자동 주입됨) 안전하게 애매한 답을 하도록 유도했다.
- **대화 일관성 프롬프트 보강**: 실전 테스트에서 이현우가 방금 스스로 인정한 사실(김영훈에 대한 반감, 여동생 관련 동기)을 곧바로 다음 질문에서 뒤집어 부인하는("동기라니요? 제가 그럴 리가요") 모순된 반응을 반복적으로 보였다. `buildPatienceSection`에 "한번 인정한 사실은 나중에 다시 부인하지 않는다 — 동기·감정은 인정해도 되지만 '그래서 죽였다'는 행위만 끝까지 부인한다"는 일관성 규칙을 추가했다. 또한 인내심 2~3 단계에서 너무 일찍 완전히 침묵/거부하던 문제(아직 4~5도 안 됐는데 대답을 거부)도 단계별 지침을 더 명확히 갈라 고쳤다.
- **무고자가 형사보다 먼저 세부사항을 특정하지 않도록 보강**: 실전 테스트에서 정민아가 형사가 "살해도구"라고만 말했는데도 스스로 "칼"이라고 특정해버려, 몰라야 할 정보를 아는 것처럼 보이는 문제가 있었다. `buildPatienceSection`에 일반 규칙 추가 + 박서연·정민아 knownSecrets에 "형사가 구체적으로 말하기 전까지는 스스로 먼저 특정하지 않는다"를 명시했다.
- **디브리핑의 "숙소 위치" 질문 인식 개선**: 실전 테스트에서 플레이어가 "방 위치"라고 물었는데 디브리핑이 이를 "숙소 위치" 질문과 다른 것으로 착각해 못 알아채는 사례가 있었다. `buildDebriefRoomLayoutSection`에 "숙소"와 "방"을 같은 뜻으로 명시하고, 삭제된 베란다 흔적 언급도 제거했다.
- **`round-review`의 임의 물품 결과 문구 변경**: "사건과 무관"(단정적) → "특이할 만한 점이 보이지 않는다"(형사가 확인해봤지만 특별한 게 없었다는 뉘앙스).
- **결과 화면 "사건의 결정적 연결고리" 재작성**: 삭제된 숙소 배정표·베란다 흔적을 더 이상 언급하지 않고, "방 위치를 직접 캐물었는지"와 "가방 속 등산장비"만으로 설명하도록 다시 썼다.
- **[미해결로 남긴 것] 정민아 디브리핑 누락**: 실전 테스트 스크린샷에서 정민아만 디브리핑 텍스트가 비어 있었다. `accuse/route.ts`는 디브리핑 생성 실패 시 조용히 빈 문자열로 대체하도록 설계돼 있어(전체 결과가 깨지지 않도록 하는 의도적 방어), 일시적인 NIM API 실패였을 가능성이 높다 — 코드 구조상 재현 가능한 버그는 발견하지 못했다. 반복 재현되면 다시 살펴볼 것.

## Phase 43: 전체 코드 점검 — 프롬프트 중복/모순 및 죽은 코드 정리 (Completed)

Phase 42 배포 후 사용자가 "프롬프트 상에 중복되거나 지금 흐름과는 맞지 않는 내용이 있는지, 쓸데없는 코드가 있는지" 전체 점검을 요청했다. `actor-prompt.ts`를 시작으로 `characters.ts`/`evidence.ts`/`types.ts`/API 라우트 4개/`scoring.ts`/`patience.ts`/`InvestigationBoard.tsx`를 전부 다시 읽고 대조했다.

- **[모순 수정] 인내심 4단계 설명이 알리바이 공백 인정 원칙과 충돌**: `buildPatienceSection`의 인내심 4(최고조) 단계 설명이 "핵심 사실(알리바이 공백, 살인 관련 행적)을 스스로 인정하지는 않는다"고 돼 있었는데, 정작 같은 프롬프트의 진범 전용 절대금지 조항과 3인 전원의 `truthBibleFacts`("이 시간대는 완전히 혼자였다는 사실 자체는 절대 숨기거나 다르게 말하지 않는다")는 알리바이 공백을 인내심 수치와 무관하게 항상 순순히 인정하라고 명시하고 있었다. 서로 다른 두 곳이 정반대 지시를 내리는 형태라 모델이 헷갈릴 수 있는 실질적 위험 — 4단계 설명에서 "알리바이 공백"을 빼고 "감춰야 하는 건 살인 행위 자체뿐"으로 정정했다.
- **[중복 정리] "동기는 인정, 행위는 부인" 규칙이 프롬프트 내에서 두 번 반복**: 진범 전용 절대금지 조항(culpritClause)과 `[반응 원칙]`의 일관성 규칙(Phase 42 추가)이 거의 같은 문장("동기·감정·정황은 인정해도 되지만 '그래서 죽였다'는 행위 자체만큼은 끝까지 부인한다")을 반복하고 있었다. culpritClause는 고유한 내용(자백 문구 자체를 명시적으로 금지)만 남기고, 상세 규칙은 `[반응 원칙]` 쪽을 참조하도록 짧게 정리했다.
- **[죽은 코드 삭제] 진술 화제 선행 물증 게이트(`requiredEvidenceIds`) 전체 삭제**: `StatementEvidenceRef.requiredEvidenceIds`(types.ts)와 이를 소비하던 `buildStatementGateSection`(actor-prompt.ts)은 원래 "형사가 관련 물증을 먼저 들이밀기 전엔 진술 화제를 스스로 밝히지 않는다"는 게이트였다. 그런데 Phase 42에서 이 필드를 쓰던 유일한 항목(`stmt-lee-park-grudge`, 인턴-사수 그루지 서브플롯)이 통째로 삭제되면서, 지금 데이터엔 이 필드를 설정한 항목이 하나도 남아있지 않았다 — `buildStatementGateSection`은 항상 빈 문자열만 반환하는 죽은 함수였다. 함수·타입 필드·호출부(`buildActorSystemPrompt`의 이제 안 쓰는 `collectedEvidenceIds` 매개변수 포함)를 전부 삭제했다.
- **[죽은 코드 삭제] 물증 간 선행 게이트(`requiresEvidenceId`) 전체 삭제**: `EvidenceItem.requiresEvidenceId`(evidence.ts)와 이를 소비하던 `InvestigationBoard.tsx`의 🔒 잠금 카드 렌더링 로직도 같은 패턴이었다 — 이 필드를 설정한 물증이 현재 하나도 없어 `locked`가 항상 `false`로만 평가됐다. 필드와 잠금 UI 분기(disabled 버튼, 🔒 아이콘, "다른 물증을 먼저 확보하세요" 안내문)를 전부 삭제해 카드 렌더러를 단순화했다.
- **[stale 주석 수정]** `types.ts`의 `requestableItems` 주석이 "목록에 없는 물품(휴대폰, 지갑 등)"이라고 예시를 들고 있었는데, Phase 42에서 휴대폰이 3인 전원의 정식 등록 물품으로 승격되면서 예시가 틀린 상태로 남아 있었다 — "가방·신발·휴대폰"이 등록 물품이라고 정정하고, 폴백 문구도 옛 "사건과 무관"에서 현재 실제 문구("특이할 만한 점이 보이지 않는다")로 갱신했다. `round-review/route.ts`의 "requestableItems가 전부 가방뿐"이라던 주석도 실제로는 가방·신발·휴대폰 3종임을 반영해 고쳤다.
- **판단해서 남겨둔 것**: `truth-bible.ts`의 `CROSS_WITNESS_STATEMENTS`/`FAIR_PLAY_PRINCIPLES`는 코드 어디에서도 import되지 않는 순수 설계 문서용 export이지만, 의도적으로 남겨둔 참고 문서(01번 문서의 코드화)라 판단해 삭제하지 않았다. `text-polish.ts`/`POLISH_MODEL`도 기존처럼 사용자가 명시적으로 "나중을 위해 남겨두자"고 한 항목이라 이번 정리 대상에서 제외했다.
- **검증**: `npm run lint`/`npm run build` 통과(타입 에러 없음 — 삭제한 필드/함수/매개변수를 참조하는 곳이 남아있지 않음을 컴파일러로 재확인). 로컬 브라우저로 전체 루프를 다시 돌며 (a) 조사 모드 카드가 잠금 아이콘 없이 정상 렌더링되는 것, (b) 박서연에게 "21시부터 22시까지 어디 있었는지" 질문 시 `buildStatementGateSection` 제거 이후에도 여전히 정확한 알리바이 공백 인정 답변("혼자 있었으니까요... 정확히 어디를 얼마나 걸었는지 기억이 잘 안 나네요")이 나오는 것을 확인해, 프롬프트 정리가 기존 동작을 깨지 않았음을 검증했다.
- **검증**: `npm run lint`/`npm run build` 통과. 로컬 브라우저에서 (a) 박서연에게 "회식 이후 동선"을 묻자 21:10 편의점 결제, 22:15 회식 복귀, 그 사이 공백을 "정확히 뭘 했는지 모르겠다"고 정확히 진술하는 것을 확인(타임라인 버그 재발 안 함). (b) 이현우에게 휴대폰 요청 시 인내심이 0→1로 오르는 것, 정민아에게 신발 요청 시에도 0→1로 오르는 것을 서버 로그 없이 UI에서 직접 확인(키워드 버그 수정 확인). (c) 2라운드 진입 시 동기 공개 진술 5종("박서연의 이력" 포함)이 전부 정상 노출되고 CCTV/숙소배정표/회사행사사진 카드는 더 이상 존재하지 않는 것을 확인.

## Phase 44: 로컬 대화 품질 테스트 하네스 구축 + 시간 앵커/사망 인지/방 번호 프롬프트 수정 (Completed)

사용자가 "언커버 더 스모킹 건"(크래프톤 렐루게임즈의 상용 AI 추리 게임)을 조사해 우리 게임과 비교해달라고 요청했다 — 조사 결과 정리 후, 사용자가 "우리 게임도 맞춤법 실수·엉뚱한 답변이 종종 있다"는 실전 피드백을 주며 "전체 게임으로 테스트하는 건 토큰 낭비니, 박서연 하나만 이식한 간단한 대화 하네스를 만들어 실험해보자"고 제안했다.

- **`harness/` 폴더 신설**: Next.js 앱과 완전히 분리된 로컬 전용 스크립트 모음(Cloudflare 배포 산출물에 포함 안 됨 — devDependency `tsx` + `src/app` 밖 위치라 `next build`가 번들에 안 넣는 것까지 확인함). `harness/chat.ts`(박서연 대화형 REPL, `/polish`·`/relevance`·`/persona`·`/reset` 명령어 지원), `harness/relevance-check.ts`(답변이 질문 의도에 실제로 대응하는지 판정하는 2차 콜 프로토타입), `npm run harness` 스크립트 추가.
- **실전 재현 테스트로 발견한 3가지 프롬프트 결함**(사용자가 실제 플레이 대화 로그를 그대로 붙여넣고 지적):
  1. **시간 앵커 부재**: "그제 회식"처럼 같은 대화 안에서도 "그제"/"어제"를 오락가락 — `CASE_OVERVIEW.background`(액터 프롬프트에 항상 주입됨)에 "지금이 사건 다음날 언제인지"에 대한 기준점이 아예 없었다.
  2. **사망 인지 전제 누락**: "김영훈이 살해된 사실 모르시나요?"라는 질문에 처음 듣는 것처럼 반응 — 플레이어용 프롤로그(`PROSE_INTRO`)에는 "경찰이 유력 용의자 3명을 지목해 불러들였다"는 설정이 있는데, 액터 프롬프트로는 이 설정이 한 번도 전달된 적이 없었다.
  3. **방 번호(202호) 유출**: 묻지도 않았는데 스스로 방 번호를 언급 — 다른 비밀들과 달리 방 번호에만 "형사가 캐물어야만 밝힌다"는 게이팅 지시가 빠져있었다.
  - `truth-bible.ts`에 `INTERROGATION_CONTEXT`(ai_only, 시간 앵커+사망 인지 전제)를 신설하고 `actor-prompt.ts`의 `buildCaseSettingSection`에 주입해 1·2번을 고쳤다. `actor-prompt.ts`에 3인 공통 `buildRoomNumberDisciplineSection`을 신설해 3번을 겨냥했다.
- **재현 테스트로 드러난 한계**: `harness/dump-prompt.ts`(조립된 시스템 프롬프트를 그대로 출력하는 디버그 스크립트)로 지시문이 실제로 프롬프트에 정확히 들어가 있는 것까지 확인했는데도, 재현 테스트에서 2번(사망 인지)과 3번(방 번호)은 ESTP·ISTJ 페르소나 양쪽에서 동일하게 계속 실패했다 — 페르소나 성향 탓이라는 가설은 기각됐다. "규칙이 있는데도 안 지켜지는" 이 현상의 원인을 여러 개 가설로 정리했다(부정형 지시의 신뢰도 문제, 사실 반복 빈도 경쟁, 강한 서사적 클리셰, 긴 프롬프트에서의 희석, 모델 자체의 지시 준수 한계 — 지금 모델(`google/diffusiongemma-26b-a4b-it`)은 애초에 정확도가 아니라 속도로 선택됐다).
- **`harness/fact-check.ts` 신설**(사용자 제안): 행동 지침(인내심/소지품 처리/절대 금지/방 번호 게이팅) 없이 순수 사실(동기/비밀/진실 성서/사건 배경)만 준 상태에서 "아는 걸 전부 편하게 얘기해봐"(1단계 자유 회상) → "애매한 공백을 자연스럽게 메워봐"(2단계 취약점 프로빙)를 시켜, 오류가 "지시문 경쟁"에서 나는 건지 "이 정도 사실도 못 지키는 것"인지 층을 분리해서 진단한다. 실행 결과, 시간 앵커는 이 단계에서도 정확히 유지됐고, 사망 인지도 실제로는 이미 알고 있는 것처럼 자연스럽게 반응했다(다만 묻지도 않았는데 "억울하다"는 방어적 반응을 먼저 꺼내는 서사적 관성은 여기서도 관찰됨) — 반면 방 번호는 게이팅 규칙을 완전히 제거해도 여전히 샜고, 애매한 공백을 메울 때도 "산책로"처럼 구체적인 지명을 스스로 지어내는 경향이 확인됐다. `npm run harness:factcheck` 스크립트로 등록.
- **flow-to-ai 도입 검토 및 반려**: 사용자가 "스토리 데이터 전달 순서가 꼬여서 생기는 문제"라며 flow-to-ai(시각적 프로세스 흐름도 도구)로 그린 파이프라인(전체 타임라인→캐릭터별 분리→페르소나→행동지침→전략→상황 어긋남 확인→게임 진행)을 제안했다. 검토 결과: 이 도구의 노드 종류(start/end/process/condition/ai_agent/human/data_store)는 시스템 처리 흐름을 그리기 위한 것이라 "캐릭터×사실×시각×공개조건"이 얽힌 관계형 스토리 데이터를 담기에는 안 맞는다고 판단해 도입하지 않기로 했다. 대신 다이어그램의 핵심 통찰("상황과 어긋나는 점 있는지 확인하는 게이트가 없었다")만 취해 우리 코드베이스 안에서(하네스 확장 + 저작 체크리스트) 구현하기로 사용자와 합의했다.
- **검증**: `npm run lint`/`npm run build` 통과(harness/ 코드도 tsconfig에 걸려 `next build`의 타입체크 대상이라, `OpenAI.Chat.ChatCompletionCreateParamsNonStreaming` 캐스팅 패턴을 기존 API 라우트와 동일하게 맞춰야 했다). `harness/chat.ts`로 실제 대화를 재현해 시간 앵커 수정이 실전에서도 정확히 반영되는 것을 확인했다.

## Phase 45: 저작 원칙 기반 기존 캐릭터 데이터 재점검 + 자동 검증 도구 확장 (Completed)

Phase 44 논의 후 사용자가 "우리 방식"(flow-to-ai 대신 하네스+저작 체크리스트로 가는 방향)에 동의하며, "이미 작성된 부분들도 저작 체크리스트 기준에 맞는지 검토해달라"고 요청했다.

- **저작 체크리스트 기준으로 기존 3인 데이터 전체 재검토, 4건 확정**:
  1. 박서연·정민아의 사실 문장에 방 번호("(202호)", "(203호)")가 서술 문장 안에 인라인으로 박혀있었다.
  2. 박서연의 사실 문장에 "칼"이라는 구체적 살해도구 명칭이 게이팅 없이 그대로 쓰여 있었다(정민아는 이미 "흉기"로 중립화돼 있었는데 박서연만 놓쳤다) — Phase 44에서 재현된 "형사가 '살해도구'라고만 했는데 '칼'이라고 답하는" 버그의 실제 데이터 원인이었다.
  3. **정민아의 `truthBibleFacts`에 "그날 밤 비가 내림" 사실이 통째로 빠져 있었다** — 박서연·이현우는 갖고 있는데 정민아만 없는 완전성 결함(3인 교차검증으로 발견).
  4. **이현우는 "숙소는 302호"라는 독립된 사실이 없고, 302호가 전부 "절대 인정 안 함" 계열 문장 안에만 있었다** — 형사가 순수하게 방 번호만 물었을 때 답할 근거가 구조적으로 없었다.
  - 번호는 각자 독립된 "숙소는 N호(형사가 직접 물어야만 답한다)" 문장으로만 존재하게 하고, 나머지 서술 문장에서는 전부 제거했다. "칼"은 "물건(흉기)"으로 중립화하고 조건절 안으로 옮겼다.
- **[중요] 수정 도중 새 버그 발견 및 즉시 수정**: 정민아 문장에서 "(202호)"를 완전히 제거하자, `harness/fact-check.ts`로 재검증하는 과정에서 정민아가 "박서연 방이 몇 호였냐"는 직접 질문에 실제 정답(202호) 대신 **"205호"라는 존재하지 않는 번호를 지어내는** 새로운 회귀가 확인됐다 — 원자성을 지키려고 번호를 통째로 삭제하면서, 정민아 본인 데이터 어디에도 그 사실의 근거가 안 남게 된 것이 원인이었다. "번호를 아예 없앤다"가 아니라 "번호는 알고 있되 형사가 물어보기 전까지는 스스로 말하지 않는다"로 정정해 재발을 막았다 — 저작 원칙 자체에도 "삭제"와 "게이팅"을 혼동하지 않는다는 교훈을 추가로 얻었다.
- **`harness/data-lint.ts` 신설**(LLM 호출 없는 정적 검사, `npm run harness:lint`): 이번에 손으로 찾아낸 4가지 결함 패턴을 회귀 방지 가드로 코드화했다 — (1) 방 번호가 독립 문장이 아닌 인라인으로 등장하는지, (2) 무고자 데이터에 "칼"이 게이팅 문구 없이 등장하는지(경고), (3) 전 캐릭터가 "비" 관련 공통 사실을 갖고 있는지, (4) 전 캐릭터가 "숙소는 N호" 독립 사실을 갖고 있는지. 정규식/문자열 비교만으로 즉시 실행되어 비용이 0에 가깝다.
- **`harness/fact-check.ts`를 박서연 1인 전용에서 3인 전체로 확장**: 캐릭터별로 실전에서 확인된 약점을 겨냥한 2단계 프로빙 질문을 따로 두었다(박서연=애매한 시간 공백 메우기, 이현우=거짓 알리바이 유지 여부, 정민아=방 번호 자진 노출 여부).
- **검증**: `npm run lint`/`npm run build`/`npm run harness:lint` 전부 통과. `npm run harness:factcheck`로 3인 전체 재실행해 정민아가 이제 "202호"로 정확히 답하는 것을 확인했다. 다만 방 번호를 먼저 나서서 말하는 경향 자체(박서연·이현우)와 애매한 공백을 메울 때 "산책로" 같은 구체적 지명을 스스로 지어내는 경향은 Phase 44에서 확인된 것과 동일하게 여전히 남아있다 — 이건 데이터 문제가 아니라 모델의 지시 준수 신뢰도 문제로 보여 별도 과제로 추적한다.

## Phase 46-Fix: B(사망 인지) 해결책을 타임라인 사실 방식으로 교체 시도 (Completed)

Phase 44의 `INTERROGATION_CONTEXT`(2인칭 메타 지시문)가 재검증에서 계속 실패하자, 사용자가 "전체 타임라인에 '다음 날 08:00 경찰 출동, 13:00 심문 시작'을 추가하고 각 캐릭터 truthBibleFacts에도 넣자 — A는 이 방식으로 자연스럽게 유지하고, B도 이걸로 해결해보자"고 제안했다.

- **`truth-bible.ts`**: `INTERROGATION_CONTEXT`(ai_only 2인칭 지시문) 완전히 삭제. `TIMELINE`(문서용, 코드에서 미사용 확인됨)과 3인 전원의 `truthBibleFacts`에 "08:00 경찰 출동 — 연수원에 있는 모든 사람을 대상으로 행적 조사 시작", "13:00 용의자 심문 시작"을 다른 사실들과 동일한 형식으로 추가.
- **`actor-prompt.ts`**: `buildCaseSettingSection`에서 `INTERROGATION_CONTEXT` 주입 제거, 순수 배경 설명만 남김.
- **재검증 결과(하네스)**: A(그제/어제)는 이 방식으로도 견고하게 유지됨(직접 "어제였어요, 그제였어요?" 질문에도 정확히 답함) — `INTERROGATION_CONTEXT`의 명시적 "어제라고 말하라" 지시가 없어도 문제없었다. **B(사망 인지)는 부분적으로만 개선**됐다 — 부드러운 질문("여기 왜 계신지 아시죠?")엔 "당연히 알죠"까지는 답하지만 이어서 "무슨 일 생긴 거 아니죠?"처럼 스스로 모순되고, 직접적/대결적 질문("모르시나요?")엔 여전히 충격받은 반응이 나온다. 데이터에 사실이 정확히 주입되는 것도 재확인했으므로 배선 문제는 아니다 — "살인 사건 용의자가 소식에 충격받는다"는 서사적 관성이 프롬프트 수정만으로는 완전히 제거되지 않는 것으로 보이며, D(규칙 불이행) 논의의 연장선에서 결정론적 접근이 필요할 수 있다는 결론에 도달했다.

## Phase 47: 캐릭터별 행동 규칙을 별도 필드로 분리 (Completed)

사용자가 "캐릭터별 규칙이 어디 있는지" 물어, `actor-prompt.ts`(3인 공통 규칙, 8~9개 함수로 분산)와 `characters.ts`(캐릭터별 규칙, `knownSecrets`/`truthBibleFacts` 문자열 안에 사실과 뒤섞여 인라인으로 흩어짐) 두 층으로 나뉘어 있다고 설명했다. 사용자가 캐릭터별 규칙 쪽을 정리해달라고 요청하며 "점검은 따로 하지 말고 정리만" 하라고 명시했다.

- **`types.ts`**: `CharacterSheet`에 `behaviorRules: string[]` 필드 신설(ai_only) — "형사가 캐물으면 인정한다", "절대 스스로 밝히지 않는다" 류의 행동 지침 전용.
- **`characters.ts`**: 3인 전원의 `knownSecrets`/`truthBibleFacts`를 항목별로 검토해, 사실과 뒤섞여 있던 행동 지침을 전부 `behaviorRules`로 이동시켰다(예: "베란다에서 발견된 물건을 전혀 모른다"는 사실로 남기고, "형사가 먼저 밝히기 전까지 스스로 특정하지 않는다"는 `behaviorRules`로 분리). 내용 손실 없이 재배치만 했다 — `ActorPromptView`/`getActorPromptView`에도 `behaviorRules`를 추가했다.
  - **재배치 도중 회귀 재발견**: 정민아의 "박서연을 방까지 데려다줬다" 사실에서 "(202호)"를 무심코 다시 인라인으로 넣었다가, Phase 45에서 만든 `harness/data-lint.ts`가 즉시 잡아냈다 — 번호는 `behaviorRules`(이미 "물어보면 정확히 202호라고 답한다"를 담고 있음) 쪽에만 남기고 사실 문장에서는 다시 뺐다. 이 발견 자체가 방금 만든 회귀 방지 가드의 실효성을 증명한다.
- **`actor-prompt.ts`**: `buildActorSystemPrompt`에 `[캐릭터별 행동 규칙 — 위 사실들을 언제·어떻게 말할지에 대한 지침]` 섹션을 신설해 `character.behaviorRules`를 주입 — 이제 사실(`knownSecrets`/`truthBibleFacts`)과 행동 지침(`behaviorRules`)이 프롬프트 안에서도 명확히 분리된 섹션으로 존재한다.
- **검증**: `npm run lint`/`npm run build`/`npm run harness:lint` 통과 확인. 사용자 요청대로 하네스 기반 행동 재검증(실제로 지금까지와 동일하게 작동하는지)은 이번 라운드에서는 수행하지 않았다 — 다음 실전 테스트 때 함께 확인 필요.

## Phase 48: 캐릭터 데이터에 실제 달력 날짜 부여 (Completed)

사용자가 프롬프트 조립 방식(함수로 문자열을 이어붙이는 것뿐, AI가 관여하지 않음)에 대해 질문한 뒤, "어제/오늘 같은 상대 표현 자체는 문제 없지만, 아예 7월 22일(워크숍·회식·살인)과 7월 23일(발견·조사·심문)이라는 실제 날짜를 부여해보자"고 제안했다.

- **`truth-bible.ts`**: `CASE_OVERVIEW.timeOfDeath`("7월 22일 밤 21:45경"), `discoveredAt`("7월 23일 06:00"), `background`("7월 22일~23일 1박 2일")에 날짜 반영. `TIMELINE`(문서용)에도 첫 항목과 06:00/08:00/13:00 항목에 날짜를 붙였다.
- **`characters.ts`**: 3인 전원의 `truthBibleFacts` 맨 앞에 "이번 워크숍은 7월 22일~23일 1박 2일 일정이다 — 7월 22일 하루 종일 프로그램과 회식이 있었고 그날 밤 김영훈이 사망했다. 7월 23일 아침 시신이 발견되어 조사가 시작됐다"를 앵커 사실로 추가하고, 첫 시간 항목과 06:00/08:00/13:00 항목에 날짜를 명시했다.
- **검증**: `npm run lint`/`npm run build`/`npm run harness:lint` 통과. `harness/chat.ts`로 "오늘이 며칠이에요?" / "회식은 며칠이었어요?"를 물어본 결과 "오늘 7월 23일이죠" / "어제, 7월 22일 저녁에 했어요"로 정확히 답하는 것을 확인 — 추상적 상대 표현("어제")과 구체적 날짜("7월 22일")를 혼동 없이 동시에 정확히 다뤘다.

## Phase 49: "정보는 주고 지시로 덮는" 방식에서 "정보 자체를 안 주는" 방식으로 전환 (Completed)

이전 턴에서 사용자가 "칼"이라는 단어만 빼는 걸로는 부족하다고 지적했다 — "베란다에서 발견된 물건(흉기)에 대해 전혀 모른다" 같은 문장도 결국 "흉기 발견"이라는 개념 자체를 캐릭터 데이터에 상시 심어두고 "모르는 척 하라"는 태도만 덧씌운 것이라, 근본적으로 같은 문제(정보와 억제 지시가 공존)라는 지적이었다. 사용자는 명확한 원칙을 제시했다: **무고자가 외부에서 발견된 사실(흉기 발견 자체)을 모른다면, "모르는 척 하라"고 연기 지시를 얹을 게 아니라 그 정보 자체를 애초에 프롬프트에 주지 않아야 한다.** 반면 진범(이현우) 본인의 행동은 예외다 — 그는 실제로 안 것이고, "이현우는 실수해도 된다(페르소나에 따라 실토하거나 안 하거나 갈릴 수 있음)"는 게 의도된 설계이므로 "알지만 숨기라"는 기존 방식을 유지한다.

- **박서연**: knownSecrets/behaviorRules/truthBibleFacts에서 "베란다에서 발견된 물건(흉기)에 대해 전혀 모른다", "칼이라는 것은 형사가 먼저 말하기 전까지 안 쓴다" 류의 문장을 전부 삭제 — 흉기/베란다 발견에 대한 어떤 개념도 그녀의 데이터에 남지 않는다. 잔여 유용 정보("23:00 방으로 돌아와 바로 잠들었다")는 인접한 사실로 옮겨 보존했다.
- **정민아**: 동일하게 "베란다에서 발견된 흉기에 대해서는 아는 바가 전혀 없다", "살해도구가 무엇인지는 형사가 먼저 말하기 전까지 특정하지 않는다" 류를 전부 삭제. "방 안에서 뭘 보지 않았다"는 잔여 사실은 인접 문장에 흡수했다.
- **이현우**: 그대로 유지 — 베란다 끈 트릭과 흉기(칼) 관련 기억은 그의 truthBibleFacts에 남아있고, "형사가 캐물으면 결국 인정할 수밖에 없다"는 기존 게이팅 방식 그대로다.
- **`harness/data-lint.ts` 규칙 2 강화**: 기존엔 "'칼'이 게이팅 문구 없이 등장하면 경고"(소프트 체크)였는데, 이제 "무고자 데이터(knownSecrets/behaviorRules/truthBibleFacts 전체)에 '칼' 또는 '흉기'가 조금이라도 존재하면 하드 실패"로 강화했다 — 게이팅 여부와 무관하게 존재 자체를 차단한다. 진범은 검사 대상에서 제외.
- **검증**: `npm run lint`/`npm run build`/`npm run harness:lint` 통과. `harness/chat.ts`로 "베란다에서 뭐 발견된 거 있어요?" / "혹시 흉기가 뭔지 아세요?"를 물어본 결과, 박서연이 "흉기라니요? 갑자기 제가 그런 걸 어떻게 알아요!"처럼 별도의 "모르는 척 하라"는 지시 없이도 자연스럽게 완전한 무지 반응을 보이는 것을 확인했다 — 정보 자체가 없으니 진짜로 모르는 반응이 나온다.

## Phase 50: 박서연의 애매한 1시간 공백에 확정 답 부여 (Completed)

이현우의 "칼" 말실수 테스트(하네스에 `/character` 명령어 신설 — park/lee/jeong 전환 가능) 결과, 11회 시도 중 형사가 먼저 "칼"을 언급하지 않는 한 단 한 번도 스스로 말하지 않는 것을 확인했다 — 사용자가 원했던 "가끔 실수"는 관측되지 않았지만, 사용자가 "1번(이현우 문제)은 지금 이대로 두자"고 결정했다.

이어서 배포 전 미해결 문제를 점검하던 중, Phase 45에서 발견했던 "박서연이 21:10~22:15 공백을 메울 때 '산책로'라는 구체 지명을 스스로 지어내는 경향"을 다시 짚었다. 사용자가 이 공백에 확정된 답을 채워넣기로 결정했다 — 편의점 이후 성과 스트레스로 연수원 주변을 걸었을 뿐 산책로에는 가지 않았다는 구체적 사실. 이현우와의 동선이 산책로에서 겹칠 수도 있다는 붉은 청어(red herring) 가능성 하나는 포기하지만, 모델이 없는 사실을 지어내는 위험을 없애는 쪽을 택했다.

- **`characters.ts`**: 박서연의 truthBibleFacts/behaviorRules에서 "본인도 명확하게 알지 못한다"는 의도적 애매함을 "머리가 복잡하고 성과 문제로 스트레스를 받아 연수원 주변을 정처 없이 걸어다녔다 — 산책로 쪽으로는 가지 않았다"는 확정 사실로 교체.
- **`harness/chat.ts` 확장**: `/character <park|lee|jeong>` 명령어 신설 — 지금까지 박서연 고정이었던 하네스로 3인 전원을 전환하며 테스트할 수 있게 됐다.
- **검증**: `npm run lint`/`npm run build`/`npm run harness:lint` 통과. 하네스로 직접 질문(편의점 이후 행적), 유도 질문("산책로 가신 거 아니죠?"), 완전 자유 서술(2회) 등 5개 시나리오를 반복 테스트한 결과 전부 일관되게 "성과 스트레스로 연수원 주변을 걸었다, 산책로는 안 갔다"고 답했고 "산책로"를 스스로 지어내는 사례는 한 번도 재현되지 않았다.

다음 단계(사용자 지시): 이 항목이 안정적으로 확인됐으니 맞춤법/오탈자/엉뚱한 답변 문제(원래 이번 세션의 출발점, `harness/relevance-check.ts`·`text-polish.ts` 프로토타입은 있지만 아직 실제 게임에 미연결)를 다음으로 살펴본다. 커밋·배포는 그 이후까지 보류.

## Phase 51: 생성 전 품질 지침 + temperature 하향 실험 (Completed)

사용자가 상용 LLM 서비스들의 "전처리(품질 지침 프롬프트) + 낮은 temperature + 후처리 4종" 조합을 실험해보자고 제안했다. 배포 전 미해결 3번 항목(맞춤법/오탈자/엉뚱한 답변)에 대한 대응이다.

- **`src/lib/nim-client.ts`**: `ACTOR_TEMPERATURE` 상수 신설(1 → 0.2). `interrogate/route.ts`와 `harness/chat.ts`가 이 상수 하나를 공유 — 값을 바꾸면 둘 다 같이 바뀐다.
- **`src/lib/prompts/actor-prompt.ts`**: `buildGenerationQualitySection()` 신설, `buildActorSystemPrompt`에 주입. 사용자가 제시한 7개 항목 중 "포맷 유지"는 기존 `[출력 형식]`/`[절대 금지]`와 중복이라 뺐다 — 문법 자연스럽게, 의도 추론, 애매하면 확인, 반복 줄이기, 과신 금지 5개 항목만 반영.
- **`src/app/api/interrogate/route.ts`**: `temperature: 1` → `ACTOR_TEMPERATURE`로 교체(실제 배포 경로에 반영됨 — 아직 커밋/배포는 안 함).
- **`harness/chat.ts`**: `ACTOR_TEMPERATURE` 사용하도록 교체. 사용자가 "후처리 4종을 항상 켜둔 채 점검하자"고 해서 `polishEnabled`/`relevanceEnabled`/`safetyEnabled` 기본값을 전부 `true`로 변경. 포맷 검증(LLM 콜 없는 순수 로직, `isFormatValid`) 신설 — 빈 응답이나 브라켓 잔재가 남으면 1회 재생성.

## Phase 52: 안전성 검사(safety-check.ts) 신설 + 버그 2건 수정 (Completed)

후처리 4종 중 우리 프로젝트에 없던 "안전/정책 검사"를 `harness/safety-check.ts`로 신설(`relevance-check.ts`와 동일 구조, fail-open). 실전 테스트에서 두 가지 버그가 즉시 드러났다.

- **버그 1 — 정상 대사를 차단**: 평범한 회식 얘기인데 "아니오 — 안전함"이라며 모순된 판정을 내렸다. 원인은 질문 프레이밍 실수 — "부적절한 내용이 있는지 판단하라"고 물어놓고 출력 규칙은 "아니오=부적절함"으로 정해, 질문의 자연스러운 답("아니오=없다=안전")과 내가 정한 답 규칙이 정반대였다. 질문 자체를 "이 대사가 적절한가"(긍정형)로 바꿔 "예=적절함"이 자연스럽게 맞아떨어지도록 정렬했다.
- **버그 2 — 피해자 이름(김영훈)을 실존 인물로 오인해 차단**: 안전성 검사기에게 "이 게임은 가상 설정"이라는 맥락이 전혀 없었던 게 원인. 사용자가 "스토리 전체를 RAG처럼 넣으면 어떨지" 제안했는데, 검토 결과 등장인물이 3인+피해자 1인뿐인 규모라 검색/임베딩 인프라(RAG)는 과하다고 판단 — 대신 `CHARACTER_LIST`/`CASE_OVERVIEW`에서 이름만 동적으로 뽑아 명단으로 주입하기로 했다(하드코딩 아님 — 소스 데이터가 바뀌면 자동으로 같이 바뀐다).
- **검증**: `npm run lint`/`npm run build` 통과. `harness/chat.ts`로 안전성 검사만 격리해서 재현 테스트한 결과 두 버그 모두 "예 — 적절함"으로 정상 통과하는 것 확인.
- **[중요/미해결] API 레이트리밋으로 전체 파이프라인 종단 검증을 못 마쳤다**: NVIDIA NIM 무료 티어가 API 키당 분당 약 40 요청(RPM)으로 제한되는데, 지금 파이프라인은 한 턴에 최대 4~5콜(생성→포맷 재시도→교정→관련성 검증→안전성 검사)이 나간다. 이번 세션에서 반복 테스트하다가 실제로 429(Too Many Requests)를 맞았다 — 개별 버그(안전성 검사 로직)는 격리 테스트로 확인했지만, **4단계를 동시에 켠 상태로 여러 턴 연속 대화하는 종단 시나리오는 레이트리밋 때문에 끝까지 검증하지 못했다.**

### 다음 세션 인수인계 (여기서부터 이어갈 것)

- **지금 상태**: `harness/chat.ts`는 temperature=0.2 + 생성 전 품질 지침 + 후처리 4종(포맷/교정/관련성/안전성) 전부 기본 on. `interrogate/route.ts`도 temperature=0.2와 생성 전 품질 지침은 이미 반영됨(실제 배포 경로). 다만 **후처리 4종(polishText/checkRelevance/checkSafety)은 아직 `harness/`에만 있고 실제 게임(`interrogate/route.ts`)에는 연결 안 됨.**
- **다음에 할 것(사용자 지시: "3번은 후처리·전처리 작업, 순서대로")**:
  1. API 레이트리밋이 풀리면, 4단계를 다 켠 상태로 하네스에서 여러 턴짜리 대화를 끝까지(레이트리밋 없이) 재검증 — 특히 `temperature=0.2`가 페르소나별 표현 다양성을 과도하게 죽이지는 않는지 확인 필요(아직 확인 못 함).
  2. 검증되면 `polishText`/`checkRelevance`/`checkSafety`를 실제 `interrogate/route.ts`에 연결할지 결정. 연결한다면 매 턴 4~5콜이 나가는 구조가 실제 게임(여러 명이 동시에 플레이)에서도 레이트리밋 문제를 일으킬 수 있어, 다 켤지/일부만 켤지(예: 안전성 검사는 랜덤 샘플링만) 같은 절충안도 같이 논의 필요.
  3. 3번이 끝나면 4번(정민아 디브리핑 빈 문자열, Phase 42부터 미해결)과 5번(채점 취약점, Phase 38부터 미해결)을 순서대로 확인.
- **커밋/배포는 아직 하지 말 것** — 사용자가 "3번까지 마무리하고 하자"고 명시적으로 보류함. 지금 우클 상태(git status)는 `characters.ts`/`truth-bible.ts`/`types.ts`/`actor-prompt.ts`/`nim-client.ts`/`interrogate/route.ts` 수정 + `harness/`(신규, 미추적) — 전부 아직 커밋 안 된 로컬 변경 상태.
- **하네스 사용법 요약**: `npm run harness`(대화형, `/character park|lee|jeong`으로 캐릭터 전환), `npm run harness:factcheck`(3인 자유회상+취약점 프로빙), `npm run harness:lint`(정적 데이터 검사, LLM 콜 없음).

## Phase 53: 후처리 3종 통합으로 API 레이트리밋 대응 (Completed)

Phase 52에서 미해결로 남긴 레이트리밋 문제("4단계를 동시에 켠 상태로 여러 턴 검증 못 함")를 사용자가 최우선으로 지목 — "지금 한 번에 보내는 콜이 너무 많다, 쳐낼 수 있는 콜을 찾아 35±5콜 체제로 줄이자"는 구체적 목표를 제시했다.

- **콜 인벤토리 분석**: 기존 파이프라인은 턴당 필수 콜이 생성(1)+교정(`polishText`)+관련성(`checkRelevance`)+안전(`checkSafety`) = **4개**였고, 포맷 재시도·관련성 실패 재생성까지 겹치면 최대 7개까지 뛰었다. 여러 턴을 빠르게 이어가면 NIM 무료 티어(40RPM)를 금방 넘긴다.
- **`harness/quality-check.ts` 신설**: 교정+관련성+안전 3개 판정을 콜 1개로 병합(`runQualityCheck`). 세 작업 모두 "주어진 텍스트를 보고 판정하거나 표면만 다듬는" 동질적 작업이라(Phase 9에서 실패했던 "롤플레이 대사 생성 + 판정을 한 호출에 동시 요구"— 창작과 분류라는 이질적 작업을 섞은 경우—와는 성격이 다르다고 판단) 병합에 따른 신뢰도 저하 위험이 낮다고 봤다. 출력 형식은 `관련성:`/`안전:`/`교정:` 3줄 고정 포맷, 줄 단위로 파싱해 순서와 무관하게 매칭되도록 했다. 개별 프롬프트 판단 기준 문구는 기존 `relevance-check.ts`/`safety-check.ts`와 최대한 동일하게 유지(안전성 검사의 등장인물 명단 동적 주입 로직도 그대로 이식).
- **`harness/chat.ts` 파이프라인 교체**: 턴당 필수 콜을 생성(1)+통합검수(1) = **2개**로 축소. 관련성 실패 시 기존과 동일하게 1회 재생성 후 통합검수를 다시 호출(재검증 결과를 신뢰), 안전 실패 시 재생성 없이 고정 폴백 문구로 즉시 대체(기존 판단 유지). `/polish`/`/relevance`/`/safety` 3개 토글을 `/quality` 하나로 통합.
- **죽은 코드 삭제**: `harness/relevance-check.ts`, `harness/safety-check.ts`를 `quality-check.ts`로 완전히 대체한 뒤 삭제(둘 다 `chat.ts`가 유일한 소비자였고, 대체 후 참조가 하나도 안 남는 것을 grep으로 확인). `text-polish.ts`(src/lib)는 Phase 35 때부터 "나중을 위해 남겨두자"는 사용자의 명시적 선례가 있어 이번에도 그대로 유지 — 병합 이후 어디서도 안 쓰이지만 삭제 대상에서 제외했다.
- **검증**: `npm run lint`/`npx tsc --noEmit` 통과. `harness/chat.ts`로 2턴 대화 재현 — 턴당 정확히 생성 1콜+통합검수 1콜만 나가는 것을 로그로 확인했고, 관련성/안전 판정도 정상적으로 "예"로 통과하며 교정 결과도 원문과 의미 손실 없이 그대로 나오는 것을 확인했다.
- **예상 콜 예산**: 기본 케이스 턴당 2콜, 재시도 겹친 최악 케이스도 턴당 4~5콜 — 10턴 안팎의 종단 시나리오라면 20~35콜 선에서 끝나 목표(35±5)에 부합한다.
- **다음 단계**: 이 축소된 예산으로 Phase 51-52 인수인계 1번 항목(4단계 켠 채 여러 턴 종단 재검증, temperature=0.2 표현 다양성 확인)을 이어서 진행한다.

### 종단 재검증 결과 (인수인계 1번 항목 완료)

- **temperature=0.2 표현 다양성 확인**: 박서연(ESTP)에게 동일 질문("회식 끝나고 몇 시에 방에 들어갔어요?")을 `/reset`으로 3회 반복한 결과, 사실 내용은 물론 문장 구조·어휘까지 거의 동일하게 반복됐다(예: "부축해서 방까지 데려다줬거든요", "기절하듯이 잠들어버려서"가 3회 모두 그대로 등장). **temperature=1로 임시 변경해 동일 테스트를 재실행했으나 결과는 사실상 동일**했다 — 즉 이 반복성은 temperature 설정 때문이 아니라 모델(`google/diffusiongemma-26b-a4b-it`) 자체가 사실관계가 명확히 고정된 질문에는 낮은 표현 다양성을 보이는 특성으로 판단된다. **결론: temperature=0.2로 낮춘 것이 표현 다양성을 추가로 해치지 않으므로(temperature=1에서도 이미 낮았음), 결정론적 이득만 취하고 그대로 유지한다.** (테스트 후 즉시 0.2로 원복 확인)
- **통합 검수 파이프라인 종단 동작 확인**: 이현우(ESTP)에게 `patienceKeywords`(베란다/칼/흉기/방 배정)를 순서대로 건드리는 6턴 압박 시나리오를 실행 — 인내심이 1→2→3→4로 정상 상승했고(4/5에서 두 개 남은 질문은 새 키워드가 아니라 인내심이 더 오르지 않음, 정상), `runQualityCheck`가 실제로 발생한 "몰아 몰아붙이시면" 같은 중복 단어 글리치를 "몰아붙이시면"으로 의미·어조 손상 없이 정확히 교정하는 것을 확인했다 — 관련성/안전 판정도 6턴 내내 전부 "예"로 정확히 통과했다. 턴당 정확히 2콜(생성+통합검수)만 발생해 재시도 없이도 파이프라인이 안정적으로 동작함을 확인했다.
- **다음 결정 필요 사항(사용자 확인 대기)**: 후처리(`quality-check.ts`)를 실제 `interrogate/route.ts`에 연결할지 여부. 연결하면 실제 플레이 중 턴당 API 호출이 1→2로 늘어나는데, 하네스 단독 테스트와 달리 실제 이벤트에서는 여러 참가자가 동시에 여러 캐릭터와 대화하므로 40RPM 한도에 더 쉽게 부딪힐 수 있다 — 연결 여부/전체 적용 vs 샘플링(예: 안전성만 랜덤 일부 턴에) 여부를 사용자와 논의 후 결정.

## Phase 54: 통합 후처리를 실제 배포 경로에 연결 + 커밋/배포 (Completed)

사용자가 "40RPM은 한 턴이 아니라 API 키 공유 총량 문제"라는 설명을 듣고 "혼자 플레이하면 지금 사양으로 문제없다"고 확인, 곧바로 커밋·배포를 지시했다. "하네스도 최종 변경된 사양으로 해볼 수 있게 해달라"는 요청은 하네스와 실제 배포 경로가 같은 모듈을 공유해야 한다는 뜻으로 해석해, 이번에 `quality-check.ts`를 프로덕션에 실제로 연결했다.

- **`harness/quality-check.ts` → `src/lib/quality-check.ts` 승격**: 하네스 전용 프로토타입이었던 통합 검수 모듈을 공유 위치로 옮겼다 — 이제 `harness/chat.ts`와 `interrogate/route.ts`가 완전히 동일한 `runQualityCheck` 구현을 쓴다. `isFormatValid`(포맷 검증, LLM 콜 없음)와 `SAFETY_FALLBACK_TEXT`(안전 실패 시 대체 문구)도 같은 파일로 옮겨 두 경로가 하나도 다르지 않게 했다.
- **`src/app/api/interrogate/route.ts`**: 기존엔 생성 콜 1개 후 바로 텍스트를 반환했는데, 인라인이던 생성 로직을 `generateOnce(extraReminder?)` 헬퍼로 추출해 재생성 시(포맷 무효/관련성 실패) 재사용하도록 리팩터링했다. 그 뒤 harness/chat.ts와 동일한 흐름을 그대로 적용: ①포맷 검증 실패 시 1회 재생성 → ②통합 검수(교정+관련성+안전, 콜 1개) → ③관련성 실패 시 1회 재생성 후 재검수 → ④안전 실패 시 재생성 없이 고정 폴백. 턴당 기본 콜이 1→2로 늘지만, 사용자가 확인한 대로 혼자/소수 플레이 상황에서는 40RPM 한도에 문제되지 않는다 — 코드 상단 주석에 이 트레이드오프와 "다수 동시 플레이 시 재검토" 조건을 명시해뒀다.
- **검증**: `npm run lint`/`npx tsc --noEmit`/`npm run build` 전부 통과. `npm run harness`로 공유 모듈 경로 변경 후에도 정상 동작 확인. 로컬 `next dev` 프리뷰로 실제 브라우저에서 조사 시작 → 박서연 심문 1턴을 끝까지 재현해, 서버 로그에서 `relevance=true safety=true`(전체 파이프라인 정상 통과)와 `elapsedMs=974`(생성+통합검수 합산, 여전히 1초 미만)를 확인했다.
- **커밋**: `Phase 44-54: 대화 품질 테스트 하네스 구축 + 통합 후처리 파이프라인 연결` — Phase 44부터 이번까지 쌓여있던 로컬 변경(harness/ 신규, characters.ts/truth-bible.ts/types.ts/actor-prompt.ts/nim-client.ts/interrogate/route.ts 수정, quality-check.ts 신규)을 한 번에 커밋했다.
- **배포**: `npm run deploy`(OpenNext + Wrangler, Cloudflare Workers) 실행 — 최초 시도는 이 세션이 비대화형이라 `CLOUDFLARE_API_TOKEN` 부재로 실패했고, 사용자가 직접 `npx wrangler login`으로 로그인한 뒤 재시도해 성공했다. `https://murder-mystery.squapple.workers.dev`에 배포 완료(Version ID `33c81d9c-ed54-4f92-8cb9-734d433a1e6d`).
- **배포 후 실전 검증**: 배포된 URL에 직접 `/api/casting` → `/api/interrogate` 순서로 실제 요청을 보내 200 응답과 1.63초 응답 시간을 확인했다 — 로컬 하네스/프리뷰뿐 아니라 실제 프로덕션 경로에서도 통합 후처리 파이프라인이 정상 동작함을 최종 확인.
